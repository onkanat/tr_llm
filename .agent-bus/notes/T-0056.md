# T-0056 Notu: KristalDataset.get_batch Testleri ve PAD Yoğunluğu Belgelemesi

**As-Of Damgası:** 2026-09-16T20:48:00Z  
**Sorumlu:** antigravity  
**Kapsam:** `KristalDataset.get_batch` metodunun şekil, kaydırma (+1 token), int64 dtype, küçük veri geri düşme yolu ve PAD yoğunluğu açısından test edilmesi; hiçbir üretim kodunun değiştirilmemesi.

---

## 1. Kiralama Durumu (Lease Status)
Tüm işlemler yazım öncesinde kiralanan dosyalar üzerinde yürütülmüştür:
- `tests/test_kristal_dataset.py` (file)
- `data/eval/t0056_kristal_dataset_2026-09-16.json` (file)
- `.agent-bus/notes/T-0056.md` (file)

---

## 2. Üretim Kodu Dokunulmazlığı
- `scripts/train_step_demo.py` dosyasına hiçbir yazım yapılmamış, salt okunur tutulmuştur.
- `sha256`: `1bac5e9263faca86e513d83ed1256dcc04305f741f86b714bc1f115b4e7f5dd7` (T-0055 ile bit düzeyinde özdeş).

---

## 3. Test Paketi (`tests/test_kristal_dataset.py`)
5 vaka oluşturulup doğrulandı:
1. `test_dataset_batch_shape`: x ve y tensörlerinin şekli `[batch_size, block_size]` doğrulanır.
2. `test_dataset_shift_property`: y'nin x'in tam 1 token ileri kaydırılmış hali olduğu (`y[b, k] == data[offset_b + k + 1]`) veriden bulunarak test edilir.
3. `test_dataset_dtypes`: x ve y'nin PyTorch embedding katmanının beklediği `torch.int64` türünde olduğu test edilir.
4. `test_dataset_small_data_fallback`: `len(data) <= block_size` durumunda fallback mantığının çökmeden `[batch_size, len(data) - 1]` boyutunda doğru kaydırılmış tensörler ürettiği doğrulanır.
5. `test_dataset_pad_density_measurement`: `data/train_chat_balanced.bin` üzerinde pencerelerdeki `<PAD>` oranı ölçülüp belgelenir (`> 0.30` gevşek eşiği ile pinlenir).

**Ölçülen PAD Yoğunluğu:**
- 200 pencere (25.600 hedef tokenı) üzerinde ölçülen `<PAD>` (ID: 1) sayısı: **12.684**.
- Ölçülen oran: **%49.55** (~%47.8 - %50 bandı teyit edildi).

---

## 4. Mutant Doğrulaması (Fonksiyon Seviyesi)
- Kaydırma satırı `y = data[i:i+block]` şeklinde bozulduğunda:
  - `AssertionError: y kaydırılmamış (x ile özdeş)` fırlatılarak test **RED** olur.
- Gerçek kaydırma satırında (`y = data[i+1:i+1+block]`):
  - Test sorunsuz **GREEN (PASS)** geçer.

---

## 5. Test Paketi Koşumu
- `pytest tests/test_kristal_dataset.py`: **5 passed in 0.64s**.
- `pytest -q tests/`: **206 passed in 9.88s** (tüm depodaki 206 test eksiksiz yeşil).

---

## 6. Kanaryalar
- Hiçbir eğitim/üretim koşumu yapılmadı.
- Hiçbir `.pt` veya `.bin` dosyası yazılmadı (`data/*.pt`: 29, `data/*.bin`: 15 korundu).
