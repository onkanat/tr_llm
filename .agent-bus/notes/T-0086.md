# T-0086 — Ölü atıf denetimi KALICI araç oldu · yürütüm notu

**Tarih:** 20 Eyl 2026 · **Yürütücü:** claude · **Kapsam:** yalnız ÖLÇÜM ARACI (KOD değişmedi)
**Kaynak:** T-0083 raporu §8, açık madde **3**
**Rapor:** `data/eval/anka_r9_olu_atif_denetimi_2026-09-20.md`

---

## 0. Kural

Kapılar **D1…D6 + iki çürütme** ölçümden **önce** ilan edildi, **değiştirilmedi**.
Bu turda çıkan **dört kusur da benim** ve **dördü de rapora yazıldı** (§4); hiçbiri
sessizce geçmedi çünkü kapılar **fail-closed** yazılmıştı.

## 1. Ölçülen zemin (yazmadan ÖNCE)

- **Kanonik kaynak VAR:** `data/eval/t0069_pre_delete_manifest.json` → `hedefler[51]`;
  digest `4e1c4d51…` kayıttaki `manifest_sha256` ile **birebir**.
- **Ağaçta doğal pozitif kontrol YOK:** 1138 dosya tarandı ⇒ "etiketli blok içinde ölü
  yol anan" **0 dosya**. Kural sınanmadan bırakılsa **vakum geçiş** olurdu.
- **İki tuzak ölçülmüş:** (a) T-0083 şişmesi (emekli notu yolu adıyla anar),
  (b) T-0085 §2.2: `grep` = **ugrep** ve `.gitignore`'a uyar ⇒ `scratch/` sessizce kaçar.

## 2. Yapılan

| # | Yer | Ne |
|---|---|---|
| A | `scratch/anka_r9_olu_atif_denetimi.py` | Asıl araç: manifestten ölü küme, **ağacı kendisi yürür**, emekli ayıklama, kapsam etiketli rapor, `rc=0/2` |
| B | `scratch/anka_r9_kanarya.{py,md}` | **İlan edilmiş** pozitif kontrol fikstürü (yakın-kaçıran blok dahil) |
| C | `data/eval/anka_r9_…json` + `.md` | Makine okunur çıktı + rapor |

**Araç `rc=0`** ve `hatalar: []`. İki tasarım ilkesi:
1. **Dış `grep` KULLANILMAZ** ⇒ ignore kuralları kapsamı sessizce daraltamaz.
2. **Araç metni hiç ölü yol adı TAŞIMAZ** ⇒ kendini şişirmez (T-0083 tuzağı, inşa gereği).

## 3. Ölçülen sonuç

```
KOD 43/171/0/171 · SCRATCH 85/406/0/406 · KANARYA 2/7/3/4 · BELGE 98/193/0/193
KAYIT 101/1529/0/1529 · TOPLAM 329/2306/3/2303 · KANARYA HARİÇ 2299 (dosya/ham/emekli/olu)
```

**Kontroller:** kanarya `.py` **4/1/3** ve `.md` **3/2/1** — ilan edilenle **birebir**;
negatif `chat_prompt.py` `olu == ham == 11` ⇒ **aşırı ayıklama yok**.

**BULGU:** gerçek ağaçta **ayıklanan satır = 0**. Kural **canlı ama işi yok**: ilan
edilen iki kalıp, ölü yolu anan etiketli blok bulmuyor. Kuralı "yakınında emekli
kelimesi" diye **gevşetmek reddedildi** (ölçüt keyfî olur, canlıyı gizler). Bu, aracın
**ilan edilmiş sınırıdır** — yeni bir işaretleme deyimi doğarsa kurala eklenmelidir.

## 4. KENDİ KUSURLARIM (dördü de)

| # | Kusur | Nasıl çıktı |
|---|---|---|
| 1 | Pozitif kontrolü **atfını bir tur önce kaldırdığım** dosyaya koydum ⇒ **VAKUM** | Araç `rc=2` verdi; kontrol kanaryaya taşındı |
| 2 | **BİRİM** yanlıştı: tam yol (51) dizin-öneksiz atfı kaçırıyor (`chat_prompt.py` 10 vs 11) | Taban ada (46) çevrildi — **hiçbir ölçüm kabul edilmeden önce** |
| 3 | Kanaryada **yakın-kaçıranı etiketli bloğun İÇİNE** koydum ⇒ kalıp sınaması boşa giderdi | Bloklar ayrıldı; kanarya `emekli=1` kalıbın **reddettiğini** kanıtlıyor |
| 4 | **İki sondamın beklentisi** yanlıştı (C, C5) — araç doğru davrandı | §6'nın **yapı bulgusu** (digest kapısı şema kapısının ÜSTÜNDE) bu hatadan çıktı |

## 5. D4 — fail-closed dalı ölmedi

**8/8** dal sınandı (modül **import** edilerek, kopyalanmadan): A yok · B digest kayması ·
C1–C5 şema (hedefler yok/boş, `yol` yok, `n_hedef` uyuşmaz, kayıtta digest yok) ·
**D pozitif dal DURMADI**. Yapı: **digest kapısı şema kapısının ÜSTÜNDE** ⇒ tek kaynağı
bozmak şema kapısına **ulaşamaz**; C1–C5 için **iki kaynak birlikte** bozulmalıdır.

## 6. Kapanış

- Kiralamalar: araç + iki kanarya + JSON + rapor + not ⇒ **boşaltıldı**.
- `changed_files` **6**, iki yönlü, `len(cf) == len(set(cf))`.
- **KOD DEĞİŞMEDİ**: `train.py`, `chat_prompt.py`, `test_model.py`, `src/**`,
  `scripts/**`, `tests/**` — bulunan ölü atıflar **düzeltilmedi, sayıldı**.
- `git add -A` **kullanılmadı**; commit/push **operatör yetkisinde**.

## 7. İlan edilmiş kapsamdan sapma — beyan edilir

İki kanarya fikstürü görev `writes[]`'inde **yoktu**; tasarım ölçümden sonra zorunlu
hale geldi (§3.1) ve `bus_post_task`'ın **güncelleme karşılığı yoktur**. Dosyalar için
**ayrıca kiralama alındı** (SPEC kural 1 karşılandı) ve `changed_files`'ta **açıkça
beyan edildi**; kapı **sessizce gevşetilmedi**.
