# T-0087 · Yürütücü Notu — LIVE okuma varsayılanları: ölü model yolu + bayat sözlük (fail-closed)

**Yürütücü:** claude · **Tarih:** 20 Eyl 2026 · **Görev:** `.agent-bus/state/tasks/T-0087.json`
**KAYNAK:** T-0085 raporu §3 (LIVE/TARİHSEL sınıflandırması) + T-0083 raporu §8 (açık maddeler)
**Kapsam:** KOD kapsamındaki 171 ölü atfın **YALNIZ LIVE okuma varsayılanı** alt sınıfı.

---

## §0 · Kural (değişiklikten ÖNCE ilan edildi, DEĞİŞTİRİLMEDİ)

Kapılar `T-0087.json` içine, herhangi bir ölçüm ve düzenleme yapılmadan önce yazıldı:
K1 kapsam · K2 ölü varsayılan sayısı · K3 argümansız sesli durma · K3-poz pozitif kontrol ·
K4 **bayat sözlük davranışının ölçülmesi** · K5 çağıran tablosu · K6 pytest · K7 git disiplini.
İki çürütme maddesi de önceden ilan edildi (bkz. §5). Ölçüm sonrası bulunan kusur tasarıma
**yansıtılmadı**, rapora yazıldı.

## §1 · Ölçülen zemin (şartname yazılmadan önce)

- `chat_prompt.py:309` `model_path = 'data/kristal_model.pt'` (dosya **YOK**) · `:285` `vocab_path = 'data/vocab.json'`
  (**VAR ama BAYAT: 31.357 giriş**) ve `--vocab` bayrağı **hiç yoktu**
- `src/gateway/agent_gateway.py:80-81` · `src/rag/rag_pipeline.py:225-226` · `src/gateway/retrain_pipeline.py:95,97`
  aynı iki ölü varsayılan
- `data/train_future_finetune.bin` **VAR** (eski koşuların kalıntısı) · `data/kristal_carpenter_model.pt` **YOK**
- Kapsam dışı ama **ölçülen**: `src/llm/prompt_contract.py:35` `vocab_path="data/vocab.json"` (tüketici yüzeyi geniş)

## §2 · Ne yapıldı

1. **Varsayılan silindi, `None` yapıldı, kullanım anında fail-closed duruluyor** (ilan edilen tasarım):
   zaten ölü olan çağıranlara dokunmak gerekmedi; yalnız **eskiden sessizce yanlış olan** yol sesli durur.
2. **Sözlük ↔ checkpoint tutarlılık kapısı** dört yükleme sitesine eklendi (`resize_state_dict`'ten **ÖNCE**).
   Gerekçe K4: uyuşmazlık koşumu **durdurmuyor**, satırları kırpıyor ve son satır **"tam eşleşme"** diyerek güven veriyor.
3. `RetrainPipeline`: `_zorunlu()` saf yardımcısı; `run_training` kapıları **derlemeden ÖNCE** ⇒ eksik argümanda **hiç yazım yok**.
4. `chat_prompt.py`: **`--vocab` bayrağı eklendi** (madde 4); iki argüman da zorunlu, `sys.exit(2)`.
5. **Beyan edilen ek kapsam (şartnamede "dört site" yazıyordu, ikisi daha bulundu ve ölçüldü):**
   - `/model` menüsü silinmiş checkpoint'leri **seçenek olarak sunuyordu** (canlı ölü atıf) ⇒ emekliye ayrıldı
   - `"kristal_carpenter" in model_path` dalı Kristal zinciri silinince **hiç eşleşemez** hâle gelmişti
     ⇒ marangoz talimatı sessizce uygulanmıyordu; ölü önek (`kristal_`) kaldırıldı
6. `tests/test_agent_gateway.py`: mevcut test sözlüğü açıkça alıyor; canary'ye `vocab_path` verildi;
   **iki yeni fail-closed canary** eklendi.

## §3 · Ölçülen sonuç (kapı kapı)

| Kapı | Sonuç | Ölçüm |
|---|---|---|
| K1 | ✓ | Kapsam dışı `prompt_contract.py:35` **ölçüldü ve beyan edildi**, sessizce atlanmadı |
| K2(a) | ✓ | `kristal_model.pt` ölü atfı 4 sitede **ÖLÜ = 0** (ham 1 = etiketli emekli notu) |
| K2(b) | ✗ **İLAN HATALI** | "küçük-harf `kristal` = 0 satır" **harfiyen imkânsız**: 26 satırın 25'i meşru tanımlayıcı |
| K3 | ✓ | 6 site: `rc` = 2/1/1/1/1/1, hepsinde kapı mesajı **var**; `data/*.pt`+`data/*.bin` 25 dosya **DEĞİŞMEDİ** |
| K3-poz | ✓ | 4 site: kapı **0 kez** ateşledi; pozitif kontrol `compile_backlog_to_bin` **başarılı**, `.bin` yazıldı |
| K4 | ✓ | **SESSİZ DEĞİL, ama YUTUCU:** 3 `[SOZLESME_UYARI]` + **1757 satır kırpma** + son satır **"0 eksik, 0 fazla"** |
| K5 | ✓ | Yedi çağıran ölçüldü (bkz. rapor tablosu); **ÇALIŞAN hiçbir çağıran kırılmadı** |
| K6 | ✓ | `venv/bin/pytest` → beklenen 215 passed, 1 failed (sandbox soket yasağı) |
| K7 | ✓ | `git add -A` kullanılmadı; `changed_files` iki yönlü, `len==len(set)` |

## §4 · Kendi kusurlarım (ölçümle bulundu, rapora aynen girer)

1. **Sonda tespit dizesi harf hatası:** kapı mesajını `'varsayilan KADIRILDI'` (küçük v) ile aradım;
   mesaj **"Varsayilan KALDIRILDI"** basıyor ⇒ boolean tablo **her satırda False** çıktı, mesajlar
   ekranda dururken. Düzeltilmiş tespitle ölçüm tekrarlandı. *[[etiket-adlari-buyuk-harf-tuzagi]] ailesinin
   bu oturumdaki tekrarı — sayı doğruydu, **iddia** yanlıştı.*
2. **Kapı sırası kusuru (kendi testimi düşürdü):** `compile_backlog_to_bin`'de `vocab_path` kapısını
   `future_train_path` varlık kontrolünün **altına** koymuştum ⇒ fikstürde `FileNotFoundError` çıktı ve
   **yeni canary testim düştü** (`214 passed, 2 failed`). Doğrusu: **yapılandırma hatası veri hatasından
   ÖNCE** raporlanır. Kapı başa alındı (tasarım değişmedi, sıra düzeltildi).
3. **İlan ettiğim K2(b) ölçülemez bir kapıydı** — kusur kodda değil **ilanda** (T-0067 kuralı).
4. **Araç kuralı kusuru (ölçümle):** T-0086 aracının emekli ayıklaması `s.startswith("#")` kullanıyor;
   **girintili yorum bloğunu görmüyor** ⇒ fonksiyon içindeki etiketli not sayaçtan **çıkarılmıyor**.
   Ölçüldü: `emekli=0` (araç kuralı) ↔ `emekli=1` (doğru kural `s.strip().startswith("#")`).

## §5 · Fail-closed sınaması ve çürütmeler

- **Çürütme 1** *(ilan edilmiş): "ÇALIŞAN bir repo-içi çağıranı kırarsa düzeltme YANLIŞTIR."*
  **Gerçekleşmedi.** Yedi çağıranın hepsi ölçüldü: yaslananlar ya **zaten ölüydü** (silinmiş checkpoint
  varsayılanı, T-0085'ten beri `--save-path` kapısı) ya da **argüman geçiyor**. Tek davranış değişikliği:
  supervisor yolunda durma **daha erken ve daha açık**, ve `data/train_future_finetune.bin` artık **yazılmıyor**.
- **Çürütme 2** *(ilan edilmiş): "kazanç yalnız mesaj kalitesiyse 'kusur düzeltildi' demek YANLIŞ olur."*
  **Model yolu için DOĞRU, sözlük yolu için DEĞİL.** Model varsayılanı ölü bir adı taşıyordu ⇒ kazanç
  **erkenlik + mesaj** (K4'ün ölçtüğü şey model yolunda değil). Sözlük varsayılanı ise **sessiz kırpma**
  üretiyordu (K4: 1757 satır, durmadan) ⇒ bu gerçek bir **davranış** kusurudur ve kapı onu kapatır.

## §6 · Kapanış

Üç sinyalin üçü de aranır: (1) `bus_send` bildirimi, (2) kiralar boş, (3) `changed_files` **iki yönlü**
ve `len(cf)==len(set(cf))`. Tam digest tablosu `data/eval/anka_r10_okuma_varsayilanlari_2026-09-20.{md,json}`
içindedir. **`scripts/**` ölçüm/tarihsel betikleri ve `data/eval/**` tarihsel kayıtları DEĞİŞTİRİLMEDİ.**
Donmuş 10 desene **YAZILMADI**. `git add -A` **kullanılmadı**.

## §7 · Kapsam dışı bırakılanlar (beyan)

- `src/llm/prompt_contract.py:35` bayat sözlük varsayılanı — tüketici yüzeyi geniş, **ayrı görev**.
- `scripts/run_agent_arena.py:49` `--model` varsayılanı `data/kristal_model.pt` (ölçüldü) — `scripts/**` kapsam dışı.
- `scripts/run_goal_pipeline.py:177,309,387,402` açık ölü yollar (ölçüldü) — `scripts/**` kapsam dışı.
- `USER_GUIDE.md:6` TARİHSEL banner · aracın etiket sözlüğü · `6bca90b` push — ayrı maddeler.
