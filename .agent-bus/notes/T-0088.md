# T-0088 · KÜÇÜK BORÇLAR (4 KALEM) — YÜRÜTÜM NOTU

**Tarih:** 20 Eyl 2026 · **Yürütücü:** claude · **Durum:** `done` · **Commit:** YOK (operatör yetkisinde) ·
**Eğitim koşusu:** YOK (hiçbir `.pt`/`.bin` üretilmedi)

Kapanış raporu: `data/eval/anka_r11_kucuk_borclar_2026-09-20.md` (+ `.json` eşi).
Kapsam kaynağı: `data/eval/anka_r10_okuma_varsayilanlari_2026-09-20.md` §7 + §10 (kendi cümlem değil).

## Ne kapandı

| Kalem | Dosya | Öz |
|---|---|---|
| a | `src/llm/prompt_contract.py` | bayat `data/vocab.json` (31.357) varsayılanı kaldırıldı; kapı **çağrı anında**, iki sitede |
| b | `scripts/run_agent_arena.py` | `--model` ölü varsayılanı kaldırıldı **+ `--vocab` eklendi** (çağrı yeri T-0087'den beri ölüydü) |
| b2 | `scripts/run_goal_pipeline.py` | 19 ölü atıf → 0; dört yol parametresi **varsayılansız**; 3 ek ölçülmüş kusur düzeltildi |
| c | `USER_GUIDE.md` | :105 dokunulmadı (iddia **doğru**), :106 **çürüdü** ⇒ değiştirildi; §7 komutları onarıldı |
| d | `scratch/anka_r9_olu_atif_denetimi.py` | **girintili blok iddiası ÇÜRÜDÜ**; etiket sözlüğü **ölçüldü** (genişletilmedi); bayat kontrol teşhisi düzeltildi |

**Kalem dışı zorunlu:** `tests/test_compiler_entrypoints.py` — (ii) assert'i **silinmiş artefaktın adına** bağlıydı;
(b2) onu kırdı. Ölçüt ad-sabitlemeyi bıraktı (guard hedefi == kopya hedefi), kanaryayla **ayırt edici** olduğu
kanıtlandı, dosya **birebir** geri yüklendi. Dosya `writes[]`'te yoktu ⇒ **düzenlemeden önce** açık kiralama alındı.

## Ölçülmüş sayılar

* Denetim aracı ağacı: ölü atıf **2438 → 2419** (KANARYA hariç); KOD `156/2/154 → 138/3/135`
* `run_goal_pipeline.py`: `ham=19 emekli=0 olu=19` → `ham=1 emekli=1` **olu=0**
* `run_agent_arena.py` / `prompt_contract.py`: **olu=0**
* `pytest`: **1 failed, 215 passed → 1 failed, 216 passed** (sapma = +1 T-0088 kanaryası; düşen tek test sandbox
  soket yasağı `socketserver.py:478 PermissionError`, tabanda da düşüyordu)
* Donmuş 10 desene **0 yazma**; `data/vocab.json` digest beyanla **birebir**; `data/*.pt|*.bin` = 25

## Kendi kusurlarım (özet — tamamı rapor §9'da)

1. **Kanıtı ezdim:** aracı `CIKTI_JSON` yamalanmadan koştum; `data/eval/anka_r9_olu_atif_denetimi_2026-09-20.json`
   yeniden yazıldı, dosya **izlenmiyor** ⇒ eski baytlar kurtarılamaz. Eş `.md` tüm eski ölçümleri saklıyor.
2. Tasarım sayısını **elle** yazdım (3. tekrar): kanarya deltası `+2/+2` ilan, ölçüm `+1/+1`.
3. Bayat negatif kontrolü **kod kusuru sandım** ("AŞIRI AYIKLAMA").
4. Seçenek metnim kusurluydu ("ölü varsayılanlar" ↔ ölçüm: 0 varsayılan / 18 literal).
5. Sondam `.md` içinde `#`'i yorum saydı (46 aday şişti).
6. 4. aşama ilk düzeltmem **semantiği kaydırıyordu** (`--active-model sft_model`) ⇒ `train_dpo.py` okunarak düzeltildi.
7. `scratch/anka_r9_kanarya.md` beyan edildi ama **net değişmedi** (digest birebir) ⇒ ölçüm esas.

## Açık maddeler (kapanmadı)

1. `README.md` **bayat, dokunulmadı** (`writes[]` dışı): 194/197/200 arena komutları, 237 seçenek tablosu, 221 sözlük iddiası
2. `train_dpo.py` T-0087 sınıfının **canlı 6. ve 7. sitesi**: `:51` bayat `data/vocab.json` sabiti (`--vocab` kabul etmez),
   `:88-89` iki ölü varsayılan + **sessiz `return`** (rc=0)
3. `tests/test_prompt_contract.py` `env` fixture'ı hâlâ `data/vocab.json` yüklüyor
4. `chat_prompt.py:314-316` asimetrisi (verilmemiş ⇒ `exit(2)`, bulunamayan ⇒ `return`)
5. `run_goal_pipeline.py` 1. aşamasında `--pretrain` **yok** (D1 adayı — ölçülmedi)
6. `run_goal_pipeline.py` **uçtan uca koşulmadı** (qdrant + öğretmen + ağır checkpoint)
7. `scratch/**` gitignore ⇒ araç ve kanarya fikstürleri **sürümlenmiyor**; taze klonda araç **VAKUM** diye durur
8. Etiket sözlüğünü genişletme kararı **bilinçli olarak alınmadı** (kapılı)
