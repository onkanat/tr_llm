# T-0091 · D1 hükmü — yürütüm notu

**Damga (betik hesapladı):** `2026-09-20T11:28:00Z` · **Yürütücü:** claude · **Hüküm:** D1 kuralı
**UYGULANMAZ**; ilan edilen iki çürütme maddesinin **hiçbiri ateşlenmedi (0/2)**; K3 · K4 **KALDI**.
**Rapor:** `data/eval/anka_r14_d1_hukmu_2026-09-20.md`
`87733f4c628c47665493ad232593b04d2367e36865fc968371a35c63e9ca322a` · **`.json`** `data/eval/anka_r14_d1_hukmu_2026-09-20.json`
`283df4de7d86ee8dc446f2ef8d94e23c4c48cf8f4e29c776719b17dc741efdf6`

> **Donmuş yüzeye YAZMA YOK.** Bu görev `src/compiler/**` ve `data/lexicon/**` yüzeyini
> yalnız **okudu**. Hüküm "ölçtüm ve yazmadım"dır, "yazamadım" değil.

---

## 1. Görev neydi

Operatör: *"Onay bekleyen işler için devam et"* ⇒ dört iş akışından **(2) D1 donmuş derleyici**.
Kaynak **tam metinden** okundu (özetten değil): plan §Faz 2/1 ve kapanış §8/1 (*"en-uzun-kök
kuralı; ölçülmüş etki TAM %81,24 → %84,90; bir sonraki turun ilk maddesi"*) ile **çürütme kaydı**
`data/eval/anka_r2_on_olcum_2026-09-19.md` §12 (*"D1 bir skorlama problemi DEĞİLDİR"*).

Yani görev, **planın iddiasını** (uygula) ile **r2'nin çürütmesini** (uygulama) karşı karşıya
koyup **ölçmekle** karar vermekti.

## 2. Çekirdek ölçümler (üç bağımsız ölçüt — üçü de aynı yönü gösteriyor)

**(a) Artefakt — plan "✓" diyor, kod aynı.** `src/compiler/core.py:174-185` `_score_paths` hâlâ
`path['score'] = 10.0 - len(path['morphemes'])`; docstring hâlâ *"For now, a basic heuristic"*;
`git log -1 -- src/compiler/` → **`6bca90b` (T-0081)**; `git status` bu dizinde **temiz** ⇒
planın *"Faz 2 derleyici ✓ D1 (en-uzun-kök)"* satırının **kodda karşılığı yok**.

**(b) Davranış — 4/4 vaka HÂLÂ bozuk, her iki sözlükte:**

```
ağacı  -> ['ağa','DERIV_CI']    aklı  -> ['ak','DERIV_lI']
kanadı -> ['kana','TENSE_PAST'] gönlü -> ['gön','DERIV_lI']
```

Karşıtlık önemli: **`roots_anka_r1.tsv` gerçekten bir şeyleri düzeltmiş** (`Fransa`, `ağzı`,
`hakkı`) — yani T-0080'in **veri** tarafı çalıştı, **derleyici** tarafı çalışmadı. İki okumayı
ayırmasaydım rapor "hiçbir şey değişmemiş" diye yanlış okunurdu.

**(c) İnsan-etiketli ROOT oracle (25 öğe):** MEVCUT **24/25** · EN-UZUN-KÖK **23/25** ⇒ **NET −1**.
Kural 25 vakanın **yalnız 1'inde** kararı değiştiriyor ve o değişim **regresyon**
(`gölgeler`: beklenen `gölge`, kural `gölgele`). **13/25** vakada skor **berabere** — zemin var,
kural oraya **neredeyse hiç dokunmuyor**. Bu, `anka_r2_on_olcum` H8'in **net −1**'ini **birebir
yeniden üretti**: iki bağımsız ölçüm aynı işareti veriyor.

**(d) Kimlik oracle (yeni araç, 42.459 lemma): kural 0 karar değiştiriyor.** Sözlükte kendisinden
kısa bir öneki de bulunan her lemma `L`, derleyiciye `L` olarak verilir; seçilen kök `L` olmalıdır
(insan etiketi gerekmez). Sonuç: MEVCUT **%99,48** · KURAL **%99,48** · kurtarılan **0** ·
bozulan **0**.

**Bu bir D1 kanıtı DEĞİL — aracın ÖLÜ olduğunun kanıtıdır.** Sebep yapısal: yalın lemma girdisinde
tam eşleşme zaten **1 morfem**lidir ve mevcut skor onu **zaten** seçer; D1 kusuru yalnız **çekimli**
yüzeyde doğar (iki aday da **2 morfem** ⇒ berabere). **Araç D1'in beraberlik zeminine hiç basmıyor.**

Ama araç **boşuna değil**: artık hataların (**222** / **231**) **tamamı büyük-harf homografı**
(`abdal`←`Abdal`, `acem`←`Acem`, …) ve **doğru kök `analyses` içinde %100 VAR** (`yokluk = 0`)
⇒ kayıp bir *kapsama* değil bir **seçim** kaybıdır — fakat rakip **eşit uzunlukta** olduğu için
**uzunluk kuralı onu da sıralayamaz**. `tatlısı`→`Tat` **aynı sınıftır**.

## 3. Hüküm

**Kural UYGULANMAZ; donmuş derleyici yazılmadı.** Üç ölçüt de aynı yönü gösteriyor: kodda zaten
yok (a), insan-etiketli oracle'da **net −1** (c), kimlik oracle'da **0 karar değişimi** (d).

**Doğru gereksinim ölçümle işaret edildi:** D1 bir *skorlama* değil, bir **seçim/oracle**
problemidir — beraberlikte **eşit uzunluklu** adaylar arasında karar vermek gerekir; bu, kural
tabanlı bir uzunluk ölçütüyle **yapılamaz**. Gereken şey r2 §12/5'in dediği **`plausibility
oracle`**'dır ve o **etiketli veri** ister ⇒ bu turun **kapsamı dışında**.

## 4. Kendi kusurlarım (ölçüm sırası ve kabı)

1. **Kimlik oracle'ı "D1 aracı" sanarak kurdum.** 42.459 vakayı ölçüp **0 karar değişimi**
   görünce önce *"kural etkisiz"* diye okumaya meylettim; doğru okuma *"aracın gücü yok"*tur.
   **Pozitif kontrolü (`değişen` sayacı) ölçüme SONRADAN ekledim**; doğrusu **önce** ilan
   etmekti: *"araç ancak en az bir vakada kararı değiştiriyorsa anlamlıdır."* Sayacı ekleyip
   **0** bulduğum için hüküm yanlış olmadı — ama **sıra yanlıştı**.
2. **İlk sondam sözlük girişini `0` okudu** (yanlış alan adı). Sözlük **gerçekten yüklüydü**
   (kanıt: `İstanbul`/`Fransa` yalnız yüklüyken çıkar). Bu satırı **bulgu olarak raporlamadım** —
   kusur ölçüm **kabımındı**, görevin değil. Ders: *"erişemedim" ≠ "yok"*.
3. **Planın "✓" satırına önce güvendim.** İçgüdüm "D1 zaten yapıldı" idi; `_score_paths`'i
   **okumadan** yazsaydım donmuş yüzeye gereksiz bir yazma ya da gereksiz bir "kapandı" beyanı
   üretirdim. Kodu **ölçtüm**, iddiaya güvenmedim.
4. **Kendi kanıt komutum SAHTE POZİTİF üretti.** `.pt`/`.bin` denetimini
   `find data -name "*.pt" -o -name "*.bin" -newermt "-60 minutes"` ile koştum; `-print`
   **yokken** ve **VEYA önceliği** yüzünden `-name "*.pt"` dalı **zaman koşulundan bağımsız**
   yazdırıldı ⇒ çıktı "7 dosya değişti" gibi göründü. **Python ile tek tek** ölçtüm: son 60
   dakikada değişen `.pt`/`.bin` **0**; en yeni artefakt `2026-09-19T23:55:08Z` (bu görevden
   **11,6 saat önce**). Ne donmuş yüzeyde yazma vardı, ne bir bulgu ⇒ **ölçüm kabımın** kusuru;
   `git status`/`git log` ile **çapraz doğrulandı**.

## 5. Bu görevde ölçülen YENİ bulgular (kapsam dışı — düzeltilmedi)

1. **Plan ↔ artefakt uyuşmazlığı (yeni kalem):** planın *"Faz 2 ✓ D1"* iddiası **kodsuz**dur;
   `src/compiler/**` T-0080'de **hiç değişmedi**. T-0090'da ölçülen "plan kendi kusurlarını yazdı"
   tablosuna eklenir; planı düzeltmek **ayrı bir iştir** (bu görev plan dosyasına yazmaz).
2. **Büyük-harf homografı sınıfı** (222 / 231 + `tatlısı`): **çözülmemiş** ve **hiçbir mevcut
   kural onu ele almıyor**; harf-koşullu bir ayrım gerektirir. D1'in "uzunluk" ekseni büyüklüğe
   **kördür** — bu sınıfı da kapsamaz.
3. **Doğru kök kaybı KAPSAMA değil SEÇİM kaybıdır** (`yokluk = 0`): kök `analyses` içinde
   **her zaman** var. Yani gereken şey arama/veri değil, **karar** mekanizmasıdır.

## 6. Kapanış sinyalleri (üç sinyal)

**(a) Bildirim/kanıt:** bu not + rapor + `.json`; ham çıktılar `$TMPDIR/t0091/` — **tam digest**
(önek değil, kural gereği): `probe.txt`
`b5745e666d6e369d61da67ee6f99212651beba0385f9be537111c19e87b3d536` · `d1.txt`
`eaa77e5af061e08ac66a248db410577f83cf577653e6c7c9e18bb9a0ff290c39` · `d1b.txt`
`4de0650ae267627f8679941169526bc7a0ebdf7cdd400b3bc2c2881b73a2ce67`. Sonda **kaynakları**
`.json`'da **tam metin** saklandı ⇒ `$TMPDIR` silinse de ölçüm **yeniden üretilebilir**.

**(b) Kiralama:** T-0091 iki kiralama aldı — `data/eval/` ve `.agent-bus/notes/` (**donmuş dosya
kiralanamaz**; beyan edilen üç yolun üçü de bu iki kökün altında). Kapanışta **boşaltıldı**.

**(c) `changed_files` — İKİ YÖNLÜ:** beyan edilen **3** yolun üçü de değişti (YÖN 1 TAM);
kira köklerinde beyan edilmemiş **iş** artefaktı **0** (YÖN 2 TAM).
`len(cf) == len(set(cf))` → **True**. Ayrıntılı tablo `bus_report_result` kanıtındadır.

**(d) Diğer kanıtlar:**

* **Donmuş yüzey:** `src/compiler/**` · `data/lexicon/**` · `data/rebuild/**` · hiçbir `.pt`/`.bin`
  — **hiçbirine yazılmadı**; `git log`/`git status` ile doğrulandı (tam digest tablosu raporda).
* **Ölçümün geçerlilik kapısı:** `roots.tsv` digest'i `tests/test_morphology_regression.py`'nin
  beyan ettiği `LEXICON_SHA256` ile **birebir aynı** ⇒ ölçüm, testin yer-gerçeğiyle **aynı kapta**.
* `venv/bin/pytest -q` → **1 failed, 227 passed** (19,07 sn) — T-0090 tabanıyla **birebir aynı**;
  düşen tek test yine `test_agent_gateway.py` **sandbox soket yasağı**
  (`socketserver.py:478 PermissionError`), ilan edilmiş davranış. Bu görev **kod değiştirmediği**
  için takımın **değişmemesi beklenen** sonuçtur; ölçüldü ve öyle çıktı.
* **Eğitim koşusu YOK · push YOK · `git add -A` YOK.** Commit yalnız **beyan edilen** yollar için
  ve bu görevin kendi niteliğindedir.

**İddia sınırı:** bu görev **kod yazmadı**, **model eğitmedi** ve **donmuş yüzeye dokunmadı**.
Ölçülen şey **D1 kuralının etkisidir**; ölçülen şey modelin **dil kalitesi DEĞİLDİR**. Ve bu
raporda kullanılan **kimlik oracle aracının kendisi bu iş için ölüdür** — bu, kanıt olarak değil
**kusur olarak** beyan edilmiştir.
