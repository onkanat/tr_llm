# T-0097 — Anka ceket külliyatını büyüt (marangoz kataloğunu genişlet)

**Durum:** **KAPANIŞTA** · Faz 1 TAMAM · **Faz 2 TAMAM (13/13 kapı)** · **Faz 3 TAMAM**
(`rc=0`, 6/6 segment, 12110,6 sn, `bitti_utc 08:07:21Z`, teslim **YAZILMADI**) · Faz 4 yazıldı
**Yürütücü anlatı günlüğü.** SPEC.md:134 gereği **koşum sürerken** yazılır, kapanışta değil.

---

## Faz 0 — İlan ve ön-kayıt (kapandı)

`data/eval/anka_r18_ceket_ilani_2026-09-21.md` — derleme ve koşum **başlamadan** yazıldı.
İki erratum ilan edildi (`§1.7` mevcut, `§1.8` bu oturumda): ikisi de **derlemeden ÖNCE**.

**Ön-kayıt çıpası mtime DEĞİL, İÇERİK:** iki erratum dosyanın mtime'ını ileri taşıdı ⇒
"ilan mtime < ilk derleme mtime" denetimi tek başına zayıf. `scratch/t0097_kos/once.json`
içine `on_kayit_cipasi` eklendi: `§4/§5/§6` bölüm digestleri + birleşik kural digesti
(`bfca40fd…`) + tam dosya digesti, **derleme başlamadan** ölçülüp yazıldı. Kapanışta
yeniden çıkarılıp karşılaştırılacak.

---

## Faz 1 — Katalog genişletme · `scratch/anka_r18_katalog.py`

**Kapılar üç turda geçildi. İki tur DÜŞTÜ ve ikisi de KENDİ ÖLÇÜM KABIMIN kusurunu açığa
çıkardı — veri kusuru değil.** Bu, "önce ölçütü doğrula" dersinin bu görevdeki karşılığıdır.

### v1 → DÜŞTÜ (veri kusuru + ölçüt kusuru birlikte)

```
--- V5 ---  cakisan ad: 2 ['mengene', 'su terazisi']
--- V5b --- 4-gram ortusmesi >=0,50 olan 1 olgu (su terazisi 0.6154)
```

İki ayrı sebep ölçüldü:
1. **Ölçüt kusuru:** v1'in `_norm`'u **Türkçe aksanları katlamıyordu** ⇒ `ege` ≠ `eğe`,
   `zimpara takozu` ≠ `zımpara takozu` **farklı ad** sayıldı ve V5 çakışmayı **göremedi**.
   Bu, T-0080/F2 sınıfının ta kendisi: *indeks yazan ile arayan normalizasyonu aynı olmalı.*
2. **Veri kusuru:** mevcut `TOOLS` tablosu kanonik **el aleti** kümesini zaten kapsıyor;
   v1'in 25 aletinin ~9'u kopya ya da yakın-kopyaydı.

### v2 → DÜŞTÜ (yalnız ÖLÇÜT kusuru)

`_norm` aksan katlaması düzeltildi, `TOOLS` bölümü tümüyle yeniden yazıldı, `V5c` eklendi.
V5c **34 çift** işaretledi — ve liste okunduğunda **hepsi sahteydi**:

* v2'nin V5c'si tüm dizgiyi **ALT DİZGİ** olarak karşılaştırıyordu ⇒
  `'isi' ⊂ 'su terazisi'`, `'zimpara' ⊂ 'bant zimpara'` gibi **sahte eşleşmeler**;
  `Isı tabancası ~ su terazisi` diye "çakışma" raporlandı.
* Yeni **varlık adlarını** mevcut `§6 WORKSHOP_SCENARIOS` girdileriyle karşılaştırıyordu —
  oysa §6'nın "adı" bir **SORUDUR** (`"Ahşap rendesi neden tek yönde çekilir?"`).
  Bir alet adını bir soruyla çakıştırmak **TÜR HATASI**dır.

### v3 → GEÇTİ

Üç düzeltme, üçü de **beyan edilmiş gerekçeyle**:

| # | düzeltme | gerekçe |
|---|---|---|
| 1 | V5c ölçütü **alt dizgi** → **sözcük kümesi kapsaması** | `'isi' ⊂ 'terazisi'` bir çakışma değil, dizgi artefaktıdır |
| 2 | V5c **yalnız §1–§5 varlık** tabloları; §6 (soru) **iki taraftan da** dışarıda | varlık adı ↔ soru karşılaştırması tür hatasıdır |
| 3 | `'ahsap'` `GENERIC_HEAD`'e eklendi | alanın **kendi adıdır**, ayırt edici içerik taşımaz; `ahşap presi ~ ahşap ağartma` sahte eşleşmesini üretiyordu |

**V5c artık "örtüşme YOK" değil, "HÜKME BAĞLANMAMIŞ örtüşme YOK" kapısıdır.** Ölçüt fazla
işaretler; her işaretli çift `V5C_HUKUM` sözlüğünde **ADIYLA** karara bağlanır. Fail-closed:
hükümsüz çift kalırsa `rc=2`. Bu, ölçütün aşırı-işaretlemesini **gizlemek yerine denetime
açan** tasarımdır — 8 çiftin 8'i gerekçesiyle `KALDI` hükmü aldı.

**Beyan edilen kör nokta (kapatılmadı, ADIYLA yazıldı):** hiçbir sözcük tabanlı kapı **eş
anlamlılığı** göremez. `gül ağacı = pelesenk`, `sapeli ~ maun`, `vişne ~ kiraz` gibi çiftler
v1'in V5/V5b kapılarından **geçmişti**; 27 çift **göz denetimiyle** çıkarıldı ve raporda
`goz_denetimi` tablosunda listelenir. Sözcük kapısı bunu kapatmaz; kapatan şey **beyandır**.
Kalan iki kör nokta (`karaçam`/`sarıçam` vs generic `çam`; `kirli boya` vs `eskitme patinası`)
**bilerek bırakıldı ve adıyla beyan edildi**, gizlenmedi.

### v3 ölçümleri

```
V4   §1 25/25 · §2 25/25 · §3 20/20 · §4 15/15 · §5 15/15 · §6 25/25 → TOPLAM 125/125 OK
V5   çakışan ad: 0
V5c  işaretli 8 · hükme bağlı 8 · hükümsüz 0     (100 varlık karşılaştırıldı; 25 §6 sorusu atlandı)
V5b  4-gram örtüşmesi ort 0,0004 · en yüksek akasya 0,0526   (kapı <0,50)
V4b  mevcut ort 131,2 · yeni ort 128,8 · ORAN 0,9812          (kapı 1,00 ± 0,20)
perspektif 450 (beklenen 450) · + 11 önek × 450 ⇒ 5.400 satır
```

**Üretilen:** `scratch/t0097_kos/katalog_yeni.jsonl` — 5.400 satır · 125 benzersiz olgu ·
olgu başına **tam 12 satır** (60/48/36 = 5/4/3 perspektif × 12) · 3.396 benzersiz istem.

**Digest'ler (TAM, önek değil):**

| artefakt | sha256 |
|---|---|
| `scratch/anka_r18_katalog.py` | `ae214d5d98e77be35b5d9768fde914007e6daaad0a91e68fd0f2c08c5bf5e8a3` |
| `scratch/t0097_kos/katalog_yeni.jsonl` | `ace4cac49069dc2da34cc8ec5f36d2d3061338137d25a41c3c99b185b36d36eb` |
| `scratch/t0097_kos/katalog.json` | `8abae2fba5cef79821c721eeb01ffc41d4cbc2b08e038ffaba53d5a5b00a7e40` |
| `scratch/t0097_kos/once.json` | `02067286ce918dd96c8de9d94574410f56d46b2a734955f61d9a1bad080608f7` |
| kaynak modül (DOGULANMADI) | `5aad56bf7c10d3e744aa8ffcd3ef2bd6d3e6497dc54b0e8698fc4a6f72f10348` |

**`kaynak` dağılımı: 125/125 `"elle yazıldı — uzman bilgisi"`.** Diskteki arşivden
türetilmedi (ilan §1.7: arşiv katalogdan **türetilmiş**, döngüsel). ⇒ **Doğruluk yükü
tamamen yürütücüdedir**; bu ilanda ve raporda **açık** olarak beyan edilmiştir.

---

## Faz 2 — Derleme · `scratch/anka_r18_ceket_build.py`

**13/13 kapı PASS.** Ama bu fazın asıl kaydı şudur: **ölçüm kendi ilanımın ÜÇ maddesini
çürüttü.** Üçü de T-0067 gereği *düzeltilmedi*; ham hükümleriyle makine kaydında duruyor.
Ayrıntı: `data/eval/anka_r18_build_2026-09-21.md`.

| # | ilanımın iddiası | ölçülen | ham hüküm |
|---|---|---|---|
| 1 | §4 *"her olgu **12 satır**"* | 60/48/36 (5/4/3 persp × 12) | `ilan_yuklemi_PASS: false` |
| 2 | §4/V7 `kept = satır×2 + 1.500 + 688 = 13.000` | **22.283** (sapma **+9.283**) | `ilan_formulu_PASS: false` |
| 3 | §3 `--steps = ceil(jeton/1024)×epoch` = **4.596** | bağlayıcı segment tablosu **24.000** | çelişki |

### V4 yeniden tanımı — beklenti ÜRETİCİDEN türetilir

Eski yüklem **karşılanamazdı**, çünkü ilanın kendi zinciri (125 olgu → 450 perspektif → ×12)
12'yi **perspektif** başına koyar. Beklenti artık:

1. üretici `anka_r18_katalog.py` yüklenir, **digest'i kapıdır** (`ae214d5d…`);
2. `PERSPEKTIF × YENI_TABLOLAR` ile olgu-başına satır **canlı tablolardan** hesaplanır;
3. ilan §1.1'in **derlemeden önce** kayda geçmiş aday sayılarıyla **çapraz denetlenir** (6/6);
4. artefakt **olgu ADI düzeyinde** karşılaştırılır (eksik/fazla/yanlış satır = 0/0/0).

Kendini doğrulayan assert **yok** (T-0096/K8). Kapı yine de boş tabloyu, eksik/fazla olguyu,
yanlış önek çoğaltmasını yakalar.

> **Kusur spec'e de geçmiş:** görev kaydının kabul kriteri aynı yanlış yüklemi taşıyor
> (*"…her olgu 12 satır üretir"*). İlan/spec ölçümden sonra onarılmaz ⇒ **beyan edildi,
> düzeltilmedi**.

### §3 çelişkisinin çözümü — segment tablosu lehine

Formül kolu (`4.596`) seçilseydi **hiçbir Ç-maddesi sınanamazdı**: §5'in bütün maddeleri
*"eşleşen adımlarda"* yazılmış ve §3 aynılığı *"kontrol kolunun tek geçerlilik koşulu"*
ilan etmiş. ⇒ Çelişki **tercihle değil, ölçümün ön koşuluyla** çözüldü: **24.000**.

### Determinizm ve held-out — iddia değil ÖLÇÜM

* `.bin` iki koşumda **bayt-özdeş**: `c9d964d9…` ⇒ hem determinizm, hem V4 düzeltmesinin
  **veriyi değiştirmediği** kanıtı.
* Eğitim carve `bf72e7ca…` ve held-out `c397eb08…` **T-0094 ile aynı** ⇒ tek değişken kanıtlı.
* Kaynak `carpenter_specialization_dataset.jsonl` digest'i baştan sona **aynı**
  (`146a73dc…`) ⇒ `generate_specialization_dataset.py` **KOŞULMADI** (koşsaydı ezerdi).

### Beyan edilen sınır (kapatılmadı)

`V6`nın **soru ekseni ayrık DEĞİL**: `input` 48 · `instruction` 83 çakışma. Cevap ekseni
**0/0** (kapı orada). ⇒ Ölçüt **yeni olguları sınamaz, 450 eski perspektifi sınar.**
Göz denetimiyle çıkarılan **27 çift** ve kalan **2 kör nokta** `katalog.json` içinde
adıyla kayıtlıdır.

---

## Faz 3 — TAM KOŞUM (sürüyor)

Sürücü `scratch/t0097_marangoz.py` · başlangıç ~`04:29Z` · 6 segment (1.000→24.000 küm.).
Karışım `scratch/t0097_kos/mix_r4.bin` — **ölçülen replay oranı %25,00** (ilan §2: %25).

**Sağlık imzaları (S1'de canlı doğrulandı):** `Metal GPU (MPS) seçildi.` ✅ ·
`Hedef: SFT (prompt maskeli)` ✅ ⇒ iki eksen de **canlı**, VAKUM değil.
`eğitim devam ettiriliyor (Resume)` ✅ · `AdamW momenti bulunamadi … optimizer SIFIRDAN` ✅
(beyan edilen S1-momentsız durumu; ilk güncelleme 1,7306×).

### ⚠️ Bu oturumda bulunan YENİ kusur — **log kabı NUL ile deliniyor** (bana ait)

`scratch/t0097_kos/seg_1.log` içinde **6.249 baytlık NUL bloğu** (offset 1753–8002) var ve
ara adım satırları **kayıp**: dosyada 1.000 adımın yalnız **4'ü** duruyor.

**Pozitif kontrol — bu YENİ, T-0096'da YOK:**

| log | bayt | NUL | adım satırı |
|---|---|---|---|
| `scratch/t0096_kos/seg_1.log` | 10.990 | **0** | **101** |
| `scratch/t0097_kos/seg_1.log` | 8.261 | **6.249** | **4** |

**İlk atfım YANLIŞTI — düzeltildi (aşağıda §ÇÖZÜM):** bu bloğu önce *"sürücünün
`stdout=lf` dosya kabının kusuru"* diye yazmıştım. Ölçüm bunu **çürüttü**: NUL deliğinin
sebebi kap değil, **aynı dosyaya yazan İKİ ayrı sürücü+eğitici çiftidir**. Düzeltilmiş
hâli §ÇÖZÜM'de; yanlış atıf burada **silinmedi**, çünkü hangi ölçümün onu çürüttüğü
kayıtta kalmalı.

**Etkisi — sınırlı, ama sınırı ölçüldü:**

| etkilenen | durum |
|---|---|
| `sn_adim` (kesme kriteri) | **ETKİLENMEZ** — `sure/adim`, duvar saati (`:186`) |
| `mps_satiri` · `sft_satiri` · `ilk_kayip` | **ETKİLENMEZ** — blok deliğin **öncesinde** |
| `son60` (kayıp referansı) | **RİSK** — aynı dosyadan okunur (`:182`); delik `Kayıp Dağılımı` bloğundan **önce** ve sınırlı |
| adım-başı aykırılık denetimi | **YAPILAMAZ** — ara satırlar yok |

**Fail-closed davranış:** blok ayrıştırılamazsa sürücü `seg["hata"]` verip **`rc=2` ile durur**
(`:476-478`) — sessizce geçmez. ⇒ Risk **görünür**, gizli değil.

**Düzeltme YAPILMADI:** koşum sürerken sürücüyü değiştirmek **ölçülen kabı** değiştirir
(T-0089: *kopyalanan betik kabını değiştirir* / T-0092: *kabul koşusu ölçtüğü artefaktı
değiştirir*). Düzeltme **T-0098'e** beyan edilir.

### Aykırı adım süresi — ölçüldü, kesme kuralı **tetiklenmedi**

`Adım 1/1000` **27,98 sn** (ilk adım: model yükleme + MPS çekirdek derlemesi — beklenen) ·
`Adım 760/1000` **18,78 sn** (tek kare sıçrama) · komşuları **0,39-0,40 sn**.

Kesme kuralı **segment ORTALAMASINA** bakar (`sn_adim = sure/adim`, `:186`, `:538`) ⇒ tek
sıçrama kesmez. S1 ortalaması **≈0,42 sn/adım** — eşiğin (`0,70`) **altında**. Sıçramanın
kaynağı (başka GPU tüketicisi mi — T-0052) **ölçülmedi**; tek kare olduğu için **iddia
edilmiyor**.

---

### 🔴 CANLI GÖZLEM — S1 durdu (adım ~760/1000), ~6 dk ilerleme yok → **ÇÖZÜM: §ÇÖZÜM**

Ölçümler (hepsi bu oturumda, `04:35-04:37Z`):

| ölçüm | değer |
|---|---|
| `seg_1.log` boyut / mtime | **8.261 bayt** · **07:30:28** — 6+ dk **değişmedi** |
| 30 sn boyunca 7 örnek | **7/7 aynı** (boyut ve mtime sabit) |
| içerik dönüyor mu | **HAYIR** — kuyruk hâlâ `Adım 740/750/760`, ilk gördüğümle **bayt-özdeş** |
| `seg_1.pt` · `eval_seg_1.*` | **YOK** — S1 bitmedi |
| harness durumu | **`running`** (`TaskOutput`) — sürücü `subprocess.run`'da bloke |

**Yorum sınırı (T-0089/T-0095):** duran bir süreç **iz bırakmaz**; `pgrep` bu sandbox'ta
*"Cannot get process list"* veriyor ⇒ **süreç listesi bir ölçüm DEĞİLDİR** ve buradan
"şu süreç yok" sonucu **çıkarılmadı** (anlık ölçümden süreç durumu çıkarma).

**Bulunan İKİNCİ kusur (bana ait): `subprocess.run`'da TIMEOUT YOK.** `:179`'da `timeout=`
verilmediği için segment **sonsuza kadar** bekler; kesme kuralı (`:538`) ancak segment
**döndükten sonra** çalışır ⇒ **asılı kalmayı yakalayamaz**. Düzeltme T-0098'e.

**Neden — ÖLÇÜLDÜ (§ÇÖZÜM):** makinede **iki eğitici** vardı; ikisi de aynı MPS GPU'yu ve
aynı `seg_1.log`'u paylaşıyordu. Adım 760'ın **18,78 sn** sürmesi (komşuları 0,39-0,40)
bir **48×** yavaşlamadır; T-0052'nin (başka GPU tüketicisi ⇒ 0,43 → 3,45 sn/adım) çok
ötesindedir ve sebebi **Chrome/YouTube DEĞİL, benim kendi ikinci koşumumdu**.

---

### ✅ ÇÖZÜM — mekanizma ÖLÇÜLDÜ: **iki sürücü + iki eğitici aynı GPU'da**

`pgrep`/`ps` sandbox'ta **engelli**, ama **`lsof` çalışıyor** ve dosyayı **kimin tuttuğunu**
doğrudan verdi. `seg_1.log` üzerinde **DÖRT** PID:

| PID | fd | kim | kendi çıktısı |
|---|---|---|---|
| **77710** | `3w` | sürücü A (**yetim**) | `$TMPDIR/t0097_run.log` |
| **77713** | `1w`,`2w` | **eğitici A** (fd `4r mix_r4.bin` + Metal önbellekleri) | seg_1.log |
| **77850** | `3w` | sürücü B (**izlenen**, `b1skvghca`) | harness `b1skvghca.output` |
| **77853** | `1w`,`2w` | **eğitici B** (fd `4r mix_r4.bin` + Metal önbellekleri) | seg_1.log |

⇒ **Kök neden: benim daha önceki koşumum ÖLMEMİŞTİ.** "Adım 400'de öldü, harness süreç
grubunu biçti" hükmüm **YANLIŞTI** (`nohup` işini yaptı). Onu ölü sanıp **üstüne ikinci bir
koşum başlattım** ve iki eğitici birbirini MPS'te kilitledi.

**Çakışmanın kanıtı — müdahale ile büyüme:** izlenen çifti (`77850`/`77853`) `TaskStop` ile
durdurduğum **an** yetimin dosyası büyüdü: **8.261 → 9.723 → 9.809 bayt**. Tek başına kalan
eğitici hemen ilerledi ⇒ yavaşlamanın kaynağı **rekabet**.

**NUL deliğinin gerçek sebebi:** iki sürücü de `open(log,"w")` ile **truncate** ediyor, iki
eğitici de **bağımsız offset**lerden yazıyor ⇒ biri keserken öteki ileri offset'e yazınca
arada **boş bölge** kalıyor. Kap kusuru değil, **çift yazıcı** artefaktı.

**Yetimin koşumu kendi kendini dürüstçe kapattı** (tasarlanan fail-closed zinciri çalıştı):

```
kesme  : sn_adim 0.9317 > 0.70 ⇒ S1 sonrası kesildi · ilan_tahmini_tuttu: false
secim  : KAPIYI GECEN YOK — tek neden: "A artis %+21.2831 > esik +%10.00"
teslim : yazildi=false · hedef=data/anka_a1r_ceket_r18.pt (kusur 3 düzeltmesi ÇALIŞTI)
atif   : ureten_ic_gorev_sabiti="T-0094" · bayat=true (kusur 5 düzeltmesi ÇALIŞTI)
```

`ls` ile doğrulandı: **teslim dosyası YOK** (`data/anka_a1r_ceket_r18.pt` yazılmadı).
`sn_adim 0.9317`, duvar saatinin **rekabeti içerdiğinin** kanıtıdır ⇒ bu S1 ölçümü
**geçersiz** (matematik temiz, süre kirlendi). Hiçbir hüküm bu koşumdan **alınmadı**.

**Saklanan kanıt (TAM digest):**

| dosya | sha256 |
|---|---|
| `scratch/t0097_kos/kanit_cift_yazar_seg_1.log` | `c851fb4b691dd687d6ad4caa5578b448428ce2ffe672e76c2ce4b1ca67cbe7d6` |
| `scratch/t0097_kos/sonuc_kesme_cift_yazar.json` | `9f1258bef4641b3087854b9b5cf78c1846d66f9ac864e3592ab6aee4f625d855` |
| `scratch/t0097_kos/kanit_yetim_surucu.log` | `bd89d638b5046e945a0213a46de8e1c6ad1bcadd1a8e9402078ac7bc59ccf1eb` |

**Kalan GERÇEK kusur (bana ait, düzeltilmedi): `subprocess.run`'da TIMEOUT YOK.** `:179`'da
`timeout=` verilmediği için sürücü **sonsuza kadar** bekleyebilir; kesme kuralı (`:538`)
segment **döndükten sonra** çalıştığı için **asılı kalmayı yakalayamaz**. T-0098'e.
**Bilinçli olarak düzeltilmedi:** kusur kanıtlanmış kök neden **değil**, ikincil koruma;
ilan edilmiş aracı ölçüm ortasında değiştirmemek için araç **aynen** bırakıldı.

### Yeniden başlatma — **TEK** çift, doğrulanmış

Durum **boşaltıldı** (`seg_1.pt` · `.opt.pt` · `seg_1.log` · `eval_seg_1.*` · `segments.jsonl`
· `sonuc.json` silindi; kanıt kopyaları yukarıda). Yeni koşum: **`bzkvnsut6`**.

**Lansmandan 25 sn sonra `lsof` ile doğrulandı:**

```
mix_r4.bin tutuculari: 78745  (TEK eğitici)      ← çakışma YOK
seg_1.log tutuculari : 78743 (sürücü, fd 3w) + 78745 (eğitici, fd 1w/2w)  = TEK çift
```

Gözcüler: segment sonu (`b5cqdx23d`) + **GPU çakışma gözcüsü** (`bvjo0vip4`, 60 sn'de bir
`mix_r4.bin` tutucu sayısını ölçer, **>1** ise alarm verir).

### Sağlık ölçümü (S1, adım 910/1000) — ve bir **ÖLÇÜM KABI kusuru (bana ait)**

`lsof` doğrulaması **tekrarlandı**, çift hâlâ **TEK**: sürücü `78743` fd `3w` · eğitici
`78745` fd `1w`/`2w` · `mix_r4.bin`'in **tek** tutucusu `78745`. İki gözcü de **sessiz**
(çakışma yok ⇒ doğru davranış).

| ölçüm | değer |
|---|---|
| `seg_1.log` | **9.378** bayt · **NUL 0** |
| adım satırı | **92** (adım 1 + her 10'uncu) · **tekil 92 · TEKRAR 0** |
| komşu adım farkı | `{9:1, 10:90}` — düzenli |
| adım süresi | **0,39-0,40 sn** · ilk adım 1,06 sn (yükleme) |
| `mps_satiri` · `sft_satiri` | **True · True** |
| `son60` bloğu · `seg_1.pt` · `eval_seg_1.json` | henüz YOK (segment sürüyor) |
| `sonuc.json` | var · `segmentler: []` (iskelet, artımlı yazım T-0063) |

> ⚠️ **Kendi ölçüm kusurum (K6) — sahte "çift yazıcı" alarmı:** ilk sayımımda
> `b.count(b"Adım ")` kullandım ve **181** buldum; T-0096'nın 1.000 adımda 101 satırı olduğu
> için bunu "≈2× ⇒ yine iki yazıcı" diye **neredeyse alarm** saydım. Ölçüm **çürüttü**:
> `"Adım "` dizgisi `"Adım Süresi:"` içinde de geçiyor ⇒ satır başına **2** eşleşme.
> **Ankralı (başlangıca sabitlenmiş) regex** ile sayınca **92 satır · 0 tekrar** çıktı; fark
> sayacın kendisindeydi, koşumda değil. Kural: **alt dizgi sayma, başlangıcı sabitle**
> (`^Adım\s+\d+/`). İyi ki `lsof` ikinci bir kanal olarak duruyordu — tek kanala bakıp
> hüküm verseydim **yanlış** olurdu.

### ✅ S1 BİTTİ — yeniden başlatmanın **kritik sınavı geçti**

```
egitim rc=0 sure=448.1s sn/adim=0.4481 mps=True sft=True ilk=2.5706 son60=1.1537±0.2036 (n=60)
eval rc=0 UNUTMA=KALDI A %+20.99 B -2.02p ROUGE 0.0980 ezber %0.00 tutarsizlik %31.00 kesisim %1.00
```

**`sn_adim 0.4481 < 0.70` ⇒ KESME ATEŞLEMEDİ ⇒ S2 başladı** (`seg_2.log` var). Yetim koşumda
aynı ölçüm **0,9317**'ydi; fark **rekabetin kalkmasıdır**. `kesme: None` · `erken_durma: None`.

**Kusur-5 düzeltmesi canlı doğrulandı** (sürücü çıktısı):
`[atif] ureten betigin ic sabiti BAYAT: 'T-0094' ⇒ kayit T-0097 (ham cikti byte-aynen korundu,
sha 95a18ffd…)` ⇒ provenance artık **hem dürüst hem tam**.

**Eşleşen adım 1.000'de T-0097 vs T-0096** (T-0096 S1: A +%23,31 · ROUGE 0,1097 · ezber %20):

| eksen | T-0097 | T-0096 | Δ |
|---|---|---|---|
| ROUGE-L | **0,0980** | 0,1097 | **−0,0117** (daha KÖTÜ) |
| A artışı | **%+20,9935** | %+23,3086 | **−2,3151p** (daha AZ unutma) |
| ezber | %0,00 | %20,00 | −20p |
| tutarsızlık | **%31,00** | %24,00 | **+7p** (daha KÖTÜ) |
| kesişim | %1,00 | %0 | +1p |

S1'de iki eksen **zıt yönde** iyileşti/bozuldu ⇒ tek başına S1 hüküm **değildir**; hüküm altı
segmentin tamamından çıkar. `tutarsizlik %31 >= %5` olduğu için `unutma_gec` zaten **KALDI**.

> **Not — ilan tahmini:** ilan §3 kesmenin **S5'te** beklendiğini yazıyordu. Ölçüm S1'de
> kesme **olmadığını** gösterdi ⇒ eğilim ilanın tersine; bu **şimdilik** yalnız bir gözlem,
> koşum bitmeden "ilan tuttu/tutmadı" **denmez** (sürücü de bu hükmü yalnız kesme anında verir).



---

---

## Faz 4 araçları — koşum sürerken YAZILDI ve **iki dalı da sınandı**

### `scratch/t0097_rapor.py` — iki dal ayrı ayrı ölçüldü

| dal | sınama | sonuç |
|---|---|---|
| **DURAN dal** | gerçek girdi (`segmentler: []`) | `rc=2` · `[DUR] … rapor URETILMEZ` · **çıktı dosyası YAZILMADI** (kontrol edildi) |
| **GEÇEN dal** | yetim koşumun saklanan kaydı (KESME, 1 segment) | `rc=0` · `.json` 12.292 B + `.md` 4.940 B · hüküm **KAPYI GECEN YOK** |

**Bu sınama BİR KUSURUMU YAKALADI (K7):** tablo `Δ ROUGE +0.0062` (ROUGE **ARTTI**) derken
C3 maddesi *"ATESLEDI (ROUGE DAHA DUSUK)"* yazıyordu. Sebep: iki eksenin **iyisi zıt
işaretli** (ROUGE'de yüksek iyi, A'da yüksek kötü) olduğu hâlde tek bir `yeni_kotu` kuralı
kullanmıştım ⇒ **C3 ters okuyordu**. Düzeltme: yön artık eksenden bağımsız **adıyla** verilir
(`artmaz` · `dusmez` · `duser` · `yuksek`) ve kanıt metni ekseni de yazar
(*"rouge: yuksek = IYI"*). Hafıza: `cift-yonunu-alan-adindan-oku`.

**Pozitif kontrol (bilinen işaretli sentetik segmentler, 6 koşul) — TEMİZ:**
A) ROUGE↑ + A↓ → hiçbiri · B) ROUGE↓ + A↑ → C1,C2,C3 · C) değişmedi → yalnız C1 ·
D) ezber %12 → C4 · E) ezber %9,99 → C4 **ateşlemez** (katı eşik) · F) kesişim %10 → C5.

> **İki ara bulgu (ikisi de kabın kusuruydu, kodun değil):**
> 1. İlk kontrolümde `curutmeler()`'e **boş listeler** geçtim ⇒ C4/C5 veri görmedi ve
>    **C5 denetimim VAKUM geçti**. *Boş liste ile "geçti" kanıt değildir.*
> 2. C5 **`kesisim %80'e çıkmaz`** kuralı gereği **her koşumda ateşler** (eşik insan
>    tavanının ~6 katı) ⇒ **ateşlemesi BİLGİ TAŞIMAZ**. Bu artık artefaktta
>    `bilgi_tasimaz: true` + not olarak yazılı.

### `scratch/t0097_kapanis.py` — 7 kapı, deneme koşumu **5/7**

```
[OK ] 1 on-kayit §4/§5/§6 kural METNI ayni (§4=True §5=True §6=True)   ← BAGIMSIZ dogrulandi
[OK ] 1 ilan TAM dosya ayni (1efacbb6…)
[OK ] 2 donmus yuzey 9 artefakt · once<->canli HEPSI ayni · degisen=[]
[OK ] 3 beyan denetimi (mtime >= claimed_at): kaynak/veri 29 + pyc 5 · DOKUNULUP BEYAN EDILMEYEN: YOK
[OK ] 4 derleme: 5/5 betik OK
[DUS] 5 rapor<->json: RAPOR YOK        ← Faz 4 henuz yazilmadi: DOGRU davranis
[DUS] 6 kiralar: 4 kira T-0097 adina acik  ← kapanista birakilacak
```

**On-kayıt çıpası — bir boşluk ölçüldü ve ÜÇTE İKİSİ KURTARILDI:**

`once.json`'ı üreten komut **hiçbir artefakta yazılmamıştı** ⇒ `birlesik_kural_sha256`'nın
hangi biçimle hesaplandığı kayıtlı değil. **426** biçim (tüm sıralar × ayırıcılar ×
sarmalar) denendi, **tutmadı** ⇒ o alan **DOĞRULANAMADI** olarak raporlanır (**K8**: cipaya
reçetesi olmayan digest yazdım). Ama **asıl olan kurtarıldı:** bölümler
`## §N` başlığından sonraki `## ` başlığına kadar **ham metin** olarak kesilince
§4/§5/§6 digestleri **3/3 birebir** tuttu. Yani kural metninin değişmediği **bağımsız
olarak kanıtlandı**; birleşik digest onların türeviyse ek bilgi taşımaz, türevi değilse
zaten yanlış kapıdır ⇒ **kapı düşmez**.

> **Kapanış aracının kendi kusuru (K10):** ilk sürümü, koşum bitmediği için henüz
> yazılmamış `korunan_sonra` alanını **"değişti"** saydı ⇒ **9/9 artefakt için sahte DÜŞ**.
> "Yokluk" ile "değişti" ayrımı eklendi: yokluk = **ÖLÇÜLEMEDİ**, hüküm `once<->canli`
> üzerinden. (Bu, ilacı olmayan bir alarmdı; ölçüm kabı düzeltildi, gözlem çürütülmedi.)

**Kapsam dışı bırakılan (beyan):** `.agent-bus/state/` · `log/` · `inbox/` — bus'ın **kendi**
durumu benim yazımım değil. `.agent-bus/notes/` **kapsam içinde** bırakıldı, çünkü kendi
notum da bir yazımdır ve dışlamak beyan edilmemiş bir yazımı gizlerdi.

---

## Sırada

Faz 3'ün altı segmenti (Monitor ile izleniyor; kapanış `SONUC_BITTI` **ve** `rc=0` ile) ·
Faz 4 (`scratch/t0097_rapor.py`, `scratch/t0097_kapanis.py`; `data/eval/anka_r18_ceket_2026-09-21.{md,json}`;
T-0096 ile eşleşen adımlarda yan yana; Ç1–Ç6 **ayrı ayrı**; `venv/bin/pytest` koşumdan SONRA).
Kapanışta `on_kayit_cipasi` (birleşik kural digesti `bfca40fd…`) **yeniden çıkarılıp**
karşılaştırılacak — değişirse kapı **DÜŞER**.

**Kapanış sırası (K17 gereği hepsi koşum BİTTİKTEN sonra — ölçüm kabı ölçülenin içine girmez):**

1. `SONUC_BITTI` + `rc=0` doğrulanır; segment tablosu **6/6** dolar.
2. S6 tam duyarlıkta ölçülür, ARA SONUÇ **n=6**'ya çıkarılır.
3. `venv/bin/python scratch/t0097_rapor.py` **gerçek** koşum (monkeypatch YOK) ⇒ beyan edilen
   iki artefakt yazılır. Girdileri **küçüktür** (checkpoint yok) ⇒ ağır I/O üretmez.
4. `scratch/t0097_kapanis.py` ⇒ **7/7** beklenir (deneme koşumu rapor yokken 5/7 idi:
   kapı 5 `rapor_var=false`, kapı 6 kiralar dolu).
5. Dört kira serbest bırakılır (`.agent-bus/`, `data/`, `data/eval/`, `scratch/`) ⇒ kapı 6.
6. Kapanış **üç sinyalle**: `bus_report_result` + kiralar boş + `changed_files` **iki yönlü**.
7. `venv/bin/pytest` — taban kaydedilir; değişirse **regresyon**.

**Beyan edilen çıktıların dördü de şu an YOK** (`…ceket_2026-09-21.{md,json}` **ve**
`data/anka_a1r_ceket_r18.pt[.opt.pt]`) — teslim checkpoint'inin yokluğu **kasıtlıdır**:
hiçbir segment kapıyı geçmedi ⇒ *"PASS" yazılmaz* (T-0096/§4-5 korunur).

**Bu oturumda rapora eklenen kusurlar: K14 · K15 · K16 · K17** (hepsi ÖLÇÜM KABI/İŞLETME
kusuru, koşumda değil). Üretici artık **17 kusur · 7 sınır** basıyor.

---

## Faz 4 üreticisi CANLI veriyle ilk kez sınandı (S2 sürerken, 08:0x)

Üretici bugüne kadar **yalnız sentetik** segmentler ve **yetim** bir kayıt üzerinde sınanmıştı.
Gerçek `sonuc.json`'a (tek segment: S1) karşı çalıştırıldı: betik **yerinde** yüklendi
(`importlib` ile, `KOK` doğru kalsın — kopyalama kabı değiştirir, T-0089) ve `CIKTI_JSON` /
`CIKTI_MD` **`$TMPDIR`'e** çevrildi.

* `rc=0` · beyan edilen artefakt **DOKUNULMADI** (ölçüldü) · `[hukum] KAPIYI GECEN YOK`.
* `n_eslesen = 1` ⇒ E6 sınırı **canlı olarak** üretildi (aşağıda).
* Eşleşen tablo S1'de: T-0097 `ROUGE 0,0980` / `A +%20,9935` / `ezber %0,0000`
  ↔ T-0096 `0,1097` / `+%23,3086` / `%20,00` ⇒ **ROUGE daha düşük, unutma daha az**.
  Tek noktada iki eksen **zıt yönde**; bu bir hüküm değil, **bir noktadır**.

**İki iyileştirme yapıldı, ikisi de ölçüme bağlandı:**

| # | ne | neden |
|---|---|---|
| **E6** | örneklem ağırlığı sınırı — `n_eslesen` **hesaplanır**, sabit yazılmaz; `n < 6` ise basılır | Ç1/Ç3 "eşleşen adımlarda" der; **n=1** ile **n=6** aynı ağırlıkta okunmamalı ([[kapi-orneklem-zayifsa-gecmek-kanit-degil]]) |
| **K12** | kontrolün **beklentisi** yanlıştı, kod değil | aşağıda |

**K12 (ölçüm kabının kusuru — bu oturumda üçüncüsü):** iki sentetik kol kurdum ve **C1'in
beklenen değerini tabloya yanlış yazdım** — A kolunda (ROUGE **artar**, A **düşer**) C1'in
**ateşlemesini** bekledim. Kod ateşlemedi ve **kod haklıydı**: C1'in çürütücüsü *"ROUGE
artmaz **VE** A düşmez"*tir ⇒ **iyileşme gösteren kol C1'i ateşleyemez**. Beklenti
**sezgiden değil ilandan birebir** okunmalıydı. Düzeltilmiş beklentiyle kontrol **6/6 TEMİZ**
(A: `C1–` `C2–` `C3–` `C4+` `C5+` `C6=ÖLÇÜLEMEDİ` · B: `C1+` `C2+` `C3+` `C4–` `C5+` `C6=ÖLÇÜLEMEDİ`
— iki **zıt** kol, dört yön kuralının **dördünü** de sınıyor).

> Bu, projenin kendi dersinin tekrarı: **pozitif kontrol "kırmızı yanarsa" önce kontrolün
> kendisini sorgula.** Yanlış beklenti, **doğru kodu suçlu ilan eder**.

---

## Adım süresi S1→S2 yükseliyor — bu **kesme eşiğinin kabı** hakkında bir ölçüm

S2 sürerken `sn_adim` **0,6012** ölçüldü; S1 **0,4481**'di ⇒ segmentler arası **+%34**.
Bunu "makine meşgul" diye okumadan **önce** iki şey ölçüldü:

**1) Açılış payı DEĞİL.** Loglardan düzgün hal (ilk–son kayıt arası) çıkarıldı:

| segment | ortalama (açılış dahil) | düzgün hal | açılış payı |
|---|---|---|---|
| S1 | 0,4413 | **0,4406** | 0,7 sn |
| S2 | 0,6007 | **0,6004** | 0,3 sn |

⇒ Fark **hesaplama**da, yüklemede değil.

**2) Sistematik mi, arıza mı?** T-0096'nın **kendi kaydından ölçüldü** — ama kayıtta `sn_adim`
**yok** (*T-0096'nın kendi kusuru; bu raporun `kendi_kusurlarim` K1'i DEĞİL, o V4'tür — ad
çakışması burada açıkça ayrıştırılır*); `sure_s` **var** ⇒ seri `sure_s / adım_sayısı` ile
**türetildi** ve türetim **açıkça işaretlendi**:

| segment | T-0096 `sure_s` | T-0096 `sn/adim` *(türetilmiş)* | T-0097 `sn/adim` *(kayıtlı)* | fark |
|---|---|---|---|---|
| S1 | 404,0 | **0,4040** | 0,4481 | +10,9% |
| S2 | 896,2 | 0,4481 | **0,5872** | +31,0% |
| S3 | 1.395,5 | **0,4652** ← *tepe* | 0,5017 | +7,9% |
| S4 | 1.788,2 | 0,4471 | 0,4633 | +3,6% |
| S5 | 2.636,9 | 0,4395 | 0,4623 | +5,2% |
| S6 | 3.504,2 | 0,4380 | *(koşuyor)* | — |

⇒ **S1→S2 yükselmesi T-0096'da da var**, ve orada tepe **S3**'te (0,4652); sonra 0,4380'e
oturuyor. Yani bu, **koşumun kendi sistematiği**; "rakip GPU tüketicisi" hükmü **bu ölçümle
kurulamaz** ([[cift-egitici-ayni-gpu-kilitler]] uyarısı: önce *kaç tüketici* olduğunu ölç —
`lsof` ile **tek eğitici** doğrulandı, PID 79123).

> 🚩 **K14 — BU TABLOYU ÖNCE ÖLÇMEDEN YAZDIM (kendi kusurum).** Bu bölümün ilk sürümünde
> T-0096 serisini `0,3988 · 0,4446 · 0,4627 · 0,4451 · 0,4383 · 0,4371` diye yazmıştım ve
> **S2'yi "yerel tepe"** ilan etmiştim. Artefakttan ölçülen gerçek seri
> **`0,4040 · 0,4481 · 0,4652 · 0,4471 · 0,4395 · 0,4380`** — her nokta **~0,001–0,006 sapmış**
> ve **tepe S3'te**, S2'de değil. Sayılar yakın olduğu için hüküm *yönü* ayakta kaldı; ama
> **hükmü taşıyan ikinci yarı (tepe konumu) yanlıştı**. Aynı sınıf:
> [[kaynak-satir-numarasi-olculur]] · [[uc-kez-ayni-sinif-iddia]] — *sayı doğru görünüyor,
> ölçüm yok.* Düzeltme: bu tabloda **her hücrenin kaynağı** (türetilmiş/kayıtlı) yazılıdır.

> **Tuzak (kayda geçsin):** T-0096'nın **S2**'si için türetilen **0,4481**, T-0097'nin **S1**
> değeriyle **dört hanede birebir aynı**. Bu **rastlantı**dır; iki tabloyu karşılaştıran okuyan
> bunu "aynı ölçüm" sanabilir ⇒ satır **etiketlenmeden** yan yana konmaz.


> **KENDİ İDDİAM ÇÜRÜDÜ (S3 ile):** S2 geldiğinde "S1→S2 sıçraması **kalıcı bir basamak**"
> dedim (S3'ün ortalaması S2'ninkiyle neredeyse aynı olduğu için). **S3 ölçüldü: 0,5017** —
> S2'nin 0,5872'sinden **düşük**, S1'in 0,4481'inden yüksek. Yani **basamak değil**,
> **monoton olmayan dalgalanma** (0,45–0,59). İddia ölçümle çürütüldü ve burada düzeltiliyor.

> **Eşik okuması için sonuç:** `KESME_SN_ADIM=0.70` **segment indeksine kör** bir mutlak
> eşiktir; S1 tabanı ile S2 tabanı aynı şeyi ölçmez. Kesme **ateşlemedi** (en yüksek 0,6012 < 0,70)
> ve ilan edilen eşik **değiştirilmez** — ama T-0097'nin tabanı T-0096'nınkinden **her segmentte
> yüksektir** (ölçülen: **+3,6% … +31,0%**, en büyük fark S2'de). **Hangi nedenle** yüksek
> başladığı **ölçülmedi** ve bu bir **açık** olarak rapora yazılır (hipotez "makine yükü" ≠ ölçüm).
> **Dosya boyutu elendi:** `mix_r4.bin` T-0096'da **2,0 MB**, T-0097'de **4,0 MB** (g≈2,00 —
> ilan edilen genişlemenin kendisi); iki boyut da sayfa önbelleğine sığar ⇒ adım süresi farkını
> **açıklamaz**. Geriye kalan adaylar **ölçülmedi**, spekülasyon rapora girmez.

---

## SONUÇ — S1…S6 (6/6 segment, KAPANDI) · n=6 eşleşen nokta

Koşum `rc=0` · **12110,6 sn (3,36 sa)** · `bitti_utc 2026-09-21T08:07:21Z` · teslim **YAZILMADI**.

| küm. adım | `sn/adim` | T-0097 A | T-0096 A | **ΔA** | T-0097 ROUGE | T-0096 ROUGE | **ΔROUGE** |
|---|---|---|---|---|---|---|---|
| 1.000 | 0,4481 | +%20,9935 | +%23,3086 | **−2,3151** | 0,0980 | 0,1097 | −0,0117 |
| 3.000 | 0,5872 | +%36,4895 | +%39,2786 | **−2,7891** | 0,1210 | 0,1173 | +0,0037 |
| 6.000 | 0,5017 | +%46,7814 | +%46,7803 | +0,0011 | 0,1317 | 0,1313 | +0,0004 |
| 10.000 | 0,4633 | +%51,9951 | +%56,4166 | **−4,4215** | 0,1519 | 0,2065 | **−0,0546** |
| 16.000 | 0,4623 | +%56,3044 | +%65,0139 | **−8,7095** | 0,2024 | 0,2777 | **−0,0753** |
| **24.000** | 0,4767 | +%62,2644 | +%75,6246 | **−13,3602** | 0,1983 | 0,3035 | **−0,1052** |

*(Δ = T-0097 − T-0096. **A'da negatif İYİ** (daha az unutma) · **ROUGE'da pozitif İYİ**.)*

### 🔎 Yapı: **ayrışma**, sabit kayma değil

S1→S6 boyunca **her iki eksende** fark büyüyor (tek ara nokta 6.000, çözünürlük içinde).

> **En sert bulgu — yön değişimi:** 16.000→24.000 arasında T-0097'nin ROUGE'u **DÜŞTÜ**
> (0,2024 → **0,1983**) ve A'sı **yükseldi** (56,30 → **62,26**); T-0096'da ise ROUGE
> **yükseldi** (0,2777 → **0,3035**) ve A da yükseldi. ⇒ İki kol **aynı yönde gitmiyor**:
> T-0097'nin ceket ekseni **tepe yapıp geriledi**. Bu, "genişleme ceketi yakınsatır"
> öncülünün (Ç3) yalnız *daha az* değil, **giderek daha az** doğru olduğunu gösterir.

| 16.000 → 24.000 | T-0097 | T-0096 | yön |
|---|---|---|---|
| ROUGE | 0,2024 → 0,1983 | 0,2777 → 0,3035 | **ZIT** |
| A artışı | +%56,30 → +%62,26 | +%65,01 → +%75,62 | aynı (ikisi de artıyor) |

**Çürütme maddeleri — n=6 (KESİN):**

| # | hüküm | kendi sayılarıyla | **ateşleyen marj** |
|---|---|---|---|
| C1 | **ateşlemedi** | ROUGE ARTAN 2/6, A DÜŞEN 5/6 | — |
| C2 | **ateşledi** | A **1/6** kez daha yüksek | **+0,0011** ← *K13, sıfır-genişlik artefaktı; kanıtı büyümedi* |
| C3 | **ateşledi** | ROUGE **4/6** kez daha düşük | en küçük −0,0117 · **en güçlü −0,1052** |
| C4 | ateşlemedi | en yüksek ezber %0,0000 (eşik %10) — **6/6 segmentte** | — |
| C5 | **ateşledi** | en yüksek kesişim %10,00 < %80 | **eşik VAKUM** (insan tavanı %13,00) |
| C6 | **ÖLÇÜLEMEDİ** | "g=1 kazancı" referansı; **g=1 kolu YOK** | eşik **uydurulmadı** |

> **Kanıt ağırlığı n ile büyüdü, ve hüküm DEĞİŞMEDİ:** n=4'te C3 tek zayıf noktaya dayanıyordu;
> n=5'te üç; n=6'da **dört** — ve en güçlü marj her adımda büyüdü (−0,0117 → −0,0753 → −0,1052).
> C1 6/6 noktada ateşlemedi ve **ateşlememesi tesadüf değil**: A 5/6 noktada **düştü** ⇒
> Ç1'in "VE" bağlacı hiçbir noktada sağlanmadı. C2 ise **tek bir artefakt noktasında** takılı
> kaldı (1/6) ⇒ K13'ün kusuru **kanıtla büyümedi**, aynen beyan edildiği gibi.

### 🎯 Hüküm — ölçülen, yorum değil

1. **Külliyat kısıtı gerçek:** jeton bütçesi ve olgu-maruziyeti sabitken külliyatı `g≈2,00`
   büyütmek unutmayı **her noktada azalttı**, fark adımla **monoton büyüdü** (ΔA 24.000'de
   **−13,36 puan**). Ç1 **ateşlemedi** ⇒ *"kısıt külliyattır"* öncülü **çürütülmedi**.
2. **Ama eski ölçüt bunu göremiyor:** ROUGE aynı noktalarda **düştü** (ΔROUGE **−0,1052**),
   üstelik son segmentte **geriledi**. Ç3 **ateşledi** ⇒ kapsam kayması: ölçüt hâlâ **eski 450
   perspektifi** sorar (E2).
3. **Bedeli ve kazancı aynı koşumda:** *"daha çok marangozluk öğrendi"* **DENMEZ** —
   ölçülmedi (E2). *"Eşleşen adımlarda unutma azaldı, eski sorularda yakınsama azaldı"*
   denir; **mekanizma ölçülmedi** (E7, hipotez: seyreltme).

**Ölçülen diğerleri:** `ezber %0,00` **6/6** ⇒ katalog açılımı ezber üretmedi (Ç4 ateşlemedi) ·
`tutarsızlık` %3–31 arası, son nokta %9 · `kesisim` hep ≤ %10.

> **Bu bir YORUM DEĞİL, ölçüm:** her iki sayı da **eşleşen adımlarda** ölçüldü ve `sonuc.json`'dan
> tam duyarlıkta okundu. Ama **neden** böyle olduğu **ölçülmedi** — makul hipotez: genişleyen
> külliyat aynı bütçede **seyreltiyor** ⇒ ceketin adım başına baskısı azalıyor ⇒ hem eski
> sorulara yakınsama hem taban tahribi azalıyor. **Bu hipotezi bu koşum kendi başına test
> ETMEZ**: seyreltme ile "daha çok olgu öğrenme" aynı yönde işaret verir; ayrım için
> **yeni olguların ölçüldüğü** ikinci bir eksen gerekir — o eksen bu koşumda **YOK**
> (operatör kararı 5: held-out sabit). ⇒ **Hipotez rapora "hipotez" olarak girer; ölçüm diye
> yazılmaz.** Raporun sınırlar bölümüne **açık** olarak da yazılır: *"iki eksen zıt yönde
> ayrıştı; hangi mekanizmanın ürettiği ölçülmedi."*

**Çürütme maddeleri — n=5 ara durumu (KAYIT İÇİN, sonuç yukarıdaki n=6 tablosudur):**

| # | hüküm (n=5) | kendi sayılarıyla | **ateşleyen marj** |
|---|---|---|---|
| C1 | **ateşlemedi** | ROUGE ARTAN 2/5, A DÜŞEN 4/5 | — |
| C2 | **ateşledi** | A 1/5 kez daha yüksek | **+0,0011 puan** ← *K13, bkz. aşağıda* |
| C3 | **ateşledi** | ROUGE 3/5 kez daha düşük | **−0,0117** (10.000'deki −0,0546 ve **16.000'deki −0,0753 daha güçlü**) |
| C4 | ateşlemedi | en yüksek ezber %0,0000 (eşik %10) | — |
| C5 | **ateşledi** | en yüksek kesişim %10,00 < %80 | **eşik VAKUM** (T-0096/K3 sürüyor) |
| C6 | **ÖLÇÜLEMEDİ** | "g=1 kazancı" referansı ister; **g=1 kolu YOK** | eşik **uydurulmadı** |

> **Bu tablo neden duruyor:** n=4 → n=5 → n=6 boyunca **hiçbir hüküm yön değiştirmedi**
> (C1 hep ateşlemedi, C2 hep tek artefakt noktasında, C3 hep ateşledi ve güçlendi). Ara
> durumları **silmek**, "hükümler önceden mi biliniyordu?" sorusunu cevapsız bırakırdı —
> sıra kayıtta kalır. n=6 hükmü üstteki tablodur.

### 🚩 K13 — ilanın çürütücü eşiği **SIFIR GENİŞLİKTE** (benim kusurum, ilanı ben yazdım)

C2'nin çürütücüsü *"A, eşleşen adımlarda T-0096'nınkinden **yüksek**"*; eşik **0**. 6.000 adımda
fark **+0,0011 puan** çıktı ve C2 **ateşledi** — *"genişletme unutmayı ARTIRDI"*. Ama
**+0,0011 puan, ölçüm çözünürlüğünde ayırt edilemez** bir farktır; kriter onu bir **etki**
gibi hükümleştiriyor.

* Eşik **DEĞİŞTİRİLMEZ** — ilan on-kayıtlı ve ölçümden sonra değiştirilemez (T-0067).
* Kusur **kayda geçer**; üretici artık **her ateşleyen hükmü marjıyla** basar
  (`en_kucuk_atesleyen_fark`) ⇒ okuyan **+0,0011**'i görür ve ağırlığını kendisi verir.
* İlan yazılırken bir **minimum etki büyüklüğü** ilan edilmeliydi; **edilmedi**.
* Tam bu sınıf: [[tavan-artefakti-kapi-gecmez-kanitsizlik]] (*fark tam 0 ⇒ "AYIRT EDEMEDİ"*) —
  burada fark **0,0011**, yani "tam 0" değil ama **aynı ölçüm gürültüsünün içinde**.

---

## S6 (son segment, 16.000→24.000) — canlı sağlık ölçümü

S6 sürerken ilerleme **sürücünün stdout'unda DEĞİL**: sürücü `train.py`'nin adım satırlarını
kendi çıktısına aktarmıyor, eğitim günlüğü **`scratch/t0097_kos/seg_6.log`**'a yazılıyor.
Bunu ölçtüm çünkü sürücü günlüğü 07:02Z'den beri **2.006 baytta sabit** duruyordu ve
"ilerleme yok" gibi görünüyordu ⇒ **sessizlik ≠ durma**; önce **kaynağı** doğrula.

| ölçüm | değer |
|---|---|
| adım satırı (sağa-hizalı sayıya **dayanıklı** regex) | **256** (adım 1→2550) |
| **tekrar eden adım** | **YOK** (tek yazıcı teyit) |
| adım süresi: ort / min / max | **0,4783** / 0,44 / **0,81** |
| kesme eşiği `KESME_SN_ADIM` | 0,70 ⇒ **ateşlemedi** (en yüksek 0,81 **tek** örnek; ortalama pay bırakıyor) |
| tek eğitici (`lsof`) | PID 83727 — **tek** |

> 🚩 **K15 — SAYACIM VERİNİN ~YARISINI SESSİZCE DÜŞÜRDÜ (kendi kusurum).** İlk sağlık
> ölçümünde regex `Ad[ıi]m (\d+)/` idi, yani **tek boşluk** varsayıyordu. Ama `train.py`
> adım numarasını **sağa hizalı** basar (`Adım    1/8000`) ⇒ **1000'in altındaki her satır
> eşleşmedi**. Sonuç: `seg_6` için "**128** adım satırı, adım 1000→2270" diye yazdım; gerçek
> sayı **256**, aralık **1→2550**. Log **eksik değildi** — sayacım kördü.
> Logun başını *okumak* kusuru ortaya çıkardı; sayıyı *saymak* çıkarmadı.
> **En öğretici yeri:** doğru kuralı **bu notun kendisi, S1 sağlık ölçümünde zaten yazmıştım**
> — *"alt dizgi sayma, başlangıcı sabitle (`^Adım\s+\d+/`)"* — ve S6'da **o kuralı çiğnedim**
> (`Ad[ıi]m (\d+)/`). Kuralın yazılı olması uygulanmasını sağlamıyor; **kapıya bağlanması**
> gerekiyor. Bu, [[ilan-edilen-kural-ayirt-edici-olmali]] sınıfının ölçüm kabındaki karşılığı.
> Aynı sınıf: [[denetim-kapsami-iddiadan-dar]] (*sayı doğru, İDDİA yanlış*).

> 🚩 **K16 — SÜTUN KAYMASI: kaybı adım süresi sandım (kendi kusurum).** S2'nin adım-süresi
> profilini çıkarırken tuple'ı `(adim, loss)` diye kurdum ama `sure` yerine **kayıp** sütununu
> yazdırdım. Çıktı "pencereler 0,71–0,92" dedi ve ben bunu *"S2 ikinci yarıda iki katına
> çıkmış"* diye okumaya başladım — **tamamen yapaydı**; 0,71–0,92 **kayıp değerleriydi**.
> Doğru profil: `seg_2` ort **0,5810**, ilk yarı **0,597**, ikinci yarı **0,565** ⇒ **düz**,
> iki katına çıkma **yok**. Ders: `float()` alan bir tuple kurduğunda **hangi grup indeksinin
> hangi alan olduğunu** bir kez yazdır ve gözle doğrula.
> Aynı sınıf: [[gosterim-uyusmazligi-olcutu-oldurur]] · [[iki-sayi-celisiyor-sanma-once-kume]]
> (*ölçü BİRİMİ/sütunu ekseni*).

> **K6 tuzağı ile karşıtlık (kayda değer):** K6 **fazla sayma** tuzağıydı (alt dizgi şişirmesi).
> K15 ise **eksik sayma** tuzağı. İkisi aynı kökten: **sayacın örtük varsayımı görünmez.**
> Doğru alışkanlık: sayıyı **logun başından da** doğrula (ilk satır beklenen ilk adım mı?),
> yoksa "kaç satır var" sorusu sessizce yanlış cevaplanır.
> Ham alt-dizgi sayımı **513** verir, gerçek satır **256** ⇒ K6 şişmesi (%100) hâlâ geçerli.

**Bitiş kapısı:** `bdbaxzqjk` (arka plan, 30 dk sınırı yok) `SONUC_BITTI|Traceback|Killed|[DUR]`
desenini bekliyor ⇒ hem **başarı** hem **arıza** dalı kapsanır. 30 dk'lık gözcü penceresi
segment satırları için tazelendi (`bv6u5f65p`), çakışma gözcüsü `bkm4oyy26`.
Gözcü penceresi **dolmadan** koşum biterse tazeleme yapılmaz.

---

## KAPANIŞ (21 Eyl 2026)

**Kapanış aracı: 7/7 GEÇTİ**, `rc=0` (`scratch/t0097_kos/kapanis.json`).

| kapı | hüküm | not |
|---|---|---|
| 1 ön-kayıt | **OK** | §4/§5/§6 kural metni **aynı** · ilan **TAM dosya aynı** |
| 2 donmuş yüzey | **OK** | 9 artefakt · TAM (önce+sonra+canlı) · **değişen YOK** |
| 3 beyan (iki yönlü + zamansal) | **OK** | beyan 16 · gerçekleşen 58 (kaynak/veri 53 + pyc 5) · **dokunulup beyan edilmeyen YOK** |
| 4 derleme | **OK** | beş betik de `py_compile` temiz |
| 5 rapor↔json | **OK** | rapor **VAR**, sayılar makine kaydıyla uyumlu |
| 6 kiralar | **OK** | dört kira serbest ⇒ `kalan=[]` |

**Üç sinyal:** (1) `bus_report_result` · (2) **kiralar boş** ✓ · (3) `changed_files`
**iki yönlü** ✓ — `beyan_edilip_dokunulmayan` **iki kalem**: teslim checkpoint'leri;
**kapıyı geçen segment olmadığı için yazılmadılar** (kasıtlı, ilan §6/5).

> **Ölçüt notu — zamansal ölçüt burada YANLIŞ POZİTİF verir:** `data/eval/anka_r18_ceket_ilani_2026-09-21.md`
> `gercekte_degisen` listesinde görünür (mtime ≥ `claimed_at`), çünkü son erratum (§1.8)
> `claimed_at` ile ön-kayıt ölçümü arasında düştü. **Yetkili ölçüt İÇERİKtir** ve o kapı
> **yeşildir**: §4/§5/§6 digestleri **ve** tam dosya digesti **aynı**. ⇒ T-0092'nin zamansal
> ölçüt dersi burada **ters yönde** işliyor: zamansal ölçüt sahte pozitif üretirken, içerik
> ölçütü hükmü kesinleştiriyor. İlan **ölçümden sonra değiştirilmedi** — bu, iki bağımsız
> kanalla (bölüm + tam dosya) doğrulandı.

**Testler:** `venv/bin/pytest` ⇒ **`1 failed, 237 passed`** — beklenen taban. Düşen tek test
`tests/test_agent_gateway.py::TestGateway::test_gateway_http_server_endpoints`; nedeni
`socketserver` `bind` → **`PermissionError` (sandbox soket yasağı)**, **regresyon değil**.
*Damga notu:* `CLAUDE.md`'deki *"213 passed"* damgası **bayattır**; ölçülen güncel değer
**237**. `CLAUDE.md` **değiştirilmedi** (beyan edilen `writes[]`'te yok — çıpa belgesi).

**Teslim edilmeyenler (açıkça):** `data/anka_a1r_ceket_r18.pt[.opt.pt]` **yazılmadı**;
*"PASS"* hiçbir yerde yazılmadı (ilan §6/5). En iyi ceket ekseni raporda **beyan edilir**.
