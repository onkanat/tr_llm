# T-0089 · `train_dpo.py` kapıları — yürütüm notu

**Damga:** 20 Eyl 2026 · **Yürütücü:** claude · **Hüküm:** K1…K8 **TAMAMI KALDI**,
hiçbir çürütme maddesi ateşlenmedi.
**Rapor:** `data/eval/anka_r12_train_dpo_kapisi_2026-09-20.md`
`a9c0f66fe1e44f1ef91c24d50b9bdcd388f745b9f696cee5a61aa8892ad5db3f` ·
`.json` `9c0628248f4c8e3a61f4503bccdcd6e385cb4a05ff0432d49d24f5f6415799cc`

---

## 1. Görev neydi

T-0088 kapanış raporunun **§10/2** maddesi: `train_dpo.py`, T-0087 sınıfının **canlı 6. ve
7. sitesini** taşıyordu.
* `:51` `vocab_path = 'data/vocab.json'` — **BAYAT** (31.357) *ve* betik `--vocab` **kabul
  etmiyordu** ⇒ yol **değiştirilemez**.
* `:88/:89` iki checkpoint varsayılanı — **ikisi de silinmiş**.
* ve dosya yoksa `print` + `return` ⇒ **rc=0**.

## 2. Çekirdek ölçüm (görevin gerekçesi)

`git show HEAD:train_dpo.py` → `$TMPDIR`'e kopya → koşum:

```
Sözlük Yüklendi. Kelime dağarcığı boyutu: 31357        <-- bayat, sessizce
  -> Yüklenen Tercih Çifti Sayısı: 5117 (1081 atlandı)
Hata: SFT referans model dosyası 'data/kristal_model_sft.pt' bulunamadı!
rc=0
```

Betik **5.117 tercih çiftini okuduktan sonra** "bulunamadı" deyip **başarı** dönüyor.
Çağıran boru hattı DPO'nun yapılmadığını **hiçbir sinyalden** öğrenemez. Aynı sınıf,
yollar **açıkça verilse de** ateşliyor (H3: `--ref-model yok.pt` → rc=0).

## 3. Yapılan

Üç varsayılan **kaldırıldı**, yerine **fail-closed yapılandırma kapısı**:
`zorunlu_eksikler` · `yol_dogrula` · `sozluk_checkpoint_uyumu` — üçü de **saf fonksiyon**,
model yüklemeden sınanabilir. `--vocab` eklendi; `--ref-model/--sft-model` ve
`--active-model/--base-model` zorunlu; sessiz soyağacı dalı **kaldırıldı**.
`rc=2` ile durur (`_durdur`).

**Varsayılan BAŞKA BİR ADA TAŞINMADI** (T-0085 emsali: uydurma ad yasak) — yokluk
**açıkça** durdurulur. `--save-path` varsayılanı `active_model_path` **korundu** (özgün
semantik: DPO aktif modelin üzerine yazar).

`kristal` soy ağacı iddiası **kanıt olarak** EMEKLİ bloğunda duruyor (canlı atıf 0).

## 4. Kendi kusurlarım

### Ölçüm kabında (dördü)

1. **HEAD kopyasını `$TMPDIR`'de `PYTHONPATH`'siz koştum.** `sys.path.append(dirname(__file__))`
   depoyu değil `$TMPDIR`'i ekler ⇒ `ModuleNotFoundError: No module named 'src'` ⇒ **rc=1**.
   Bu, dört dakika boyunca **"taban rc=0"** iddiamı **çürütüyor gibi** göründü. Dikkatsiz bir
   koşum tabanı **rc=1** diye yazardı. Kap düzeltildi; gerçek taban **rc=0**.
2. **`find -newermt "2026-09-20 00:00"` 1 yanlış pozitif verdi.** `data/anka_a1r.pt` yerel
   mtime `02:55` (UTC+3) ama **UTC damgası `2026-09-19T23:55:08Z`** = **T-0080 Faz 5
   kaydı**; T-0089 `07:46:30Z`'de sahiplenildi ⇒ dosya **7 sa 51 dk ÖNCE** yazılmış. Eşik
   görevin `claimed_at`'ine (UTC) bağlandı ⇒ **0**.
3. **EMEKLİ bloğumun BAŞLIK satırı etiketsizdi** (etiketi 3. satıra düşürmüştüm) ⇒ T-0086
   aracı `train_dpo.py` için **`olu=2`** ölçtü. Aracın ilan edilmiş kuralı yalnız **başlığı**
   etiketli bloğu ayıklar. Etiket başlığa taşındı ⇒ **`olu=0`**. **Her iki sayı da raporlandı**
   — etiketlemeyi gizlemek, aracın ölçtüğü kusuru saklamak olurdu.
4. **Etiket kuralını varsaydım.** Aracın kodu okundu (`ETIKET_BELGE = TAR[İI]HSEL`, blok =
   ardışık `>` satırları, etiket **başlıkta**) ve **dört dalda** sınandı: etiketli→ayıklandı ·
   etiketsiz→yakalandı · etiket blok DIŞINDA→yakalandı · etiket 2. satırda→yakalandı.
   Araç **kör değil**.

### Yeni dosyalarımda (ikisi de kendi ürettiğim canlı ölü-atıf)

5. **Yeni test dosyamın docstring'i** ölü artefakt adını etiketsiz andı ⇒ KOD'da `olu=1`.
   T-0086 aracının **yakalamak için var olduğu sınıfı ben ürettim**. Ad KOD yüzeyinden
   çıkarıldı, kanıt KAYIT kapsamına (raporda) taşındı.
6. **`USER_GUIDE.md`'ye eklediğim not** ölü yolu etiketsiz andı ⇒ BELGE +1. Blok **gerçekten**
   tarihsel kayıt olduğu için başlığına `TARİHSEL` etiketi kondu (ad silinmedi — okuyucu için
   taşıyıcı).

**Ağaç etkisi:** KOD **133 → 130** · BELGE **202 → 201** · dört hedef dosyamda **0 canlı
ölü-atıf**. Görev ağacı **temizledi**.

## 5. Şartname beyanım ölçümle düzeltildi

Şartnamede *"--active-model YOKSA aktif model **sessizce** ref'ten başlatılır"* yazıyordu.
**Ölçüm:** eski `else` dalı **bir bilgi satırı basıyordu** (`HEAD :115`).
⇒ Durum **"sessiz"** değil **"uyarısız"**dır. Çürütülmeyen kısım: yüklenen model adı
verilenden **FARKLI** ve **uyarı yok**. Sayı doğruydu, **iddia** genişti.

## 6. Kapanış

* pytest **1 failed, 216 passed → 1 failed, 223 passed**; sapma **TAM +7**.
* Donmuş 10 desen: **72 dosya numaralandı, 0 yazıldı**; 28 baseline yolunda **0 değişen
  digest, 0 silinen**; `.pt`+`.bin` **25 = 25**, yeni/kaybolan **0**.
* **Eğitim koşusu YOK**, hiçbir `.pt`/`.bin` üretilmedi.
* **Commit yok, push yok, `git add -A` yok.**
