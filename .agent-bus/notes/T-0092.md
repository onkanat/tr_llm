# T-0092 · AdamW momentleri — yürütüm notu

**Damga (betik hesapladı):** `2026-09-20T11:52:54Z` · **Yürütücü:** claude · **Hüküm:**
**G0…G6b kapılarının TAMAMI GEÇTİ (10/10)**; ilan edilen üç çürütme maddesinin **hiçbiri
ateşlenmedi (0/3)**. Kusur **ölçüldü** (varsayım değil), **kapatıldı** ve **maliyeti beyan edildi**.
**Rapor:** `data/eval/anka_r15_adamw_moments_2026-09-20.md`
`786212320e743fdb55eabcb15f055f25e02a90a8318bf364268300050311cc08` · **`.json`**
`data/eval/anka_r15_adamw_moments_2026-09-20.json`
`2cb90f2a527d1c97e2ad1fdb8a48c5fcfd4bc156a790b89c0227d7d2dbb71ca1`

> **Donmuş yüzeye YAZMA YOK.** `data/**` donmuş desenleri · `src/compiler/**` ·
> `src/llm/tokenizer.py` · `src/llm/frozen_guard.py` — hiçbirine yazılmadı. İki checkpoint
> (`anka_a1.pt`, `anka_a1r.pt`) **yalnız okundu**. Koşumlar 2 adımlık **CPU sözleşme
> sınamalarıdır**; bu görev **model eğitmedi**.

---

## 1. Görev neydi

Operatör: *"Onay bekleyen işler için devam et"* ⇒ dört iş akışından **(4) AdamW momentleri**
(README yarısı T-0090'da kapandı). Kaynak **tam metinden** okundu: T-0080 kapanış raporu **§8/3**.

## 2. Çekirdek ölçüm (değişiklikten ÖNCE)

| Ölçüm | Sonuç |
|---|---|
| Referans `optimizer.state` kaydı (2 adım sonrası) | **101** |
| **MEVCUT devam** (yalnız `model.state_dict()` yazılmış) | **0 ⇒ momentler KAYIP** |
| Yan dosyalı devam | **101** |
| Gidiş-dönüş bit-özdeş | **True** (farklı alan **[]**) |
| Kaybın ölçüsü: `||delta||` momentli → momentsiz | 4,735192 → 8,194721 ⇒ **1,7306×** |
| Yan dosyadan gelen adımın referanstan farkı | **TAM `0,000e+00`** |

**Maliyet (ölçüldü):** yan dosya **712,9 MiB = modelin %200,01**'i; `sha256_file` **0,160 sn**
(2232 MiB/sn). Asıl maliyet **disk ayak izidir**, digest süresi ihmal edilebilir.

**ÇÜRÜYEN İDDİA:** *"checkpoint'lerin %21'i mask tamponu"* — `anka_a1r.pt`'de `mask`/`cached`
anahtarı **YOK** (ölçüldü). Bu artefakt için **geçersiz**.

## 3. Yapılan

* `train.py`: `--save-optimizer` / `--load-optimizer`; yan dosya = `optimizer.state_dict()` +
  `adim` + `model_sha256`; **eşleşme kapısı**; yan dosya yokken **sessiz düşmeyen** uyarı;
  `check_frozen_save_path` **yan dosya için de**. **VARSAYILAN KAPALI.**
* `--seed N` **eklendi** (ölçüm aracı). Gerekçe ölçüldü: `train.py`'de **hiç tohum kurulmuyordu**
  ve `KristalDataset.get_batch` global `torch.randint` kullanıyor ⇒ *"değişiklik davranışı
  değiştirdi mi?"* sorusu **ölçülemezdi**. G1'in digest ayağı bu bayrağa dayanır.
* `tests/test_train_optimizer_state.py` (**YENİ**, **10 kapı**): G0 (tohum, **iki dal**) · G1 ·
  G2 · G3 · G4a (**duran-değil**) · G4b · G5 · G5b · G6a · G6b. **DURAN ve GEÇEN dal birlikte.**
* `tests/test_compiler_entrypoints.py`: `len(torch.save) == 1` **sayı sabitlemesi** →
  **guard sayısı ≥ save sayısı** ilişkisi (§6/6 ve aşağıda).

**Varsayılan logun sınırı (dürüst beyan):** model digest'i bayrak AÇIK/KAPALI **birebir aynı**;
ama **"log bit-bit aynı" DEĞİLDİR** — `diff` üç satır gösterdi (iki zamanlama değeri + kayıt
yolunu taşıyan satır). Kayıp satırları **aynı**. Yeni `[ckpt]` satırı **basılmadı**.

## 4. Kanarya — güçlendirilen kapı VAKUM mu?

`test_ast_train_py_has_frozen_guards` üç denek üzerinde koşturuldu (**testin KENDİ kodu**):

```
KANARYA-1 (guard'sız 3. save)  guard=2 save=3 → DÜŞTÜ   (kusuru yakalıyor)
KANARYA-2 (guard'lı 3. save)   guard=3 save=3 → GEÇTİ   (yanlış pozitif yok)
KANARYA-3 (gerçek train.py)    guard=2 save=2 → GEÇTİ
```

Neden gerekliydi: **yalnız konum** kontrolü (`save`'den önce bir guard var mı) **hiçbir zaman
düşemez**, çünkü erken guard **bütün** save'lerden önce gelir ⇒ o hâliyle **VAKUM KAPI** olurdu.

## 5. Bu görevde ölçülen YENİ bulgu (KAPSAM DIŞI — DÜZELTİLMEDİ)

**Donmuş kapı MUTLAK yollarda FAIL-OPEN.**

| Dal | `--save-path` | rc | Kapı | Artefakt |
|---|---|---|---|---|
| A | göreli donmuş model | 1 | **ATEŞLENDİ** | yok |
| B | göreli, **yalnız yan dosya** donmuş | 1 | **ATEŞLENDİ** (yan dosyayı anar) | yok |
| C | **MUTLAK** donmuş model | **0** | **ATEŞLENMEDİ** | **model + yan dosya YAZILDI** |

**Kök neden:** `src/llm/frozen_guard.py:59` — `os.path.normpath` **mutlak yolu göreli YAPMAZ**;
`data/*.pt` deseni `/Users/.../data/x.pt` ile **eşleşmez**.
**ÖNCEDENDİR:** dosyanın son commit'i **`4251c53` (2026-09-16)**; bu görevde **DEĞİŞMEDİ**.
Yalnız yan dosyayı değil **model kaydını da** etkiler.

**Neden düzeltilmedi:** dosya `writes[]`'te **değil**; **paylaşılan bir güvenlik modülünde**
kapsam dışı değişiklik başka görevlerin tabanını geçersiz kılar; projenin yerleşik uygulaması
ölçülen yeni kusuru **beyan edip** düzeltmemektir. **Operatör kararı bekler.**

**Rezidüel risk (BEYAN — ÖLÇÜLMEDİ):** model önce, yan dosya sonra yazılır ⇒ iki `os.replace`
arası kesinti **uyuşmazlık** bırakır ve eşleşme kapısı **durur**. Kesintiyi **üretip ölçmedim**;
ölçülen şey kapının **yanlış digest'te durduğudur** (G5); kesintinin **aynı kapıya** düştüğü
**çıkarımdır**.

## 6. Kendi kusurlarım

1. **v1 sonda SAHTE bulgu üretti:** durumu **parametre nesnesi kimliğiyle** karşılaştırdım;
   `optimizer.state` nesne anahtarlıdır ⇒ her arama `None` ⇒ *"bit-özdeş DEĞİL"*. Doğrusu
   `state_dict()`. **Bulgu değil, aracımın kusuruydu.**
2. **zsh unquoted değişkeni kelimelere ayırmaz.** `train.py $C ...` çağrısında `$C` **tek
   argüman** olarak geçti ⇒ koşum **varsayılan** bayraklarla çalıştı; ben diğer bayrakların
   uygulandığını sandım. Dizi ile yeniden ölçtüm. *"Komut bir şey yazdırdı" ≠ "ölçtü".*
3. **Kanarya sayacım `.attr`'ı okumadı** ⇒ ekrana `save=0` bastı ⇒ "bağımsız çapraz doğrulama"
   **boştu**. Düzeltilince `2/3`, `3/3`, `2/2` çıktı.
4. **İlk G6 testi donmuş yüzeye 356 MB yazdı:** testi **mutlak** yolla kurdum, kapı ateşlenmedi,
   koşum tamamlandı ve `data/` altına yazdı. Artefaktlar `finally` + `rm` ile **silindi**
   (kalıntı **YOK**). Test **göreli** yola çevrildi — orada kapı `torch.save`'dan önce durur,
   koşum maliyeti **sıfırdır**. **Test tasarımım kapının çalışacağını varsaymıştı.**
5. **Dosya digest'i karşılaştırmam SAHTE determinizm hatası verdi:** iki koşumu **farklı dosya
   adlarıyla** kaydedip `cmp` ettim; `torch.save` bir **ZIP** kabıdır ve **arşiv üye adları dosya
   adını taşır**. **Tensör tensör: 101/101 özdeş.** **Aynı taban adla** digest'ler **birebir eşit**.
6. **`writes[]` beyanım GEÇ kaldı.** `tests/test_compiler_entrypoints.py`'yi beyanımda **yokken**
   değiştirdim (kira kapsamındaydı — `tests/` dizini kiralandı — ama **beyan eksikti**).
   Spec **sonradan düzeltildi** (oku→yaz→**tekrar oku**); `spec_duzeltmeleri[0]` altında
   **önceki `writes[]` ve önceki spec sha256'sı saklandı**; `bus_send` ile bildirildi
   (`2026-09-20T11-50-47Z-claude.json`). **Sıra yanlıştı; kayıt düzeltilmedi, BÜYÜTÜLDÜ.**

## 7. Kapanış sinyalleri (üç sinyal)

**(a) Bildirim/kanıt:** bu not + rapor + `.json`; ham çıktılar `$TMPDIR/t0092/`; sonda
**kaynakları** `.json`'da **tam metin** ⇒ `$TMPDIR` silinse de ölçüm **yeniden üretilebilir**.

**(b) Kiralama:** T-0092 dört kiralama aldı — `train.py`, `tests/`, `data/eval/`,
`.agent-bus/notes/` (**donmuş DOSYA kiralanamaz**; beyan edilen altı yolun altısı da bu dört
kökün altında). Kapanışta **boşaltıldı** ⇒ kiralamasız yazma **0**.

**(c) `changed_files` — İKİ YÖNLÜ:** beyan edilen **6** yolun altısı da değişti (**YÖN 1 TAM**).

**YÖN 2'nin ÖLÇÜTÜ (yazılması zorunlu — aksi hâlde iddia kanıtından geniş olur):** ölçüt
*"kiralamada beyan edilmemiş her untracked dosya"* **DEĞİLDİR**; o kaba ölçüt **102 sahte
pozitif** verir, çünkü `data/eval/` altında **T-0062…T-0091'den kalma izlenmeyen raporlar**
zaten duruyor ve bu görev onlara **dokunmadı**. Doğru ölçüt **zamansaldır:** kira
köklerinde **`claimed_at` (2026-09-20T11:33:14Z) SONRASI** mtime taşıyan dosyalar.

Ölçüm (Python, `rglob` + `st_mtime >= claimed_at`; damga **betik hesapladı**):
pencerede değişen **10** dosya ⇒ **beyanlı 6** + **`tests/__pycache__/*.pyc` 4**.
`.pyc` dosyaları **iş artefaktı DEĞİLDİR** (pytest ikili önbelleği) ve `.gitignore:2`
ile **ignore** edilmiştir ⇒ *beyan edilmemiş **iş** artefaktı* = **0** ⇒ **YÖN 2 TAM**.

`len(cf) == len(set(cf))` → **True**. Ayrıntılı tablo `bus_report_result` kanıtındadır.
**Kendi kusurum (bu notu yazarken ölçüldü):** ilk YÖN 2 kontrolüm kaba ölçütü kullandı ve
**102 sahte pozitif** bastı; sayı doğru, **ölçüt yanlıştı** — [[denetim-kapsami-iddiadan-dar]]
ailesi. Ölçüt notta **açıkça** yazılıdır ki okuyucu aynı tuzağa düşmesin.

**(d) Diğer kanıtlar:**

* `venv/bin/pytest -q` → **1 failed, 237 passed** (77,30 sn). Taban **227**; sapma **TAM +10** =
  yeni kapı dosyası. Düşen tek test yine `test_agent_gateway.py` **sandbox soket yasağı** —
  ilan edilmiş taban davranışı; **bu görev o tabanı değiştirmedi**.
* **Donmuş yüzey denetimi (Python ile, kabuk `find` DEĞİL):** son 60 dk'da değişen `data/**`
  `.pt`/`.bin` = **0**; `data/anka_t0092_sonda*` kalıntısı **YOK**.
* **Eğitim koşusu YOK · push YOK · `git add -A` YOK.** Commit yalnız **beyan edilen** yollar
  için ve bu görevin kendi niteliğindedir.

**İddia sınırı:** bu görev bir **kayıt/yükleme sözleşmesi** kusurunu ölçtü ve kapattı. Ölçülen
şey **optimizer durumunun korunmasıdır** — modelin **dil kalitesi DEĞİL**. Eğitim kalitesine
etkisi **ölçülmemiştir**: ölçülen, **ilk güncellemenin büyüklüğü** (1,7306×) ve **gidiş-dönüşün
bit-özdeşliğidir**. *"Momentler kaydediliyor" ≠ "model daha iyi".* Ve **mevcut** iki checkpoint
moment **taşımaz** ⇒ gerçek kazanç **bundan sonra** kaydedilen checkpoint'lerde başlar.
