# T-0098 — Ölçüm kabı ve koşum işletmesi kusurlarını KALICI KAPIYA çevir

**Durum:** **KAPANIŞTA** · Faz 1 TAMAM (`olcum_kabi.py` 26 PASS · 0 HATA · rc=0) · Faz 2 TAMAM
(`kosum_kapisi.py` 9 PASS · 0 HATA · rc=0) · Faz 3 TAMAM (11/11 test, mutasyon 4/4 yakalandı) ·
Faz 4 yazıldı (rapor çifti) · **kapanış üç sinyalle yapılacak**
**Yürütücü anlatı günlüğü.** SPEC.md:134 gereği yürütüm sırasında yazılır.

**Operatör talebi:** *"Bu onyedi kusur için plan yap"* — T-0097'nin kendi raporuna yazıp
**düzeltmediği** 17 kusur (T-0067: ölçüm sonrası kusur tasarıma göre onarılmaz, rapora yazılır).
Bu görev o kaydı **yürürlüğe koyar.** Operatör kararları: (1) kapılar `tests/` + `scripts/`
(kalıcı) · (2) önce en ağırları · (3) K4 ardıl sürücüye taşınır, donmuş `scratch/t0097_marangoz.py`
**düzenlenmez.**

---

## Faz 1 — `scripts/olcum_kabi.py` (ölçüm kabının kendini doğrulaması)

Saf fonksiyonlar, global durum yok, tip ipuçları zorunlu (CLAUDE.md). Altı kapı: G1 `say_adim_satirlari`
(K6+K15) · G2 `tuple_alan_denetimi` (K16) · G3 `yon_adli`+`karsilastir` (K7) · G4 `sayi_kaynagi` (K14) ·
G5 `yokluk_beyani` (K10) · G6 `referans_dogrula` (K11).

**Kalibrasyon (ölçüldü, `scratch/t0097_kos/seg_2.log`):** doğru **201** (adım 1→2000) · kör tek-boşluk
sayacı **101** · şişen alt-dizgi sayacı **403** ⇒ **ikisi de reddedildi**. G1 iki **bağımsız** sayaç
kullanır (çapalı + `|` alan-ayrıştırma, dördü de etiket şartıyla); uyuşmazlarsa `SayacUyusmazligi` —
sessiz kalmaz.

**K7'nin kaynağı kopya DEĞİL, okuma:** `yon_adli` mantığı `scratch/t0097_rapor.py:134-163`'ten
**okunup** yazıldı (dosya DÜZENLENMEDİ); kopya, `tests/`te 40 noktalık ızgarada kaynağa karşı sınanır.

**İki kendi kusurum bu fazda yakalandı:** K18 (string literal içindeki örneği kod saydı ⇒ 2 sahte
pozitif) ve K19 (`_kontrol` FAIL basıp `hatalar`a yazmıyordu ⇒ **kapı FAIL basıp rc=0 dönebilirdi**).
İkisi de ölçümle bulundu, ikisi de fail-closed'a çevrildi.

## Faz 2 — `scripts/kosum_kapisi.py` (koşum işletmesi)

H1 `tek_egitici_onkontrol` (K5) · H2 `kosum_canli_mi`/`kosum_bekcisi` (K17) · H3 `timeout_denetimi`
(K4). `ps`/`pgrep` sandbox'ta yasak ⇒ **`lsof -t`** (T-0097/K5). Fail-closed: `lsof` yoksa
**GEÇMEZ**, `OrtamHatasi` verir.

**H3 gerçek sürücüde kayıtlı olandan FAZLASINI gördü:** eksik `timeout=` siteleri = **[179, 228]**;
K4 yalnız `:179` için kayıtlıydı. Kâhinin kusura kör olmadığının kanıtı bu — ve `:228` yeni kayıt
olarak deftere girdi (K28).

### 🔶 H2 ölçütü — **plandan BİLEREK SAPMA** (docstring'in söz verdiği gerekçe burada)

Plan *"tutucu **VE** `sonuc.json` taze"* (bir **AND**) diyordu. Uygulanan: **`tutucu >= 1` (KATI)**.
Gerekçe: AND, `sonuc.json` silindiğinde/okunamadığında kapıyı **fail-open** yapardı — T-0075'in dersi
*"okunamayan sinyalde durmayan koruma kapı değildir."* `sonuc_taze` yalnız **destekleyici kanıt**
olarak raporlanır; durma kararına girmez. `test_kosum_canli_kapisi`, `sonuc.json` **yokken de**
durduğunu sınıyor.

### 🔶 `Popen` — ilan edilen **kapsam sınırı**, gizli delik değil

`timeout=` yalnız onu **kabul eden** işlevlerde aranır: `run`/`call`/`check_call`/`check_output`.
`Popen` **dışarıda**: `Popen.__init__`'te `timeout` parametresi **YOK** (`inspect.signature` ile
ölçüldü) ⇒ istemek **yanlış pozitif üretir** (K21). `Popen` için daha zayıf ölçüt: değişkene bağlıysa
`.wait(timeout=`/`.communicate(timeout=` aranır; bağlanmayan `Popen` `sinirlama` listesinde **adıyla**
raporlanır — gizlenmez, ama `rc`'yi düşürmez, çünkü orada `timeout`'un varlığı bu kaptan ölçülemez.

## Faz 3 — `tests/test_olcum_kabi_kapilari.py` (kuralı YÜRÜTEN katman)

`pytest.ini` konvansiyonuna uyar (düz dosya, conftest YOK, `sys.path.insert` + `importlib.util`).
11 test. **Her kapı iki yönlü sınanır** (T-0075): bozuk fikstürde **ateşler**, iyi fikstürde **geçer** —
tek yönlü sınama, kapının "her şeye dur diyen sabit" olup olmadığını ayırt edemez.

**K12 bu dosyaya GÖMÜLDÜ:** G3'ün kural sözlüğü ve eksen eşlemesi **ilan §5'ten ayrıştırılır**
(`_ilan_kurallari()`), elle yazılmaz; iki yönlü hizalanır — (a) kodda ilanın attest etmediği kural
olmamalı, (b) ilanın ilan ettiği her kural kodda bulunmalı.

**Mutasyon sınaması (vakum kontrolü): 4/4 yakalandı** — K15 kör sayaç · K11 referans yutması ·
K4 timeout yutması · K7 işaret ters. İki kapı dosyası **bit-ödeş** geri alındı. Bu, testlerin
gerçekten kuralı düşürdüğünün kanıtıdır; sırf yeşil olmaları kanıt değildir.

**Üçüncü kez yanlış pozitif kontrol (K20).** `test_yon_eksenden_okunur`'un A-ekseni kanaryasının
**beklenen değerini yanlış yazdım** (`A` ekseninde `yuksek`/`dusmez` pozitif farkta **ayırt edilemez**).
T-0097/K12, sonra H2, sonra bu ⇒ *"beklentiyi sezgiden yazma"* kuralının **yazılı olması yetmiyor**;
bu yüzden G3'ün beklentileri artık **ilan §5'ten ayrıştırılıyor**. Test, aynı kural kelimesinin
(`duser`) iki eksende **zıt** hüküm vermesini kullanıyor: rouge'da ATEŞLER, A'da ATEŞLEMEZ.

**K9 burada kapanır:** testler `tests/__pycache__/*.pyc` üretir ⇒ `writes[]`'e **beyan edildi**
(T-0097'de edilmemişti). Ayrıca `_yukle`, gitignored `scratch/t0097_rapor.py`'yi import ederken
`sys.dont_write_bytecode` ile **kapatıldı** (K23): önbellek bayat olsaydı **beyan edilmemiş**
`scratch/__pycache__/`'e yazacaktı.

### K30 — guard'ın çalıştığı **ölçüldü**, sızıntı **başka** yoldan geldi

Kapanışın **iki yönlü** beyan denetimi, `scripts/__pycache__/` altında **beyan edilmemiş 2 `.pyc`**
buldu (`olcum_kabi`, `kosum_kapisi`). *"Guard tutmadı"* diye **varsaymadım**; üç dalı ölçtüm:

| dal | yol | `.pyc` yazdı mı |
|---|---|---|
| DAL 1 | `pytest tests/test_olcum_kabi_kapilari.py` (guard'lı `_yukle`) | **HAYIR** |
| DAL 3 | guard'lı ad-hoc import | **HAYIR** |
| DAL 2 | **guard'sız** ad-hoc import — **pozitif kontrol** | **EVET** |

⇒ **K23 iddiası çürümedi, DOĞRULANDI**: kalıcı test katmanı `.pyc` üretmiyor. Sızan yan ürün
**benim geçici doğrulama harness'ımdan** (guard'sız import). Düzeltme: 2 `.pyc` **kaldırıldı**, yüzey
**geri alındı** (20 dosya, en yeni `03:42:47Z` < `claimed_at 10:24:39Z`).

**Pozitif kontrolü kurmasaydım bu dal "temiz" görünürdü** — ve tersi de olabilirdi: kontrolü
kurmasaydım guard'ı haksız yere suçlayabilirdim. İkisi de aynı hatanın iki yüzü.

**Yapısal kalan (beyan edildi):** `scripts/__pycache__/` **20 dosya** taşıyor ve **hiçbiri** hiçbir
görevin `writes[]`'inde yok ⇒ K22/K29 ile **aynı sınıf**: beyan sınıflandırması `.pyc` üreten **tüm**
dizinleri kapsamalı (`scripts/`, `tests/`, `scratch/`), yalnız `"__pycache__/"` alt dizgisini değil.

---

## T-0097 araçlarında ölçülen kusurlar — **BEYAN, düzeltme DEĞİL**

Plan B1–B5 için *"bunlar henüz dondurulmamış araçlarda"* deyip **"düzeltilir"** diyordu.
**Ölçüm bu öncülü yanlışladı:** beşi de `scratch/t0097_*` içinde, yani **T-0097'nin tescilli kaydında**
(digest'leri `kapanis.json` gate 4'te ve notlarda kayıtlı) ⇒ düzenlemek **kapalı raporu bayatlatır**.
Kalemler beyan edildi, düzeltme **kapıya taşındı** (B4 → H3).

* **B1** `BIRLESIK_ARANAN` (`kapanis.py:42`) **ÖLÜ KOD** (grep -c = 1; yalnız atama).
* **B2** `denetim_donmus` **kısmi anahtar deliği** — `korunan_sonra`'da 15 anahtarın **1'i** varken
  araç `durum: TAM (once+sonra+canli)`, `dokunulmadi: True`, `degisenler: []` dedi. **Üretildi,
  varsayılmadı:** eksik anahtar `None` ⇒ asla "değişti" denemez. K26; G5 bu deliği kapatır.
* **B3** Kapanış banner'ının paydası **sabit `/7`** (`kapanis.py:339`): pay türetiliyor, payda literal
  ⇒ kapı eklenirse banner **yanlış** basar. K27.
* **B4** İkinci `timeout=` sitesi (`marangoz.py:228`) — **H3 ile ölçüldü ve kapıya bağlandı.**
* **B5** `.pytest_cache/` beyan sınıflandırmasında **YOK** (`grep pytest_cache kapanis.py` = 0;
  sınıflandırma yalnız `"__pycache__/"` alt dizgisine bakıyor). K22/K29 aynı sınıf.

> **🔻 GERİ ÇEKME — kendi plan iddiam ölçümle çürüdü.** Plan, `kapanis.py:108`'deki
> `cipa.get("birlesik_kural_sha256")` için **"fail-open"** demişti. **Ölçüm çürüttü:** araç o kalemi
> `"ozet"` sözlüğüne **hiç koymuyor**; `KAPI_DISI_BEYANLAR` altında *"DOGRULANAMADI (recete kayitli
> degil) — KAPI DEGIL, beyan"* diye **açıkça** dışlıyor ve `gecen = all(ozet.values())` yalnız
> **7 gerçek kapıyı** sayıyor ⇒ **fail-open değil, dürüst beyan.** İddia raporla **kayıttan
> düşürüldü**; plan metni düzeltilmedi, sapma rapora yazıldı (§6).

---

## Faz 4 — Rapor çifti ve kusur defteri

`data/eval/anka_r19_olcum_kabi_kapilari_2026-09-21.{md,json}`. Defter **normalize şemayla** yazıldı:

```json
{"id":"K15","yer":"...","metin":"...","kanit":"olcum","sinif":"olcum_kabi",
 "durum":"KAPI","kapi":"G1","tekrar":1,"kaynak":"T-0097"}
```

`sinif ∈ {ilan, olcum_kabi, kosum_isletmesi, beyan, surucu}` · `durum ∈ {KAPI, KAPATILDI, ERTELENDI, ACIK}`
· `kaynak` = kusurun ilk kaydedildiği rapor · `tekrar` = kaçıncı kez üretildiği.
**Ölçülen gerekçe:** `data/eval/*.json` taramasında yalnız **r18** kararlı `K1…K17` kimliği kullanıyor;
**r10/r15** `id: None`, **r13** `1,2,3,4` ⇒ *tekrarı önlemenin ön koşulu tekrarı ölçmektir* ve bugün
tekrar **ölçülemiyor.** Bu şema onu ölçülebilir kılar: **29 kayıt** (K1…K29), 12 KAPI · 8 KAPATILDI ·
5 ERTELENDİ · 4 AÇIK.

**Damga ve digestler ELLE YAZILMADI:** `damga_utc` `datetime.now(utc)` ile, digestler `hashlib` ile
betikte hesaplandı (zaman dilimi karışıklığı ve ezber digest sınıflarının ikisine karşı).

**Kapalı döngü denetimi (yazdıktan SONRA ölçüldü):**
* MD §10 tablosunun 15 satırı şu anki dosyalarla **birebir** (BAYAT=0) — tablo kendi kendini
  doğrulamıyor, dışarıdan ölçüldü.
* MD tablosu ↔ JSON `korunan_digestler`: **15 kesişim · 0 çelişki**.
* Defter: 29 kayıt **tekil** · `KAPI` durumundaki her kaydın kapısı **var** · K1…K17 **tam**.
* JSON **sabit nokta**: yaz → oku → yeniden yaz ⇒ digest **değişmedi** (kayıt donmuş).

### Karşılıklı digest çıpası — neden dosyaların İÇİNDE değil

MD ile JSON birbirinin digest'ini **içermez**: içerseydi her yazım diğerinin digest'ini değiştirirdi
(**döngü**). Bu yüzden ikisinin de TAM sha256'sı **ayrı kanalda**, bu notta, **iki dosya da
dondurulduktan sonra** hesaplandı:

| dosya | sha256 (TAM) |
|---|---|
| `data/eval/anka_r19_olcum_kabi_kapilari_2026-09-21.md` | `0a3091b8b5989ca8a14f77aa84e04bfe49f7d69e8660295970c00e09e93dcfb0` |
| `data/eval/anka_r19_olcum_kabi_kapilari_2026-09-21.json` | `be6669cb5dc38b3de0db6a6c699b5ff958315bfbea953fc159dcec67e20f37f9` |

> **Bu tablo BİR KEZ bayatladı ve ölçüm yakaladı.** İlk sürüm iki dosya donduktan sonra yazılmıştı;
> sonra kapanışın iki yönlü beyan denetimi **K30**'u (beyan edilmemiş `scripts/__pycache__/*.pyc`)
> buldu ⇒ rapor çifti **değişti** ⇒ tablo bayatladı. İki dosya **yeniden dondurulup digest'ler
> yeniden hesaplandı** ve tablo **son hâliyle** yazıldı. *Kapalı bir çıpayı, onu kapatan artefakt
> bayatlatır* — bu, T-0081'in dersinin bu görevdeki ikinci görünümüdür.

Doğrulama: bu iki digest yazıldıktan **sonra** yeniden hesaplandı ve **aynı** çıktı (not dosyasının
kendisi bu iki dosyaya yazmaz ⇒ çıpa zayıflatmaz).

---

## Korunan yüzey ve taban

**15/15 artefakt bit-ödeş** (önce `10:25:20Z` anlık görüntüsü → sonra). **pytest tabanı: 1 failed,
248 passed** (T-0097: 1 failed / 237 passed) ⇒ **+11 passed bu görevin testleri**, düşen tek test
`test_agent_gateway` sandbox soket yasağı — **aynı kaldı, regresyon yok.**

`CLAUDE.md` **DEĞİŞTİRİLMEDİ.** Sonucu beyan edilir: damgalı taban (`213 passed`) **zaten bayattı**
(T-0097 ölçümü: 237); bu görev sayıyı **248**'e taşıdı ⇒ damga **daha da bayatladı**. Çıpa belgesini
güncellemek **operatörün** kararıdır.

## Sırada

* **Sonraki tur — ilan sınıfı (K1/K2/K3/K8/K13):** donmuş ön-kaydın ölçüm sonrası onarımı T-0067'ye
  aykırı ⇒ çözüm **ileriye dönük**: ilan **dondurulmadan önce** iddialarını **üreticiden** hesaplayan
  bir **ilan kapısı**; K13 için **asgari etki büyüklüğü ilanı**; K8 için **tarifiyle yazılan digest**.
* **AÇIK kalanlar (beyan edildi, kapatılmadı):** K22/K29 `.pytest_cache` beyan sınıfı · K25 ölü
  `BIRLESIK_ARANAN` · K27 sabit `/7` paydası · T-0092'nin *"donmuş kapı mutlak yolda FAIL-OPEN"*
  kaydı (`frozen_guard.py` — **düzeltilmez**, beyan edilir).
* **Kapılar T-0097'nin 17 kusurunu geriye dönük ONARMAZ** — yalnız **tekrarını** önler.
* **Gerçek koşum başlatılmadı** ⇒ "canlı koşumda dur" kapısı **sahte fikstürle** sınandı (ilan edilen sınır).
* `scratch/` altındaki kapı araçları **taşınmadı** (T-0086 aracı yerinde) — taşıma ayrı karar.
