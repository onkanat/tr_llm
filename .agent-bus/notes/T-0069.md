# T-0069 — TÜM MODEL CHECKPOINT'LERİ SİLİNDİ (Anka doğuşu)

**Talimat (Onkanat, 18 Eyl 2026):** *"Antigravity ve claude-code Tüm Modelleri silin ve elinizdeki
data-set ve Proje dökümanları ile modeli yeniden aşama aşama eğitin. Data setleri model ihtiyacına
göre elden geçirin mimari temel yapısını koruyun."* + *"Yeni modelin adı Anka çünkü Kristalin
küllerinden doğuyor."*

**Onaylar (AskUserQuestion, 18 Eyl):** silme = kalıcı `rm` · kapsam = hepsi (temel dâhil) ·
donmuş veri yolu = güncelleme izni ver · ön-eğitim ölçeği = **~100 M jeton (~1 gün)** ·
yürütücü = **yalnız claude-code** (Antigravity GPU'ya yük bindirmemeli, T-0052).

## Ölçülen sonuç

- **51 model checkpoint'i · 20.41 GiB silindi**; atlanan 0, uyuşmazlık 0.
- Disk **26.1 → 46.2 GiB** (kazanç 20.10 GiB).
- **Veri kümesi olan 6 `.pt` korundu ve bit düzeyinde değişmedi** (6/6 doğrulandı).
- `data/` 18 GB → 691 MB.

## Kırılan bağımlılıklar

`data/kristal_model.pt` şu varsayılan yoldu ve artık yok: `chat_prompt.py:309`, `train.py:89`,
`train_dpo.py:89`, `scripts/retrain_clean_models.py`, `scripts/run_goal_pipeline.py`.
**Şu anda hiçbir model yok** — çıkarım/eğitim komutları çalışmaz.

## Yöntem (geri dönüşsüzlüğe karşı)

Manifesto (51 × tam sha256) → betik içi manifest bütünlük kapısı → **silmeden önce her hedefi yeniden
doğrula** → artımlı kayıt → **sonra korunan 6 dosyanın digest'ini yeniden ölç**. `.pt` git dışı
(`.gitignore: *.pt`), repo dışı yedek yok ⇒ bu manifesto tek kayıttır.

## Kusurlar / sınırlar (§)

1. **Yorum farkı:** "tüm modeller" *veri kümesi* `.pt`'lerini kapsamaz diye okudum; 6 dosya korundu.
   Düzeltilebilir bir yorumdur, rapora açıkça yazıldı.
2. **Bu görev ölçüm/kapı görevi DEĞİL** — hiçbir model kalitesi iddiası yok; silme bir sıfırlamadır.
3. **Raporun kendi sha256'sı** tabloda yok (kendine referans); üretim çıktısındaki değer geçerlidir.
4. **Bus'a ait `state/tasks|results` dosyaları** hash tablosuna konmadı — kapanış çağrısı onları
   kendisi günceller ([[kabul-kosusu-olctugu-artefakti-degistirir]]).

## Kapsam eşlemesi

`writes[]` = `data/eval/t0069_pre_delete_manifest.json`, `data/eval/t0069_model_deletion_2026-09-18.md`,
`.agent-bus/notes/T-0069.md`, `data/`, `data/_archive/`, `scratch/`. Tümü kiralandı.

## Sonraki adım (ayrı görevler)

Veri elden geçirme (Wikipedia kurtarma + kanonik 32.852 sözlük ile yeniden derleme) → A1 ön-eğitim
(~100 M jeton) → SFT → sohbet → ceket. Her aşama kendi ilan edilmiş kapısıyla kapanır.
İlgili: [[hf-onbelleginde-wikipedia-kurtarildi]], [[anka-yeni-model-adi]].
