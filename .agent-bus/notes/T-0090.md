# T-0090 · Onaysız küçük açıklar — yürütüm notu

**Damga (betik hesapladı):** `2026-09-20T11:21:01Z` · **Yürütücü:** claude · **Hüküm:** Y1 · Y1b · Y2 ·
Y3 · Y4 **BEŞİ DE KALDI**, ilan edilen üç çürütme maddesinin **hiçbiri ateşlenmedi (0/3)**.
**Rapor:** `data/eval/anka_r13_onaysiz_kucuk_aciklar_2026-09-20.md`
`5440d4ac06f45ba7b92e2153ff7be758191acf945c33e677482f6959eabcd956` · **`.json`** `data/eval/anka_r13_onaysiz_kucuk_aciklar_2026-09-20.json`
`9aa4ae52504c039cd3661b795e2b903fe772ff25198df3f391f6ed650e17a5f1`

> **Ölü artefakt ADLARI bu notta ANILMAZ.** Adlar T-0086 aracının kanonik manifestindedir.
> Gerekçe ölçülmüştür: adı **anmak** sayacı şişirir — sınıf T-0083, T-0088, T-0089'da üç kez,
> bu görevde **dördüncü kez** üredi (bu kez görev beyanında, §4/1).

---

## 1. Görev neydi

Operatör: *"Onay bekleyen işler için devam et"* ⇒ dört iş akışından **(3) Onaysız küçük
açıklar** + **(4)'ün README kısmı**. Kaynak: T-0088 kapanış raporu **§10/1, §10/3, §10/4,
§10/5** — özetten değil, **tam metinden** okundu.

| Kalem | Ölçülmüş kusur |
|---|---|
| §10/4 | `chat_prompt.py`'de **üç** yapılandırma dalı `print` + **çıplak `return`** ⇒ **rc=0** |
| §10/5 | `run_goal_pipeline.py` stage-1 `train.py`'yi **`--pretrain` olmadan** çağırıyor |
| §10/3 | `tests/test_prompt_contract.py` fixture'ı **BAYAT** sözlüğü (31.357) yüklüyor |
| §10/1 | README: arena komutları **ZORUNLU** bayrakları taşımıyor ⇒ **çalışmayan** komutlar |

## 2. Çekirdek ölçümler (görevin gerekçesi)

**(a) Üç dal — ölçülen taban `rc=0` ⇒ düzeltme sonrası `rc=2`**, mesaj **stderr**'de,
stdout'ta **yok**. Üçü de denendi; `main()` normal döndüğü için çağıran *"dosya yok"*u
*"başarıyla koştu"*dan **hiçbir sinyalden** ayırt edemiyordu.

**(b) stage-1, birebir bayrak kümesiyle iki dal:**

```
--pretrain YOK  -> rc=1 · RuntimeError @ train.py:275 "SFT maskelemesi ... HICBIR hedef birakmadi"
--pretrain VAR  -> rc=0 · ilk kayip 10,5279 · bitis 8,3653 · ln V referansi 10,4076
```

⇒ Boru hattı 1. aşamada **ölüydü**. Ama **sessiz değil**: T-0073 canlı kapısı ilk partide
duruyor. Kusur "sessiz sıfır-kayıp" değil, **"hiç koşamama"**dır.

**(c) Fixture:** 31.357 → güncel **33.114**. Test takımı ürünün gördüğünden **farklı**
tokenizer kuruyordu.

**(d) README:** komutlar bugün **çalışmıyordu**; 32.852 bir **TABAN**, külliyat sözlüğü
33.114 ve `--vocab` ile **açıkça** verilir.

## 3. Yapılan

* `chat_prompt.py`: `_durdur(mesaj) -> NoReturn` — `stderr` + `flush` + `sys.exit(2)`.
  **Beyan edilen genişleme:** dosyadaki iki mevcut `sys.exit(2)` sitesi de aynı yardımcıya
  taşındı ⇒ dosya içi **tek** durma davranışı (onların `rc`'si zaten 2'ydi; değişen yalnız
  **akış**, stdout → stderr). Beş site birebir aynı sözleşmeyi taşır.
* `run_goal_pipeline.py`: `stage1_cmd`'e `--pretrain` (`--from-scratch` **korundu**).
* `tests/test_prompt_contract.py`: fixture `GUNCEL_VOCAB` kullanır.
* `tests/test_chat_prompt_gates.py` (**YENİ**, 4 test): üç **duran** dal + **bir geçen** dal.
  Geçen dal zorunludur: yalnız *"duruyor mu"* bakan test, **her şeyi durduran** kapıyı da
  geçirirdi — **vakum kapı**.
* `README.md`: arena komutları + seçenek tablosu ZORUNLU bayraklarla; TABAN/külliyat ayrımı.

## 4. Kendi kusurlarım

1. **"Ölü adı anma sayacı şişirir" sınıfı DÖRDÜNCÜ kez üredi — bu kez görev beyanımda**
   (`.agent-bus/state/tasks/T-0090.json`). Ölçüm bunu **+2**'ye çevirdi: biri doğrudan (benim
   beyanım), biri **aracın öz-referansı** (§5/2). Ham toplam **2447 → 2449**; ama **gerçek
   ağaç** (aracın kendi kaydı çıkarılınca) **1511**. Kural biliniyordu; **biçim** yeniydi.
   Beyan, sayacı düzeltmek için **sonradan değiştirilmedi** — kaydı düzeltmek kaydı bozmak
   olurdu. **Her iki sayı da** raporlandı.
2. **Ölçmeden yanlış tarif ettim:** README kalemini *"2 miras ölü-atıf"* sandım. Kaynağı
   okuyunca miras olduğu (kalem **değil**) ve gerçek kalemin **arena komutları + `--vocab`
   iddiası** olduğu çıktı.
3. **Sandbox MPS'i gizledi:** ilk stage-1 ölçümüm `train.py:48` kapısında `rc=1` verdi.
   Neredeyse *"stage-1 zaten rc=1"* yazacaktım; oysa o **kabın** kusuruydu. İki ayrı
   `rc=1` ayırt edilmeseydi rapor **yanlış** olurdu. Ölçüm `--device cpu` kabında alındı;
   soruyu değiştirmez — hedef seçimi ve kapısı tensörden **önce, CPU'da** çalışır.
4. **Ölçüm aracının çıktısının üzerine yazdım** (T-0086 aracı) — hedefin **önceki**
   digest'ini almadan. Araç deterministik ve ağaç değişmedi ⇒ kayıp **yok**; "önce" hâli
   `$TMPDIR/t0090/denetim_once.json`. Ders: **aracı koşturmadan önce hedefin digest'ini al.**

## 5. Bu görevde ölçülen YENİ bulgular (kapsam dışı — düzeltilmedi)

**(1) `chat_prompt.py` REPL, stdin `EOF`'ta SONSUZ DÖNGÜ.** Gerçek checkpoint'li pozitif
koşumun çıktısı ölçüldü: **322.98 sn** pencerede **6,290,372**
istem döngüsü, **94,355,597 satır**, **37 benzersiz satır**,
**4,724,070,497 bayt** (~4,4 GiB) ⇒ **19,476.1 döngü/sn**. Süreç
hiç bitmiyor (elle durduruldu). Kanıt dosyası digest alındıktan **sonra silindi**
(`8aa741ec…`); kanıt artık raporun tablosunda. Boru hattını **diskle ve CPU'yla**
boğan bir asılmadır ve bu görev onu **kapatmadı**. Doğrudan sonucu: `chat_prompt.py`'nin uçtan
uca pozitif dalı **otomatik teste alınamaz** ⇒ testteki pozitif kap **ucuz** kaptır (iddia
sınırı raporda beyan edildi).

**(2) T-0086 aracı ÖZ-REFERANS — ilk kez ölçüldü.** Araç kendi çıktısını
(`data/eval/anka_r9_…json`) da **ağaç** sayar:

* Aracın artefaktı ham toplamın **934** adını taşır (**%38,1**) ⇒ aracın ilan ettiği
  `gerçek_olü_kanarya_haric: 2445` **kendi kaydını içerir**; KANARYA (4) **ve** aracın kendi
  kaydı (934) çıkarılınca **gerçek ağaç 1511**'dir.
* Ağaca **yeni** bir ölü-atıf dosyası girdiğinde araç **bir sonraki koşumda +1** yazar.
* **İdempotans ölçüldü:** ağaç **değişmeden üç ardışık koşum** → `2449 · 2449 · 2449` ⇒
  **sınırsız şişme YOK**, etki **tek seferlik**, sonra sabit. Araç **kaçak değil**, ama
  **sayacı kendi kaydıyla kirlidir**. Düzeltilmedi (doğru düzeltme: aracın kendi çıktısını
  `KAPSAM` dışı bırakması) — ölçüldü ve yazıldı.

## 6. Kapanış sinyalleri (üç sinyal)

**(a) Bildirim/kanıt:** bu not + rapor + JSON; kapıların ham çıktıları
`$TMPDIR/t0090/`.

**(b) Kiralama:** T-0090 altı kiralama aldı (`chat_prompt.py`, `scripts/`, `tests/`,
`README.md`, `data/eval/`, `.agent-bus/notes/`) ve kapanışta **boşaltıldı** ⇒ beyan edilen
9 yolun **tamamı** kira kapsamındaydı; kiralamasız yazma **0**.

**(c) `changed_files` — İKİ YÖNLÜ:**

| Yön | Sonuç |
|---|---|
| YÖN 1 — beyan edilen 9 yolun hepsi gerçekten değişti mi? | **TAM** (eksik 0) |
| YÖN 2 — kira köklerinde değişen her şey beyan edilmiş mi? | **TAM** (beyansız iş artefaktı 0) |

`changed_files` = **9** · `len(cf) == len(set(cf))` → **True**.

**YÖN 2'de çıkan 4 ek kalem SINIFLANDIRILDI (iş artefaktı değil):**

* `.agent-bus/state/tasks/T-0090.json` — **bus'ın kendi durumu** (görev kaydı); yürütücü
  yazmaz, bus yazar.
* 3 × `__pycache__/*.pyc` (`scripts/`, `tests/`) — **Python bayt-kodu önbelleği**;
  `.gitignore:2` (`__pycache__/`) ile **ignore'lu**, `git status`'ta **hiç görünmüyor**.

Yani `changed_files` sayısı **9**'dur, 13 değil; 13 yalnız *dosya sistemi dokunma* sayısıdır.

**(d) Diğer kanıtlar:**

* `venv/bin/pytest -q` → **1 failed, 227 passed** (taban **223**; sapma **TAM +4** = yeni test
  dosyası). Düşen tek test yine `test_agent_gateway.py` **sandbox soket yasağı**
  (`socketserver.py:478 PermissionError`) — ilan edilmiş taban davranışı.
* T-0086 denetimi **rc=0**; dokunulan canlı yüzeyde **0 yeni canlı ölü-atıf**; README
  **2 → 2**; BELGE kapsamı rapor yazıldıktan **sonra da 203** (rapor ölü ad eklemedi).
* **Donmuş yüzey:** `data/**` donmuş desenlerine **YAZMA YOK**; **hiçbir `.pt`/`.bin`
  üretilmedi** (stage-1 sondası `$TMPDIR`'e yazdı — 356 MB, depo dışı).
* **Eğitim koşusu YOK · push YOK · `git add -A` YOK**; commit yalnız **beyan edilen 9 yol**
  için ve bu görevin kendi niteliğindedir.

**İddia sınırı:** kapatılan sınıf **"sessizlik"**tir. Ölçülen şey **yapılandırma
kapılarıdır** — modelin kalitesi değil. Boru hattı **uçtan uca koşulmadı** ve REPL'in EOF
asırması **hâlâ açıktır**.
