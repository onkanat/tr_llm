# T-0095 — Yürütücü anlatı günlüğü

**Yürütücü:** claude (TEK) · **Aralık:** 2026-09-20T15:44Z → 2026-09-20T15:5xZ
**Soru (operatör):** *"Devam et, dört kolu değerlendir chat arayüzünün arka kapısı ile temel model
Anka ve Modül model Marangozu test et text çıktıları kullan çıktıları görmek istiyorum. gerekirse
yeni marangoz eğitelim."*

---

## 1. Arka kapı nerede — ölçülerek bulundu

`chat_prompt.py --gateway` → `scripts/run_agent_arena.py --server` →
`AgentGateway.create_http_server()` (src/gateway/agent_gateway.py:389) → `POST /api/query` →
`AgentGateway.ask()` → `EpistemicCuriosityAgent.process_query()` → yanıt gövdesinde
`response_text` (= `decompiled_text`).

Üretim uzunluğu **45 jeton** (`src/rag/epistemic_agent.py:311`). Bu, kapının **kendi**
davranışıdır; değiştirilmedi — ölçüm kapıyı olduğu gibi raporlar.

## 2. Kapı TAKLİT EDİLMEDİ

`scratch/t0095_arka_kapi.py` `ask()`'i doğrudan çağırmaz: gerçek bir `HTTPServer` ayağa kaldırır,
`urllib.request` ile **soketten** POST atar. Beş model × 6 soru = **30 sorgu, hepsi HTTP 200**.

## 3. Ölçüm ve ham çıktılar

| model | sha256 (kısa) | benzersiz yanıt | F1 ort. |
|---|---|---|---|
| TABAN `data/anka_a1r.pt` | `b93cc1cd5409…` | 6/6 | 0,0625 |
| R0 `scratch/t0094_sonda/arm_R0.pt` | `ba3136774661…` | 5/6 | 0,1652 |
| R4 `…/arm_R4.pt` | `f3bd5be6aa71…` | 6/6 | 0,2044 |
| R10 `…/arm_R10.pt` | `1163b918abd6…` | 5/6 | 0,1361 |
| R20 `…/arm_R20.pt` | `1d04365518cb…` | 5/6 | 0,1217 |

Cihaz **mps** (kayıtlı). Çıktılar `data/eval/anka_r17_arka_kapi_2026-09-20.md` §2'de ham hâlde.

Nitel gözlem (ölçülmüş, cümle değil): TABAN yer tutucu/ansiklopedik dağınıklık üretir
(`, [?] ve [Özel İsim] gibi`); dört kol **marangoz kalıbını** üretir
(`teknik çözüm: bu gereksinim için marangozlukta …`). Ama kollar altı sorunun çoğuna **aynı
cümleyi** verir ⇒ **biçim öğrenilmiş, içerik öğrenilmemiş** (0,16 epoch'ta beklenen).

## 4. K15 — BULGU: arka kapı SALT-OKUNUR DEĞİL

`process_query` içinde **tasarlanmış** bir yazma yolu var (`:339-366`):
`match_score >= 0.85 VE (entropy_post > tau VEYA çıktı "bilgi yok" der)` ⇒ kayıt
`data/future_train_vector.jsonl`'e **EKlenir**.

Bu koşumda **tek** yazım oldu: TABAN / M5 (`rag=1,000`, `entropy_post=4,3457`). Aynı M5
dört kolda da `rag=1,000` buldu ama `entropy_post` **0,4544–1,1132** aralığında kaldı ⇒ tau'yu
aşmadı ⇒ **yazmadı**. Yani yazma **modele bağlı**, sorguya değil.

* Dosya koşumdan önce **0 bayt** (`e3b0c442…`), sonra **1 kayıt** (2,2 KiB, `964a168d…`).
* Kayıt kanıt olarak `scratch/t0095_arka_kapi/KANIT_future_train_yazilan_kayit.jsonl`'e kopyalandı.
* **Geri alma YAPILMADI:** `data/**` altına elle yazım bu oturumda engellendi (kural 3 doğru
  çalışıyor). Karar operatöründedir.

**Yönetişim boşluğu (kapatılmadı, beyan edildi):** `data/future_train_vector.jsonl` `data/**`
altında ama **10 donmuş desenden hiçbirine uymuyor** ⇒ kural 3 dışında hiçbir **makine**
kapısı onu korumuyor. T-0094'ün 20 adet `.bin.meta.json` bulgusuyla **aynı sınıf**.

## 5. K16 — KENDİ KUSURUM: iddia kendi kanıtıyla çelişiyordu

Raporun §4 tablosu `src/**`/`scripts/**` için *"DEĞİŞMEDİ"* diyor, kanıt olarak da tek bir
`?? scripts/evaluate_carpenter_anka.py` satırını basıyordu. `??` = **izlenmeyen** dosya ⇒
"değişmedi" hükmü kanıtla çelişiyordu. Düzeltildi: `??` ile `M/A/D` **ayrı** ölçülür, hüküm
yalnız izlenen-değişene bağlanır (`izlenen_degisen=[]` ⇒ TEMIZ). Bu K11/K13/K14 ile **aynı
sınıfın dördüncüsü**: cümleyi artefakta bağlamadan yazmak.

## 6. Sınırlar (beyan)

* **`src/**` değiştirilmedi**; `max_new_tokens=45`, `tau`, `similarity_threshold=0.85` olduğu gibi.
* **Lexicon uyuşmazlığı (ölçüldü, düzeltilmedi):** `create_default` varsayılanı
  `data/lexicon/roots.tsv` (52.374); Anka'nınki `roots_anka_r1.tsv`. `run_agent_arena.py`
  lexicon geçmiyor ⇒ kapı Anka'ya **taban lexicon**'u ile hizmet etti. T-0093/U6 temsil
  ayrışmasının bir kaynağı bu olabilir; **bu koşumda izole edilmedi**.
* **RAG koşullaması pratikte devreye girmedi:** 6 sorudan yalnız M5 eşiği (0,85) geçti — o soru
  külliyat cümlesinin kendisi. Diğerleri 0,24–0,38.
* **F1 yeni bir ölçüttür, T-0094 ROUGE'u DEĞİL** (farklı n, 45 ↔ 128 jeton, farklı zarf).
  Ölçüt çıpası: ref↔ref 1,0000 · boş↔ref 0,0000 ⇒ kör değil. İki sayı **kıyaslanmaz**.
* **Kollar arasında sıralama iddiası YOK**; T-0094 zaten sıralanamadıklarını ölçmüştü (R²=0,0162).

## 7. "Yeni marangoz eğitilelim mi?" — ölçülmüş cevap

1. **"Modül model Marangoz" diye bir checkpoint YOK.** `data/anka_*.pt` altında yalnız
   `anka_a1.pt` ve `anka_a1r.pt` var (ikisi de TABAN). Marangoz kelimesini hak eden tek ağırlık
   **sonda kolları**; onlar 0,12–0,16 epoch'luk birer **sondaydı**.
2. Kollar arka kapıdan **boş değil**: dilbilgisel, alan içi Türkçe üretiyorlar ⇒ ceketin
   **biçimi** öğrenilmiş.
3. Ama **içerik yok**: aynı cümle farklı sorulara tekrar ediyor. Bu *"ceket öğrenilemez"* değil,
   *"0,16 epoch yetmez"*.
4. **Uzun koşum T-0094'ün kapısından geçmedi:** dört kol da A ekseninde (genel CE) eşiği aştı
   (+%24,5…+%35,8 > +%10). Replay oranı **seçilmeden** tam giydirme başlatılamaz; seçim için
   gereken sonda da kolları sıralayamadı.
5. Ölçüm üç seçeneği ayırıyor (rapor §6): **(a)** sıra sabit + oran değişken yeni sonda;
   **(b)** oran seçilmeden tam giydirme; **(c)** önce lexicon'u Anka'nınkiyle hizala.
   **Öneri üretilmedi** — karar operatöründedir.

## 8. Kapanış kanıtları

* `venv/bin/pytest -q` → **1 failed, 237 passed** (92,29 s). Düşen **tek** test ilan edilenin
  aynısı (`test_agent_gateway.py::…http_server_endpoints`, `PermissionError` = sandbox soket
  yasağı). Taban **değişmedi**.
* Donmuş yüzey taraması (T-0095 `claimed_at` sonrası, **zamansal** ölçüt): taranan 1.478 ·
  eşleşen 69 · sonrası yazım **0** ⇒ **TEMIZ**.
* `scratch/*.py` py_compile kanaryası: **163/163 OK**.
* Rapor ↔ JSON sayı denetimi: 48 kontrol, uyuşmazlık **0**.
* `data/qdrant_db/**` (6 dosya) **değişmedi**; `data/anka_a1r.pt` ve 4 kol **değişmedi**.
* **Cihaz kapısının İKİ dalı da sınandı:** sandbox içinde `--device mps` ⇒ **rc=2**
  (*"MPS YOK … sessiz CPU düşüşü engellendi"*); `--device cpu` açıkça verilince kapı **geçti**
  (ve sandbox soket yasağına takıldı — beklenen).

## 9. Kendi kusurum — betiğim ilk sürümde rc=0 ile geçiyordu

İlk sürüm, soket bağlanamayınca modeli `hata` alanına yazıp yine de **rc=0** dönüyordu ⇒
"başarı" görüntüsü. Düzeltildi: model düzeyi hata varsa **rc=2** ve *"ölçüm GEÇERSİZ"*.
(T-0089'un "sessiz `return` rc=0 başarı gibi görünür" sınıfı — kendi betiğimde tekrar ürettim.)

---

## Beyan edilen `writes[]` sapmaları

`scratch/t0095_rapor_uret.py` · `scratch/t0095_kapanis.py` · `scratch/t0095_arka_kapi/**` —
görev `writes[]`'inde **yok**; `scratch/` kirası altında yazıldı.

## Açık taşınanlar (kapatılmadı, beyan edildi)

* **K15:** `data/future_train_vector.jsonl` donmuş desen **dışında**; arka kapı oraya yazabiliyor.
* **§6/2:** gateway lexicon'u `roots.tsv` (Anka'nınki değil) — `run_agent_arena.py` geçmiyor.
* `src/llm/frozen_guard.py:59` mutlak yolda FAIL-OPEN (T-0092'den beri açık).
* `data/*.bin.meta.json` 20 dosya donmuş desenin dışında (T-0094'ten).
* `anka_a1r.pt` için `.opt.pt` yan dosyası YOK ⇒ `--load-optimizer` rc=2.
