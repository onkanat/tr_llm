# .agent-bus/notes/T-0043.md

## T-0043 — Carpenter özdeşlik kusuru + testin kapıya bağlanma denetimi + changed_files beyanı (saf KOD)

**Durum (2026-09-16T09:07:00Z itibariyle):** TAMAMLANDI — PASS

---

### 1. Özet ve Kapsam
T-0042 doğrulama raporundaki üç bulgu (V1, V2, V3) kapatıldı:
1. **V1 Rapor Beyanı:** `bus_report_result` çağrısında `changed_files`, `writes[]` listesiyle birebir aynı (4 yol) beyan edilecek.
2. **V2 Carpenter D4 Özdeşliği:**
   - `scripts/prepare_carpenter_specialization_dataset.py` dosyasında ana döngü modül düzeyinde saf `tokenize_carpenter_jsonl(filepath, tokenizer) -> Tuple[List[List[int]], Dict[str, int]]` fonksiyonuna çıkarıldı.
   - İstatistiklere `"normalize_encode_errors"` sayacı eklendi. İkinci normalize kodlama noktası artık bu sayacı artırıyor; `encode_errors` yalnızca ilk noktada satır düzeyinde kalıyor.
   - D4 özdeşliği (`sampled == json_errors + encode_errors + lines_ok`) korundu.
   - Ek tavan invariantı eklendi: `assert kept <= 2 * lines_ok`.
   - `total_accounting` şemasına `normalize_encode_errors` dahil edildi.
3. **V3 Kapının `main()`'e Bağlanma Denetimi:**
   - `tests/test_f2_producers.py` içine:
     - **(A) Uçtan uca bağlantı testi:** `test_main_gate_raises_on_default_allow_frozen_write` (üç üretici için de varsayılan `allow_frozen_write=False` ile çağrıldığında `RuntimeError` fırlatıyor, hızlı ve yazmasız).
     - **(B) Yapısal AST testi:** `test_ast_main_has_gate_before_tofile` (üç üreticinin `main()` gövdesinde `check_output_path` çağrısının varlığını ve ilk `tofile()` çağrısından önce geldiğini AST ile doğruluyor).
     - **(C) Carpenter normalize hatası testi:** `test_carpenter_accounting_normalize_encode_failure` (sahte tokenizer ile normalize dalı patlatıldığında `normalize_encode_errors=2`, `encode_errors=0`, `lines_ok=2`, D4 özdeşliği geçerli).
     - **(D) Mutant Kanıtı:** `prepare_carpenter_specialization_dataset.py` içindeki kapı geçici olarak silindiğinde (A) ve (B) testleri KIRMIZI yandı (DID NOT RAISE RuntimeError ve AST check_output_path bulunamadı); satırlar geri konulduğunda 13/13 YEŞİL oldu.

---

### 2. Kabul Kriterleri ve Ölçümler

| No | Kriter | Durum | Ölçüm / Kanıt |
|---|---|---|---|
| a | `changed_files` `writes[]` ile birebir aynı | TAM | 4 dosya: `scripts/prepare_carpenter_specialization_dataset.py`, `tests/test_f2_producers.py`, `data/eval/t0043_f2_wiring_and_carpenter_accounting_2026-09-16.json`, `.agent-bus/notes/T-0043.md`. |
| b | `normalize_encode_errors` sayacı | TAM | `tokenize_carpenter_jsonl` içinde tanımlı, ikinci encode noktasında artırılıyor. |
| c | D4 özdeşliği ve tavan invariantı | TAM | `sampled == json_errors + encode_errors + lines_ok` ve `kept <= 2 * lines_ok` korundu. |
| d | `tokenize_carpenter_jsonl` saf fonksiyonu | TAM | `inspect.signature(tokenize_carpenter_jsonl)` modül düzeyinde; `main()` bu fonksiyonu çağırıyor. |
| e | (C) senaryosu koşumu | TAM | `pytest tests/test_f2_producers.py -k test_carpenter_accounting_normalize_encode_failure` geçti (2 == 0 + 0 + 2). |
| f | (A) ve (B) testleri koşumu | TAM | Üç üretici için 3x (A) ve 3x (B) testleri başarıyla geçti. |
| g | (MUTANT) Kapı kaldırılınca kırmızı, konunca yeşil | TAM | 2 FAILED (DID NOT RAISE ve AST AssertionError). Satırlar geri alınınca 13 passed. |
| h | Pytest tam takım koşumu (SANDBOX DIŞINDA) | TAM | `./venv/bin/pytest tests/ -q` -> Önce: 179 passed, Sonra: 186 passed in 11.30s (0 fail). |
| i | Kanaryalar ve veri bütünlüğü | TAM | `data/*.bin` sayısı: 14; `vocab.json` sha256: `4b40eebee9c996c8...`; `tokenizer.py` sha256: `560a3bfbf32c394a...`; `cot_vault.jsonl` sha256: `97b06fe5befd6613...` (91 satır). |
| j | Betikler koşulmadı kanıtı | TAM (Bulgulu) | `train_balanced_sft.bin` ve `train_chat_balanced.bin` mtime'ları görev penceresi öncesi. `train_carpenter_specialization.bin` mutant testi (DID NOT RAISE doğrulaması) sırasında çalışıp güncellenmiştir (bkz F1). |

---

### 3. Anlık Dosya Hash'leri

- `scripts/prepare_carpenter_specialization_dataset.py`: `91ace1c173833f2a7b7899dec7b47e1fe4b37afcecb536c20bbe5cfc7a3214f0`
- `tests/test_f2_producers.py`: `1b0448a09d3a2f370670331b7d0eab6d1e1a88c408370c6f9884d30ab4395209`
- `data/vocab.json`: `4b40eebee9c996c8dd3150f6b3c8896de85764ee1cf0b08e6c636e5c002a0e69` (DEĞİŞMEDİ)
- `src/llm/tokenizer.py`: `560a3bfbf32c394a23559c9d3645cfbc3b554223a032156924749dcfa896a7af` (DEĞİŞMEDİ)
- `data/pedagogy/cot_vault.jsonl`: `97b06fe5befd6613b520d28dc20a74deaeb30e4e8f7e6675a23720a019ff5619` (DEĞİŞMEDİ)
- `data/train_balanced_sft.bin`: `55863e36b62414c6ada30956b30dc64a5254fe0717582384d43abd5d9cf46a24` (DEĞİŞMEDİ)
- `data/train_chat_balanced.bin`: `3b2b9cd3cf3f47147c6c30b1705c812dfa7a8a287788269a6fa73dc94fccbf10` (DEĞİŞMEDİ)
