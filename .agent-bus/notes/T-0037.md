# .agent-bus/notes/T-0037.md

## T-0037 — Y2 İcraatı (SECENEK_A): Kesme İşaretli Formların Taban Tokenizer'da Çözümü

**Durum (2026-09-15T20:42:00Z itibariyle):** TAMAMLANDI — PASS_WITH_FINDINGS

---

### Özet

T-0035'in ölçtüğü rakam+kesme kaybını gidermek için SECENEK_A uygulandı:
- Kesme işareti (`'`, U+0027) taban sözlüğüne tek başına token olarak eklendi (id 32850)
- `literal_entity_mode=True` iken `1973'te` → `[1, 9, 7, 3, ', <ek>]` (UNK yok)
- `İstanbul'da` → `<ENT><CAP>i s t a n b u l</ENT> ' CASE_LOC` (UNK yok)
- `literal_entity_mode=False` yolu bit-özdeş korundu (G3 değişmezlik kapısı)
- N2 = 0 (15 örnekli corpus, mode=True, yeni vocab)

---

### Yapılan Değişiklikler

**`src/llm/tokenizer.py`** (sha256: `560a3bfbf32c394a23559c9d3645cfbc3b554223a032156924749dcfa896a7af`):
1. `_TR_LOWER` sınıf değişkeni + `_turkish_lower()` yöntemi eklendi — İ (U+0130) → i (U+0069)
2. Kesme dalı (literal_entity_mode=True): `stem.isdigit()` → rakam tokenleri
3. Kesme tokeni (apostrophe_id) yayımı
4. Ek çözülemezse verbose bildirim
5. U+2019 → has_apostrophe=True tanıma

**`data/rebuild/vocab_base_32852.json`** (sha256: `7611b6a523a2bb524cc21aa9cc64235aa6a2ae99f87f46ac51cb2915e61164ad`):
- len(stoi)=32852, `'` id=32850
- Üretim komutu: `./venv/bin/python scratch/t0037_kesme_probe.py`

**`tests/test_apostrophe_tokenization.py`** — 15 yeni test (G0-G5 kapsar)
**`tests/test_two_layer_tokenizer.py`** — `test_entity_decompile_roundtrip` G2 davranış değişikliğine güncellendi

---

### Ölçüm Sonuçları

| Metrik | Değer | Komut |
|---|---|---|
| N0 (eski, False) | 1 | `./venv/bin/python -c "count_unk(tok_old_f, corpus)"` |
| N1 (yeni, False) | 1 | `./venv/bin/python -c "count_unk(tok_new_f, corpus)"` |
| N2 (yeni, True) | 0 | `./venv/bin/python -c "count_unk(tok_new_t, corpus)"` |
| G3 değişmezlik | 0 fark | `./venv/bin/python -c "sum(1 for t in corpus if tok_old_f.encode(t) != tok_new_f.encode(t))"` |
| pytest | 159 passed | `./venv/bin/pytest -q tests/` |
| cot_vault sha | `97b06fe5...` 91 satır | `shasum -a 256 data/pedagogy/cot_vault.jsonl` |

---

### Bulgu F1 (ORTA)

**Tipografik kesme U+2019 regex seviyesinde bölünüyor:**
`re.findall` U+2019 (Pf kategorisi) ile `\w` eşleşmediğinden `'1973\u2019te'` → `['1973', 'te']`. `has_apostrophe` dalı bu durumda tetiklenmiyor. N2=0 hesabını etkilemiyor (rakam + 'te' ayrı işleniyor, <UNK> yok). Çözüm: `_normalize_text` veya regex deseninde U+2019→U+0027 ön geçişi — F2 veya ayrı görev.

---

### Kapsam Dışı (Bilinen Ara Durum)

- F2 (.bin yeniden derleme) ve F3 (yetkinlik tamamlama eğitimi) bu görevin kapsamı dışındadır.
- Mevcut `data/*.bin` dosyaları yeni sözlükle (32852) uyumsuz kalmaktadır.
- Hiçbir .pt/.bin değişmedi; eğitim koşulmadı.

---

### narrative_log

`{"path": ".agent-bus/notes/T-0037.md", "sha256": "<rapor anında tazelenir>", "ozet": "G0-G5 tüm ölçümler tamamlandı. N2=0, 159 passed, değişmezlik korundu. F1: U+2019 regex bölünmesi."}`
