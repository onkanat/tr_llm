# T-0081 · Doküman borcu onarımı — yürütüm notu

**Kapsam:** yalnız `CLAUDE.md` + `.agent-bus/SPEC.md`. Kod, kapı (G1…G6, K1…K8, L1)
ve artefakt DEĞİŞMEZ. Tek geri dönüşsüz nokta (`data/anka_a1r.pt`) bu işe girmez.
Plan: `~/.claude/plans/dokuman-borcu-2026-09-19.md`.

## CLAUDE.md — TAMAMLANDI (D1…D5)

| # | Yapılan | Doğrulama (ölçüm) |
|---|---|---|
| **D1** | Eğitim komutuna `--pretrain` + `--vocab` + `--allow-frozen-write` + `--loss-report` eklendi; `--pretrain`'in sessiz `0,0000` tuzağı ve **moda bağlı** canlılık imzası uyarı bloğuna yazıldı | `grep -c pretrain` = **2** (öncesi **0**) |
| **D2** | "195 passed" → **damgalı** ölçüm: `213 passed, 1 failed`, düşen testin adı + nedeni (sandbox `PermissionError`) | `venv/bin/pytest -q` → `1 failed, 213 passed in 26.18s` |
| **D3** | Bkz. **§D3 düzeltmesi** — planımdaki iddia YANLIŞTI | `grep -c 33114` = 1 |
| **D4** | Ölü `kristal_model.pt` referansı → Kristal zinciri silindi / model Anka | `ls data/kristal_model.pt` → **No such file** · `grep -c 32816` = **0** |
| **D5** | Geniş `data/vocab…json` kalıbı → `frozen.json`'un **10 deseni birebir** | üç yönlü **BIREBIR** (aşağıda) |

### D3 düzeltmesi — planım yanlış yazmıştı (kayda geçer)

Planın D3 satırı *"eğitim hattı artık `vocab_anka_r1_33114.json` yükler"* diyordu.
Ölçüm bunu **çürüttü**: `train.py:66` varsayılanı **hâlâ `vocab_base_32852.json`
(32.852)** ve `33114` `train.py`'de **hiç geçmiyor** (`grep -c` = 0). 33.114 yalnız
`--vocab` ile **açıkça** veriliyor (koşumun gerçek komutu: `scratch/anka_r5_kos.py:30,145`
+ log satır 6 `Kelime dağarcığı boyutu: 33114`).

İki sayı **çelişmiyordu**; **kümeler farklıydı**: *varsayılan* vs *`--vocab` ile verilen*.
Belgeye bu ayrım yazıldı. **Belgenin eski cümlesi bu yönüyle yanlış değildi** — eksikti:
`CLAUDE.md`'de `--vocab` **hiç geçmiyordu** (`grep -c` = 0) ⇒ belgelenen komut güncel
külliyatı **yeniden üretemiyordu** (D1 ile aynı sınıf: kopyalanabilir komut eksiği).

**D3 neden D1 kadar tehlikeli DEĞİL:** `train.py:112-116` sözlük küçükse `RuntimeError`
ile **yüksek sesle durur**; sessiz değildir. Sessiz olan tek yol D1'dir.

## CLAUDE.md doğrulaması — D5/D7 iki yönlü küme karşılaştırması

Üç kaynak: **belge** (kural 2 parentezindeki ilan edilen küme) · **`frozen.json`** ·
**`bus_frozen_list` aracının çıktısı**. Üçü de **10 desen**, üç yön de `BIREBIR`
(`yalniz_solda=[] yalniz_sagda=[]`). Pozitif kontrol: kümeler boş olamaz (boş küme
vakum geçişi verir). Negatif kontrol: `data/uydurma/**` eklenince `AYRISMA` ✓.

### Ölçüm aracımın kendi kusuru (ilk koşumda yakalandı)

İlk sürüm belgedeki **tüm** `` `data/…` `` jetonlarını desen sayıyordu; bu yüzden
*"kaldırılan geniş `data/vocab*.json` kalıbı"* açıklama cümlesi **sahte AYRISMA**
üretti (11 vs 10). Araç, **ilan edilen desen** ile **emekliye ayrılmış deseni**
ayırt edemiyordu. Düzeltme **iki taraflı**: (a) belgede emekli kalıp `data/vocab…json`
biçiminde yazıldı (ilan edilen listeden görsel olarak ayrık), (b) araç artık kümeyi
**yalnız "10 desen" ifadesinden sonraki parentezden** çıkarıyor **ve** parentez dışı
adayları **raporluyor** (sessizce atmıyor) ⇒ `parentez disi aday : yok`.

### Doğrulama kriterimin kendisi de yanlış ilan edilmişti

Planda D3/D4 kriteri `grep -c "32816|kristal_model" == 0` idi. **D4'ün düzeltmesi
ölü dosyayı ADIYLA anmak zorunda** ("`kristal_model.pt` artık yoktur") ⇒ kriter
kendi düzeltmemle çelişir. Doğru kriter yazıldı: `32816` = **0**; `kristal_model`
geçen **tek** satır, ölü olduğu **açıkça etiketlenmiş** satırdır. Kusur tasarımda
değil **kriterdeydi**; sessizce değiştirilmedi, buraya yazıldı.

## SPEC.md — TAMAMLANDI (D6…D9)

Oku → yaz → **TEKRAR oku** sırası uygulandı; `bus_send` ile `antigravity`'ye bildirildi
(`state/inbox/antigravity/2026-09-19T14-54-30Z-claude.json`).

| # | Yapılan | Doğrulama (ölçüm) |
|---|---|---|
| **D6** | `Durum: tasarım (uygulama bekliyor)` → **uygulandı ve çalışıyor** + son düzenleme damgası | `grep -c "uygulama bekliyor"` = **0** (öncesi 1) |
| **D7** | "Varsayılan donmuş küme" 5 desen → **10 desen**, tek yetkili kaynak `frozen.json` olarak ilan edildi | `CLAUDE.md` ile **aynı** üç yönlü BIREBIR testi geçti |
| **D8** | "`notes/` sürümlenir" → **niyet/ölçüm ayrımı** yazıldı: diskte 44, izlenen **27**, izlenmeyen **17**; sürümleme **T-0061'de durmuş** | `git status --short .agent-bus/notes/` → 17×`??` · `git ls-files` → 27 |
| **D9** | "Bilinen açık kusurlar (yalnız ÖLÇÜLMÜŞ)" bölümü eklendi | `events.jsonl` → **1.174 olay / 6 tip**; `task_updated` kaynakta **0**; `read` hiç `true` yazılmıyor |

### D9'da ölçüm, hafızamdaki İKİ kusuru çürüttü

- **"Okuma `state/` ↔ yazma kök asimetrisi kusurdur"** → **yanlış**. Ölçüm: yazma
  `state/tasks/`'e (satır 338), okuma **iki** dizini tarar (satır 313/349). Üstelik bu
  tasarım SPEC'in **kendi "Kök dizin" bölümünde belgeli**. Kusur olarak **yazılmadı**;
  gerçek artık yazıldı: yalnız kökte duran bir şartname **okunur ama durumu hiç
  güncellenmez** (bayat `status`).
- **"Ayna diff'i kusurdur"** → **yanlış**; aynalı görevlerde beklenen fark. Yazılmadı.
- İkisi de D9'un altına "**Çürütülen iddialar**" başlığıyla geçti ki bir sonraki pencere
  bunları yeniden "kusur" diye eklemesin.

## Bu turda KENDİ ürettiğim kusurlar (kayda geçer)

| # | Kusur | Nasıl yakalandı | Düzeltme |
|---|---|---|---|
| 1 | **`## Uygulama gereksinimleri` başlığını sildim** — D9 bölümünü eklerken `old_string` olarak o başlığı verdim ve yeni metne **geri koymadım** | **TEKRAR okuma** (başlık listesi + satır 240-242'de üç boş satır) | Başlık geri eklendi; 17 başlık doğrulandı |
| 2 | `CLAUDE.md`'ye **`[[anka-yeni-model-adi]]`** yazdım — bu sözdizimi **hafıza dosyalarına** ait, repo belgesinde sarkık bağ | CLAUDE.md'yi yeniden okurken | Bağ kaldırıldı, düz metin yapıldı |
| 3 | `*sandırırdı*` / `*durmasi*` — bozuk Türkçe | yeniden okuma | `*sayardı*` / `*durması*` |
| 4 | **Ölçüm aracım ilan edilen desen ile emekli deseni ayırt edemedi** → sahte AYRISMA (11 vs 10) | ilk doğrulama koşumu **rc=2** | Araç kapsamı parentezle sınırlandı + parentez dışı adaylar **raporlanıyor**; belgede emekli kalıp `data/vocab…json` yazıldı |
| 5 | **Doğrulama kriterimi yanlış ilan etmiştim** (`grep -c "kristal_model" == 0`) — ama D4'ün düzeltmesi ölü dosyayı **adıyla anmak zorunda** | doğrulama koşumunda görüldü | Kriter, kusur **tasarımda değil kritrede** olduğu belirtilerek düzeltildi |
| 6 | **Planımın D3 maddesi yanlıştı** (bkz. yukarıda §D3 düzeltmesi) | `train.py:66` + `grep -c 33114` = 0 | Belgeye doğru ayrım yazıldı (varsayılan vs `--vocab`) |

**Ortak ders:** altısının **hiçbiri** sayı hatası değildi; hepsi **kapsam/yapı** hatasıydı
(silinen başlık, sarkık sözdizimi, fazla-geniş araç kapsamı, yanlış ilan edilmiş kriter).
Sayılar doğruyken çerçeve yanlış olabiliyor — [[denetim-kapsami-iddiadan-dar]] ailesi.

## D10 — git provenance: ölçüm beklediğimden KESKİN çıktı

**Donmuş 10 desenin 6'sında tek bir sürümlenmiş dosya yok.** Ölçüm (`git ls-files <desen>`):

| desen | izlenen dosya | not |
|---|---|---|
| `src/compiler/**` | **6** | ✓ |
| `data/lexicon/**` | **2** | ✓ (`roots.tsv` izleniyor) |
| `data/vocab.json` | **1** | ✓ |
| `src/llm/tokenizer.py` | **1** | ✓ |
| `data/*.pt` | 0 | `.gitignore` — **kasıtlı ve doğru** (yüz MB'lık ikili) |
| `data/*.bin` | 0 | `.gitignore` — **kasıtlı ve doğru** |
| `data/vocab_entity.json` | **0** | ✗ **izlenmiyor ve gitignore'da DEĞİL** |
| `data/realistic_rag/**` | **0** | ✗ izlenmiyor (4,1 M) |
| `data/b1_5_splits/**` | **0** | ✗ izlenmiyor (**139 M**) |
| `data/pedagogy_canonical/**` | **0** | ✗ izlenmiyor (7,3 M) |

**En keskin madde — `data/vocab_entity.json`:** bus bu dosyayı **yazmaya karşı bloklar**,
ama **sürümlenmiş tek bir kopyası yoktur**; yani "koruma" yalnız *çalışma zamanında*
vardır, **geri dönüş yolu yoktur**. `.gitignore`'da **değildir** (`git check-ignore` boş) —
yani onu saklayan bir kural yok, **ihmal** var. Tek eşi
`data/rebuild/vocab_base_32852.json` ile **bit-özdeş** (741.650 B,
`7611b6a523a2bb524cc21aa9cc64235aa6a2ae99f87f46ac51cb2915e61164ad`) — **ama o da
izlenmiyor**, dolayısıyla "ikiz" **gerçek bir kurtarma yolu değil**; ikisi birlikte
kaybolur. (Planım "onu yeniden üreten betik yok" demişti; ölçüm bunu **doğruladı ve
ağırlaştırdı**.)

**D8 ile aynı kök:** dosyanın diskte *durması* sürümlendiğini göstermiyor. Sürümleme
**T-0061'de durmuş**; aynı ihmal donmuş yüzeyin 4 deseninde de var.

### Önerilen `git add` listesi — **YOL-YOL, operatör onayı bekler**

`git add -A` / `git add .` **YASAK**. Aşağıdaki komutlar **çalıştırılmadı**:

```bash
# 1) Bu görevin kendi belgeleri (küçük, açıkça T-0081 kapsamı)
git add CLAUDE.md .agent-bus/SPEC.md .agent-bus/notes/T-0081.md

# 2) D8: sürümlemesi T-0061'de durmuş notlar (17 dosya)
git add .agent-bus/notes/T-0062.md .agent-bus/notes/T-0063.md .agent-bus/notes/T-0064.md \
        .agent-bus/notes/T-0065.md .agent-bus/notes/T-0066.md .agent-bus/notes/T-0067.md \
        .agent-bus/notes/T-0068.md .agent-bus/notes/T-0069.md .agent-bus/notes/T-0070.md \
        .agent-bus/notes/T-0071.md .agent-bus/notes/T-0072.md .agent-bus/notes/T-0073.md \
        .agent-bus/notes/T-0074.md .agent-bus/notes/T-0075.md .agent-bus/notes/T-0076.md \
        .agent-bus/notes/T-0077.md

# 3) D10: DONMUŞ ama sürümlenmemiş KÜÇÜK dosyalar (741 KB x2, en kritik madde)
git add data/vocab_entity.json data/rebuild/vocab_base_32852.json

# 4) Değişmiş izlenen kod + ölçüm belgesi (zaten izleniyor)
git add src/compiler/core.py src/compiler/lexicon.py src/compiler/morphotactics.py \
        src/compiler/phonology.py tests/test_morphology_regression.py train.py \
        data/eval/forgetting_measurement_design_2026-09-17.md
```

**Bilinçli olarak LİSTEYE KONMAYANLAR** (operatör kararı ister, sessizce eklenmez):
`data/b1_5_splits/**` (**139 M** — LFS veya karar gerekir) · `data/realistic_rag/**` ·
`data/pedagogy_canonical/**` · `data/eval/` altındaki **89** rapor (izin verilen yüzey
ama hacim kararı operatörün) · `data/*.pt` ve `data/*.bin` (**doğru biçimde ignore'lu**,
açılmamalı).



## Kapanışta yapılacak

1. D10 `git add` listesi **hazırlandı** (yukarıda); **operatör onayı bekliyor** —
   hiçbir `git add` çalıştırılmadı.
2. Kapanış: mesaj + **kiralar boş** + `changed_files` **iki yönlü**,
   `len(cf) == len(set(cf))`.
3. Faz 5 koşumu bitince `--kapanis` + dağılım + tam digest + `PushNotification`.

**Not (kendi kusurum, İKİ KEZ):** `old_string` olarak bir **başlık** verip onu yeni metne
geri koymamak başlığı **siliyor**. Bu turda iki kez oldu: SPEC.md'de
`## Uygulama gereksinimleri`, burada `## Kapanışta yapılacak`. Kural: başlığı
`old_string` yapıyorsan `new_string`'e **birebir geri koy**, ya da başlığı hiç hedefleme.
İkisi de **TEKRAR okuma** ile yakalandı — bu yüzden o adım atlanamaz.

