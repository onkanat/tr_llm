# .agent-bus/notes/T-0042.md

## T-0042 — F2 üreticileri: sözlük+mod parametresi, donmuş çıktı kaçış kapısı, muhasebeli tokenizasyon (saf KOD)

**Durum (2026-09-16T08:22:00Z itibariyle):** TAMAMLANDI — PASS

---

### 1. Özet ve Hedef
Üç veri hazırlama betiğinde (`prepare_chat_balanced_dataset.py`, `prepare_carpenter_specialization_dataset.py`, `prepare_balanced_sft_dataset.py`) saf kod standartlaştırması yapıldı:
- **D1:** Sözlük ve mod (`vocab_path: str = "data/rebuild/vocab_base_32852.json"`, `literal_entity_mode: bool = True`, `allow_frozen_write: bool = False`) parametrik hale getirildi.
- **D2:** Saf `check_output_path(output_bin: str, allow_frozen_write: bool = False)` fonksiyonu eklendi; `src.llm.frozen_guard.is_frozen_path` tek otoritesi kullanıldı.
- **D3:** Muhasebeli `tokenize_jsonl(filepath, tokenizer, max_samples=None, min_tokens=2) -> tuple[list, dict]` eklendi (8 sayaç: `missing`, `raw_lines`, `sampled`, `json_errors`, `encode_errors`, `lines_ok`, `short_skipped`, `kept`). İlk 5 hata stderr'e loglanıyor, sessizce yutulan `except Exception` yok.
- **D4:** Özdeşlik doğrulaması: `assert st["sampled"] == st["json_errors"] + st["encode_errors"] + st["lines_ok"]`.
- **D5:** Meta kütüklerinde zorunlu alanlar (`total_records`, `total_tokens`, `vocab_path`, `vocab_size`, `literal_entity_mode`, `record_accounting`, `source_stats`) standartlaştırıldı.
- **KOD KOŞULMADI:** Hiçbir betik `main()` fonksiyonu üzerinden çalıştırılmadı; kanarya dosyaları ve donmuş `.bin` / `.meta.json` kütükleri mutlak surette korundu.

---

### 2. Kabul Kriterleri ve Ölçümler

| No | Kriter | Durum | Ölçüm / Kanıt |
|---|---|---|---|
| a | `main` imzaları ve `KristalTokenizer` kurulumu | TAM | Her 3 betikte `(vocab_path: str = ..., literal_entity_mode: bool = True, allow_frozen_write: bool = False) -> None`. `tokenizer = KristalTokenizer(compiler, vocab, literal_entity_mode=literal_entity_mode)`. |
| b | `is_frozen_path` importu ve gömülü desen olmaması | TAM | Her 3 betikte `from src.llm.frozen_guard import is_frozen_path` mevcut; kod gövdesinde `data/*.bin` gibi gömülü desen listesi bulunmuyor. |
| c | `check_output_path` davranışı | TAM | Donmuş hedef + `allow_frozen_write=False` -> `RuntimeError("Donmus yola yazma engellendi: ...")`. Donmuş hedef + `True` -> geçiyor. Donmamış hedef (`scratch/probe.bin`) + `False` -> geçiyor. |
| d | `tokenize_jsonl` muhasebe ve D4 özdeşliği | TAM | 4 satırlık test kütüğünde: `raw_lines=4, sampled=4, json_errors=1, encode_errors=0, lines_ok=3, short_skipped=1, kept=2`. D4: `4 == 1 + 0 + 3` özdeşliği tam sağlandı. |
| e | İstisna yönetimi ve loglama | TAM | `except Exception` bloklarında `sys.stderr` loglaması var; çıplak `pass` veya `continue` yok. Hata loglama tavanı (5) uygulandı. |
| f | Pytest test takımı ve yeni testler | TAM | `tests/test_f2_producers.py` 6/6 test geçti (`0.08s`). Tam test takımı: Önce 173 passed -> Sonra 179 passed (`9.80s`, 0 hata/atlama). |
| g | Kanaryalar ve veri bütünlüğü | TAM | `data/vocab.json` sha256: `4b40eebee9c996c8...`, `cot_vault.jsonl` sha256: `97b06fe5befd6613...` (91 satır), `tokenizer.py` sha256: `560a3bfbf32c394a...`, `data/*.bin` sayısı: 14. |
| h | Betikler koşulmadı kanıtı (mtime / sha256) | TAM | `train_balanced_sft.bin` (mtime: 1789286687.80, sha: `55863e36b6...`), `train_carpenter_specialization.bin` (mtime: 1789283566.34, sha: `6d5a1d2c3b...`), `train_chat_balanced.bin` (mtime: 1789286774.93, sha: `3b2b9cd3cf...`) ve `.meta.json` mtime/sha256'ları birebir korundu. |
| i | Rapor ve bus bildirimi | TAM | `data/eval/t0042_f2_producer_accounting_2026-09-16.json`, `.agent-bus/notes/T-0042.md` oluşturuldu; `bus_report_result`, `bus_send` ve kiralama iadesi tamamlandı. |

---

### 3. V1 Bulgusu: fnmatch ve data/*.bin Deseni
- `.agent-bus/frozen.json` içindeki `"data/*.bin"` deseni standart Python `fnmatch.fnmatch` fonksiyonu ile karşılaştırıldığında, `/` karakteri ayrıcı olarak ele alınmadığı için `data/eval/test.bin` yolu da donmuş olarak tespit edilmektedir.
- Bu nedenle pozitif kontrol testinde donmamış yol olarak `scratch/` veya `/tmp` dizinleri kullanılmıştır.

---

### 4. Dosya Hash'leri (Raporlama Anı)

- `scripts/prepare_balanced_sft_dataset.py`: `b591b2a39974cca1074561f176db23f089ed50e85332f3b4721a569695a80f0a`
- `scripts/prepare_carpenter_specialization_dataset.py`: `8caf0fb9478eb3229a06a5931fb09e25911863f08ac2e893808f029e06aed4f1`
- `scripts/prepare_chat_balanced_dataset.py`: `466cd2501e7413ca1e1f3d0fb9f5f923bc2ca4f9cbf1b9b88a90a2cb68fce7fc`
- `tests/test_f2_producers.py`: `e9738545ef4424053e9af15e608fd0f7572f839f8146f5acbbeb413bdd8acb44`
- `data/vocab.json`: `4b40eebee9c996c8dd3150f6b3c8896de85764ee1cf0b08e6c636e5c002a0e69` (DEĞİŞMEDİ)
- `data/pedagogy/cot_vault.jsonl`: `97b06fe5befd6613b520d28dc20a74deaeb30e4e8f7e6675a23720a019ff5619` (DEĞİŞMEDİ)
- `src/llm/tokenizer.py`: `560a3bfbf32c394a23559c9d3645cfbc3b554223a032156924749dcfa896a7af` (DEĞİŞMEDİ)
- `data/train_balanced_sft.bin`: `55863e36b62414c6ada30956b30dc64a5254fe0717582384d43abd5d9cf46a24` (DEĞİŞMEDİ)
- `data/train_carpenter_specialization.bin`: `6d5a1d2c3b55ab883a4b9420485f908a8f773a16bade4c501a0c1c9fbf2b1121` (DEĞİŞMEDİ)
- `data/train_chat_balanced.bin`: `3b2b9cd3cf3f47147c6c30b1705c812dfa7a8a287788269a6fa73dc94fccbf10` (DEĞİŞMEDİ)
