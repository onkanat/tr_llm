# T-0047 Görev Kapanış Notu (F2-4: Pipeline'ın Donmuş Checkpoint Yazımı İçin Operatör Kapısı)

- **Görev ID:** `T-0047`
- **Tarih:** `2026-09-16`
- **Yürütücü:** `antigravity`
- **Durum:** `PASS`
- **V5 Durumu:** `KAPANDI`

---

## 1. Değiştirilen Dosyalar (`writes` & `changed_files`)
Birebir eşitlik sağlanmıştır (5 dosya):
1. `scripts/run_goal_pipeline.py`
2. `train_dpo.py`
3. `tests/test_compiler_entrypoints.py`
4. `data/eval/t0047_frozen_write_gate_2026-09-16.json`
5. `.agent-bus/notes/T-0047.md`

---

## 2. Kapı ve Doğrulama Özetleri

### KAPI a: `check_frozen_save_path` Üç Durumlu Fonksiyon Testi (KOŞUM)
- `tests/test_compiler_entrypoints.py::test_check_frozen_save_path_gate`
- Donmuş yollar (`data/kristal_model.pt`, `data/kristal_carpenter_model.pt`) ile `allow_frozen_write=False`: `RuntimeError` fırlatır (`PASS`).
- Donmuş yollar ile `allow_frozen_write=True`: Hata fırlatmaz (`PASS`).
- Donmamış yollar (`scratch/test_model.pt`, `tests/fixtures/test_model.pt`) ile `allow_frozen_write=False`: Hata fırlatmaz (`PASS`).
- Hem `run_goal_pipeline` hem `train_dpo` fonksiyonları ayrı ayrı doğrulanmıştır.

### KAPI b: AST Sıralama Kanıtı (STATİK)
- `tests/test_compiler_entrypoints.py::test_ast_run_goal_pipeline_has_frozen_guards`
- `scripts/run_goal_pipeline.py` `main()` fonksiyonundaki tüm eğitim komutları (Stage-1 Pretraining, Stage-2 SFT, Stage-3 Chat SFT, Stage-4 DPO, Aşama-4 Marangozluk Retraining) öncesinde `check_frozen_save_path` çağrısı olduğu AST ile denetlenmiştir.
- Guard sayısı: 5. Her `run_cmd` öncesinde ilgili `check_frozen_save_path` yer almakta ve satır numarası `run_cmd` satır numarasından küçüktür.

### KAPI c: Operatör Açık Onayı Gereksinimi (STATİK)
- `scripts/run_goal_pipeline.py` CLI'ına `--allow-frozen-write` (default=False) eklenmiştir.
- `train_dpo.py` CLI'ına `--allow-frozen-write` (default=False) ve `--save-path` desteği eklenmiştir.
- Operatör `--allow-frozen-write` vermedikçe koruma tetiklenir ve alt süreç / kayıt başlatılmadan boru hattı güvenle durdurulur.

### KAPI d: Kapsam Dışı Siteler Ölçüm Tablosu (STATİK)
| Dosya | Satırlar | Rol / Fonksiyon | Hedef | Pipeline Bağlantısı | Karar / Not |
|---|---|---|---|---|---|
| `scripts/retrain_clean_models.py` | 72, 85, 99 | Manuel yeniden eğitim betiği | `data/kristal_model.pt` | `run_goal_pipeline.py` tarafından **çağrılmaz**. | Değiştirilmedi (kapsam dışı). |
| `scripts/merge_teacher_dataset.py` | 262 | Öğretmen veri entegrasyon aracı | `data/kristal_model.pt` (train.py default) | `run_goal_pipeline.py` tarafından **çağrılmaz**. | Değiştirilmedi (kapsam dışı). |

---

## 3. Mutant Kanıtı (STATİK)
- `scripts/run_goal_pipeline.py` Stage-1 öncesindeki guard çağrısı geçici olarak yorum satırı yapıldı.
- AST testi çalıştırıldı ve `AssertionError: Beklenen 5 check_frozen_save_path çağrısı, bulunan: 4` vererek **KIRMIZI (FAIL)** oldu.
- Koruma satırı geri konuldu ve AST testi tekrar çalıştırıldı; **YEŞİL (PASS)** oldu.

---

## 4. Koşum Yok Kanıtı & Kanaryalar
- `scripts/run_goal_pipeline.py` ve `train_dpo.py` dosyalarının `main()` gövdesi ASLA çalıştırılmamıştır.
- `data/` altında değişen dosya sayısı: **0** (yalnızca `data/eval/t0047_frozen_write_gate_2026-09-16.json` eklendi).
- Dosya sayıları sabit: `data/*.bin` = 15, `data/*.pt` = 23.
- Kanarya hash değerleri değişmemiştir:
  - `data/kristal_model.pt`: `02e15b8198f8f3334609eb52691d699e80f88972245377c1a79772cb42ae1679`
  - `data/kristal_carpenter_model.pt`: `5f0b8c71727d15e338e4301634b0d05652c3230cebc532b1124aff3761e93b4d`
  - `data/train_balanced_sft.bin`: `55863e36b62414c6ada30956b30dc64a5254fe0717582384d43abd5d9cf46a24`
  - `data/train_balanced_sft_v2.bin`: `53b8406eee7e98d8339cd047a18a281b7a355ca5ba1891137701890dc322402c`
  - `data/train_chat_balanced.bin`: `443f93d352b07defca91b2a620f2e4154c3ed0b14f7c695535c959209e7608fa`
  - `data/train_carpenter_specialization.bin`: `13dd81bf700690b78ca15246a2fc1582ee6f8fea587c6d4e78f8ab1e1017b109`
  - `data/vocab.json`: `4b40eebee9c996c8dd3150f6b3c8896de85764ee1cf0b08e6c636e5c002a0e69`
  - `src/llm/tokenizer.py`: `560a3bfbf32c394a23559c9d3645cfbc3b554223a032156924749dcfa896a7af`
- Test paketi: `189 passed in 9.44s`.
