# .agent-bus/notes/T-0040.md

## T-0040 — Donmuş-yol kapısı tek otoriteyi okusun (T-0039 V1/V3/V4, saf KOD)

**Durum (2026-09-16T04:48:30Z itibariyle):** TAMAMLANDI — PASS

---

### 1. Özet ve Hedef
T-0039 bağımsız doğrulamasında tespit edilen bulgular (V1 gömülü desen listesi kopyaları, V3 boş girdide ulaşılamayan koruma kapısı, V4 testteki eski kod önbelleği) kapatıldı:
- `src/llm/frozen_guard.py` tek hakikat kaynağı olarak `.agent-bus/frozen.json` kütüğünü okuyan saf fonksiyonlarla yazıldı (fail-closed garantisiyle).
- `scripts/run_goal_pipeline.py` ve `src/llm/prepare.py` içindeki gömülü desen listeleri tamamen kaldırıldı ve `is_frozen_path` ile değiştirildi.
- `src/llm/prepare.py` içindeki `save_vocab_to` kontrolü fonksiyon girişine taşındı (boş girdide de derhal fail eder).
- `tests/test_compiler_entrypoints.py` testinde eski kod çıkarımı `/tmp` statik önbelleği yerine `tmp_path` altında her testte taze çıkarılacak şekilde güncellendi; dinamik otorite ve fail-closed testleri eklendi.

---

### 2. Uygulanan Değişiklikler ve Bulguların Kapatılması

1. **D1 (V1 Kapatıldı): `src/llm/frozen_guard.py`**
   - `load_frozen_patterns(frozen_file=".agent-bus/frozen.json") -> List[str]`
   - `is_frozen_path(path: str, frozen_file=".agent-bus/frozen.json") -> bool`
   - Dosya yoksa veya bozuksa `RuntimeError` (fail-closed).
   - `scripts/run_goal_pipeline.py` ve `src/llm/prepare.py` gömülü literal listelerden arındırıldı (`grep -c 'data/\*\.bin'` -> 0 ve 0).
2. **D2 (V3 Kapatıldı): Erken Koruma**
   - `prepare_dataset` fonksiyonunda `save_vocab_to` doğrulaması en başa alındı. Boş girdi durumunda bile donmuş yola yazma engellenir ve hiçbir veri yazılmadan `RuntimeError` fırlatılır.
3. **D3 (V4 Kapatıldı): Test İzolasyonu**
   - `test_probe_passes_on_new_code_fails_on_old_code` testi `/tmp/eski_rgp.py` dosyasını önbelleğe almaz; doğrudan `tmp_path / "eski_rgp.py"` içine `git show HEAD:...` ile taze çıkarır.
4. **D4 (Mutant Kanıtı):**
   - `test_frozen_guard_authority_and_fail_closed` testi özel desen dosyası tanımlandığında otoritenin dosyadan okunduğunu kanıtlar. Gömülü listeye geri dönülmesi durumunda test KIRMIZI yanar.

---

### 3. Kabul Kriterleri Doğrulama Sonuçları

| No | Kriter | Durum | Ölçüm / Komut |
|---|---|---|---|
| a | Gömülü desen listesi kalmadı | TAM | `grep -c 'data/*\.bin' scripts/run_goal_pipeline.py src/llm/prepare.py` -> 0 ve 0 |
| b | `is_frozen_path` imzası | TAM | `./venv/bin/python -c "import inspect,src.llm.frozen_guard as g; print(inspect.signature(g.is_frozen_path))"` -> `frozen_file` parametresi görünüyor |
| c | Test takımı regresyonu | TAM | `./venv/bin/pytest tests/ -q` (SANDBOX DIŞINDA) -> 171 -> 173 passed (+2 test) |
| d | Örnek yol sorguları | TAM | `f('data/train_balanced_sft.bin')` -> True, `f('data/rebuild/vocab_base_32852.json')` -> False, `f('data/realistic_rag/val.jsonl')` -> True |
| e | Fail-closed davranışı | TAM | `is_frozen_path('x', frozen_file='yok.json')` -> `RuntimeError` ([FAIL-CLOSED]) |
| f | Kanaryalar ve veri bütünlüğü | TAM | `shasum -a 256 data/vocab.json` -> `4b40eebee9c996c8...` değişmedi, `data/*.bin` sayısı 14, `cot_vault.jsonl` sha256 `97b06fe5...` değişmedi |
| g | D4 mutant kanıtı | TAM | Gömülü listeye geri dönen mutant kurguda test KIRMIZI yanar, dinamik dosya okumada YEŞİL |
| h | Boş girdi + `save_vocab_to='data/vocab.json'` | TAM | `prepare_dataset` boş girdiyle çağrıldığında hiçbir dosya üretmeden derhal `RuntimeError` fırlattı |

---

### 4. Anlık Dosya Hash'leri (Raporlama Anı)

- `src/llm/frozen_guard.py`: `38672ec7ac6a5d29c32ce439494245855b9df8b0353a41bcc94c6fbe41d3a617`
- `scripts/run_goal_pipeline.py`: `8d4945f5f9aa385617341fea44e4c46d7f44b0c605db8503cd00908d17a0a466`
- `src/llm/prepare.py`: `6dbd89b1d76fc65c4f225608a7a92a5dee49ecaf439b992e11d0b8172571c6c0`
- `tests/test_compiler_entrypoints.py`: `d3faba3f05acae2d80ca6b0e7642c19a1d0698009c47af1bc7d5dfa959383758`
- `data/vocab.json`: `4b40eebee9c996c8dd3150f6b3c8896de85764ee1cf0b08e6c636e5c002a0e69` (DEĞİŞMEDİ)
- `data/pedagogy/cot_vault.jsonl`: `97b06fe5befd6613b520d28dc20a74deaeb30e4e8f7e6675a23720a019ff5619` (DEĞİŞMEDİ)
