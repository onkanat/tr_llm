# T-0031 Yürütüm Notu: Asimetrik Görünürlüğün Kapatılması

**Tarih:** 2026-09-15
**Yürütücü:** antigravity
**Durum:** Devam ediyor / Tamamlanma aşamasında

---

## 1. Problem Tespiti ve Gerekçe

Yürütücünün karar gerekçeleri ve ara ölçüm bulguları daha önce çalışma alanı dışındaki walkthrough.md dosyasında tutulmaktaydı. Bu durum üç temel yapısal soruna yol açıyordu:
1. **Kiralanamaz:** Protokol yalnızca repo-göreceli yolları kiralayabildiği için (`scripts/agent_bus_mcp.py` `sanitize_rel_path`), çalışma alanı dışındaki yollar kural kapsamı dışında kalmaktaydı.
2. **Keşfedilemez:** Danışman etmen bu dosyalara erişim yetkisine sahip olsa da, nerede durduklarına dair protokol düzeyinde bir gösterici bulunmamaktaydı.
3. **Büyüyor:** 23 görevin yürütüm anlatısı tek bir monolitik dosyada birikmekteydi.

---

## 2. Yapılan İcraat

### a) Protokol ve Şema Güncellemesi (`.agent-bus/SPEC.md`)
* Kök dizin şemasına `.agent-bus/notes/T-XXXX.md` yüzeyi eklendi.
* `results/T-XXXX.json` şemasına opsiyonel `narrative_log` alanı (`path`, `sha256`, `ozet`) eklendi.
* `bus_send` için katı atıf kuralı (Yol + SHA-256 zorunluluğu, satır/bölüm numarasına ve uzun alıntıya yasak) tanımlandı.
* Değişmez 3 kuralın metni (`grep -n 'Kiralama olmadan yazma yok'`) birebir korundu.
* Kaynak dosya: `.agent-bus/SPEC.md` (sha256: `d397877d40accf7365979a013783d9ad638f9ce92920913bd3fa524804cddae4`)

### b) MCP Sunucu Yetenek Genişletmesi (`scripts/agent_bus_mcp.py`)
* `report_result` metoduna opsiyonel `narrative_log` parametresi eklendi.
* Yol güvenliği denetimi (`sanitize_rel_path`) yeni alana da uygulandı; `../` veya mutlak yol geçişleri yazma yapılmadan önce `ValueError` ile reddedilir hale getirildi.
* Kaynak dosya: `scripts/agent_bus_mcp.py` (sha256: `7339e0ae30d91038c38a2635270cb3119e450cda3859f654bdf10d9535840737`)

### c) Test Koruması (`tests/test_agent_bus_narrative.py`)
* Geriye uyumluluk: `narrative_log` verilmediğinde eski şema bit düzeyinde korunur.
* Geçerli girdi: `narrative_log` alanı sonuç JSON'ına başarıyla yazılır.
* Negatif test: `../escaping.md` ve `/tmp/escaping.md` reddedilir ve diske dosya yazılmaz.
* Tüm test takımı: 135 passed in 7.70s.

---

## 3. Somut Artefakt Atıfları (Yol + SHA-256 Kuralı)

* `data/pedagogy/cot_vault.jsonl` (sha256: `97b06fe5befd6613b520d28dc20a74deaeb30e4e8f7e6675a23720a019ff5619`) — 91 satırlık üretim akıl yürütme kasası; test süresince bit düzeyinde değişmez kaldı.
* `data/eval/t0030_wiki_revival_2026-09-15.json` (sha256: `128f266cf178becce73a1347d3675851eb9d86c897e617d15ba4524d254a2cd2`) — T-0030 wiki canlandırma raporu.

---

## 4. Kapsam Dışı Öneriler ve Değerlendirme

1. **Geriye Dönük walkthrough.md Taşınması:** Eski görevlerin (8.1..8.24) taşınması geriye dönük rekonstrüksiyon riski taşıdığı için bu görevde yapılmamıştır; ancak ihtiyaç duyulursa kaynak yol ve sha256 açıkça beyan edilerek yapılabilir.
2. **`external_writes` İhtiyacı:** T-0028 ve T-0029'da olduğu gibi repo dışı konfigürasyon dosyalarına (`~/.gemini/config/skills/docs_update/SKILL.md`) yapılan müdahalelerin SPEC üzerinde `external_writes` listesi (yol + sha256 + yedek yolu) olarak resmiyet kazanması protokolün sağlamlığını artıracaktır.
