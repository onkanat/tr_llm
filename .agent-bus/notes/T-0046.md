# .agent-bus/notes/T-0046.md

## T-0046 — F2-3: Hat geçişi — eğitim/değerlendirme okuma yolları _v2'ye + sözlük/model hizalaması (32.852)

**Durum (2026-09-16T12:49:00Z itibariyle):** TAMAMLANDI — PASS

---

### 1. Amaç ve Kapsam
Eğitim ve değerlendirme hatlarının `_v2` veri kümesine (`data/train_balanced_sft_v2.bin`) geçirilmesi ve sözlük/model embedding boyutlarının 32.852 (`data/rebuild/vocab_base_32852.json`) tabanına hizalanması gerçekleştirildi.
Yalnızca 5 kod dosyası güncellendi; hiçbir `.bin`, `.pt` veya `.jsonl` kütüğüne yazılmadı (smoke testi yalnızca `scratch/t0046_smoke.pt` kütüğüne yazdı).

---

### 2. Ön Koşullar

- `data/train_balanced_sft_v2.bin`: `53b8406eee7e98d8339cd047a18a281b7a355ca5ba1891137701890dc322402c` (DEĞİŞMEDİ)
- `data/kristal_model.pt`: `02e15b8198f8f3334609eb52691d699e80f88972245377c1a79772cb42ae1679` (472,572,529 B) (DEĞİŞMEDİ)

---

### 3. Değiştirilen Siteler ve Dosya Hash'leri

| Dosya | Yapılan Değişiklik | sha256 (Önce) | sha256 (Sonra) |
|---|---|---|---|
| `train.py` | `vocab_path = 'data/rebuild/vocab_base_32852.json'`; yerel `resize_state_dict` yerine `src.llm.prompt_contract` kanonik aktarımı | `42389319e602e72a...` | `791d470e2c1c6698...` |
| `scripts/train_scientific_sft.py` | `vocab_path = 'data/rebuild/vocab_base_32852.json'`; `train_bin_path = 'data/train_balanced_sft_v2.bin'`; kanonik `resize_state_dict` aktarımı | `0d96df150fc639a2...` | `35f56d76e2d77c7c...` |
| `scripts/retrain_clean_models.py` | Stage 2 `--data data/train_balanced_sft_v2.bin` ve başlık yorumu güncellemesi | `74905148605f893a...` | `22a27b7869f01699...` |
| `scripts/run_goal_pipeline.py` | Satır 261 Stage-2 SFT çağrısı `--data data/train_balanced_sft_v2.bin` (Satır 232 compile çağrısı spec gereği değiştirilmedi) | `8d4945f5f9aa3856...` | `774b5c94ef851175...` |
| `scripts/evaluate_sft_benchmarks.py` | `vocab.load("data/rebuild/vocab_base_32852.json")`; `val_loss_base_sft` yolu `data/train_balanced_sft_v2.bin` ve ekran etiketi güncellemesi | `0462f9a005034e8d...` | `0395a62e113dd606...` |

---

### 4. Kapı Ölçümleri (Ampirik Kanıtlar)

1. **Kapı a (Okuma Yolları):**
   - Grep ve statik analiz: Tüm aktif okuma siteleri `data/train_balanced_sft_v2.bin` yoluna bağlandı.
   - Bilerek değiştirilmeyen tek site: `scripts/run_goal_pipeline.py:232` (hareketsiz compile çağrısı).
2. **Kapı b (Sözlük Hizalaması 32.852):**
   - `train.py`, `train_scientific_sft.py`, `evaluate_sft_benchmarks.py` sözlük olarak `data/rebuild/vocab_base_32852.json` kütüğünü yüklüyor.
3. **Smoke Testi (KOŞUM):**
   - Komut: `./venv/bin/python train.py --data data/train_balanced_sft_v2.bin --load-path data/kristal_model.pt --save-path scratch/t0046_smoke.pt --steps 2 --batch-size 2`
   - Çıktı:
     - `Sözlük Yüklendi. Kelime dağarcığı boyutu: 32852`
     - `Veri kümesi yüklendi: data/train_balanced_sft_v2.bin (Blok boyutu: 64). Toplam morfem token sayısı: 1,793,433`
     - `[SOZLESME_UYARI] Parametre 'embedding.embedding.weight' boyut uyumsuzluğu: eski (32816, 768) -> yeni (32852, 768) (36 satır dolduruldu (padding))`
     - `[SOZLESME_UYARI] Parametre 'lm_head.weight' boyut uyumsuzluğu: eski (32816, 768) -> yeni (32852, 768) (36 satır dolduruldu (padding))`
     - `[SOZLESME_UYARI] Parametre 'lm_head.bias' boyut uyumsuzluğu: eski (32816,) -> yeni (32852,) (36 satır dolduruldu (padding))`
     - Başlangıç Kaybı: `7.0575`, Bitiş Kaybı: `7.3342` (Sonlu, NaN/inf yok).
     - Adım: 2, Süre: 1.94s, Device: `cpu`.
4. **Pytest (KOŞUM):**
   - Sandbox dışında `./venv/bin/pytest tests/ -q` çalıştırıldı: **186 passed in 9.24s** (0 fail, 0 warning).

---

### 5. Kanaryalar ve Değişmezlik

- `data/kristal_model.pt`: `02e15b8198f8f3334609eb52691d699e80f88972245377c1a79772cb42ae1679` (DEĞİŞMEDİ)
- `data/train_balanced_sft.bin`: `55863e36b62414c6ada30956b30dc64a5254fe0717582384d43abd5d9cf46a24` (DEĞİŞMEDİ)
- `data/train_balanced_sft_v2.bin`: `53b8406eee7e98d8339cd047a18a281b7a355ca5ba1891137701890dc322402c` (DEĞİŞMEDİ)
- `data/vocab.json`: `4b40eebee9c996c8dd3150f6b3c8896de85764ee1cf0b08e6c636e5c002a0e69` (DEĞİŞMEDİ)
- `src/llm/tokenizer.py`: `560a3bfbf32c394a23559c9d3645cfbc3b554223a032156924749dcfa896a7af` (DEĞİŞMEDİ)
- `data/*.bin` dosya sayısı: 15 (DEĞİŞMEDİ).
- `data/*.pt` dosya sayısı: 23 (DEĞİŞMEDİ).
- Görev penceresinde `data/` ağacı altında değişen dosya sayısı: 0 (yalnızca t0046 raporu yazıldı).
- `scratch/f2_backup_2026-09-16/` altındaki 6 bağımsız yedek eksiksiz ve sha-özdeş korundu.
