# T-0057 Notu: Prompt Maskeleme Mantığının Tek Kaynağa İndirilmesi ve EOS Sızıntısının Kapatılması

**As-Of Damgası:** 2026-09-16T21:03:00Z  
**Sorumlu:** antigravity  
**Kapsam:** Dört sitedeki (`scripts/train_scientific_sft.py`, `scripts/train_sft_fine_tuning.py`, `scripts/train_chat_sft.py`, `scripts/evaluate_sft_benchmarks.py`) satır içi prompt maskeleme döngülerinin kaldırılarak `scripts/train_step_demo.py` içindeki paylaşılan `mask_prompt_targets` saf fonksiyonuna bağlanması; EOS->sonraki token sızıntısının her 4 sitede kapatılması ve regresyon/dedup testleriyle pinlenmesi.

---

## 1. Kiralama Durumu (Lease Status)
Tüm işlemler yazım öncesinde kiralanan dosyalar üzerinde yapılmıştır:
- `scripts/train_scientific_sft.py` (file)
- `scripts/train_sft_fine_tuning.py` (file)
- `scripts/train_chat_sft.py` (file)
- `scripts/evaluate_sft_benchmarks.py` (file)
- `tests/test_prompt_masking_dedup.py` (file)
- `data/eval/t0057_masking_dedup_2026-09-16.json` (file)
- `.agent-bus/notes/T-0057.md` (file)

---

## 2. Dört Sitedeki Değişiklikler ve Satır Numaraları

1. **`scripts/train_scientific_sft.py`** (sha256: `7619f6a5...`)
   - 119-138 arasındaki satır içi döngü kaldırıldı.
   - `targets_np = mask_prompt_targets(x_cpu, y_cpu, output_start_id, eos_id)` çağrısı eklendi.
2. **`scripts/train_sft_fine_tuning.py`** (sha256: `b6790f2c...`)
   - 91-111 arasındaki satır içi döngü kaldırıldı.
   - `targets_np = mask_prompt_targets(x_cpu, y_cpu, output_start_id, eos_id)` çağrısı eklendi.
3. **`scripts/train_chat_sft.py`** (sha256: `1397ca6c...`)
   - 138-158 arasındaki satır içi döngü kaldırıldı.
   - `targets_np = mask_prompt_targets(x_cpu, y_cpu, output_start_id, eos_id)` çağrıldı; ek `pad_id` maskelemesi korundu.
4. **`scripts/evaluate_sft_benchmarks.py`** (sha256: `1f190d09...`)
   - `compute_val_loss` içindeki 84-97 satır içi döngüsü kaldırıldı.
   - 1-D tensör uyarlaması ile `targets_np = mask_prompt_targets(x_np[None, :], y_np[None, :], output_start_id, eos_id)[0]` çağrıldı.

---

## 3. Davranış Farkı Beyanı
Dört sitedeki davranış değişikliği YALNIZCA:
(a) `x[k] == eos_id` anındaki ilk hedefin (yani ilk `<PAD>`) sızıntı olarak öğrenilmesinin kapatılması (`targets_np[b, k] = -100`),
(b) Satır içi kopyaların tek ve saf fonksiyon `mask_prompt_targets`'a taşınmasıdır.
Bunun dışında hiçbir mantıksal veya davranışsal fark yoktur.

---

## 4. Test Paketi (`tests/test_prompt_masking_dedup.py`)
6 test oluşturuldu:
1. `test_no_inline_prompt_masking_copies_left`: Grep/AST denetimi ile `is_output` değişkeninin depoda SADECE `scripts/train_step_demo.py` içinde kaldığı doğrulandı.
2. `test_compute_val_loss_file_not_found`: Olmayan dosyada `-1.0` dönüşü doğrulandı.
3. `test_compute_val_loss_short_file`: Çok kısa dosyada `-1.0` dönüşü doğrulandı.
4. `test_compute_val_loss_determinism`: `RandomState(42)` ile iki çağrıda deterministik eşit sonuç doğrulandı.
5. `test_compute_val_loss_finite_loss`: Sentetik veri ve modelle sonlu geçerli kayıp doğrulandı.
6. `test_compute_val_loss_target_difference_only_at_eos`: `compute_val_loss` hedef üretiminin eski satır içi mantıkla YALNIZCA `x[k] == eos_id` konumlarında farklı olduğu (`targets_old == 1` vs `targets_new == -100`), diğer tüm konumlarda birebir özdeş olduğu kanıtlandı.

**Pytest Sonucu:**
- `pytest tests/test_prompt_masking_dedup.py`: **6 passed in 1.96s**.
- `pytest -q tests/`: **212 passed in 10.13s**.

---

## 5. Mutant Doğrulaması
`scripts/train_chat_sft.py` içine geçici olarak `is_output = False # MUTANT COPY` satırı eklendiğinde `test_no_inline_prompt_masking_copies_left` testi hemen **RED** (AssertionError, exit code 1) oldu.
Satır temizlenip dosya aslına döndürüldüğünde sha256 (`1397ca6c...`) tam olarak doğrulandı ve test **GREEN** oldu.

---

## 6. Kanaryalar
- Hiçbir eğitim veya üretim koşumu yapılmadı.
- Hiçbir `.pt` veya `.bin` yazılmadı (`data/*.pt`: 29, `data/*.bin`: 15 korundu).
