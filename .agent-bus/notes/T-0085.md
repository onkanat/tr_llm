# T-0085 — `train.py` ölü varsayılanı kaldırıldı (`--save-path` ZORUNLU) · yürütüm notu

**Tarih:** 20 Eyl 2026 · **Yürütücü:** claude · **Kapsam:** kod + belge
**Kaynak:** T-0083 raporu §8, açık madde **1**
**Rapor:** `data/eval/anka_r8_train_varsayilan_2026-09-20.md`

---

## 0. Kural

Kapılar **K1…K7 + iki çürütme** ölçümden **önce** ilan edildi, sonra **değiştirilmedi**.
Ölçüm sonrası çıkan kusur (§1) **tasarım düzeltilerek değil, rapora yazılarak** kapandı.

---

## 1. KENDİ KUSURUM — ilan ettiğim sayı hiçbir ölçütün çıktısı değil

Şartnamede **K1 = "111 atıf / ~35 dosya"** diye ilan ettim. Bugün ölçtüm:

| Kapsam | Desen | Adet / dosya |
|---|---|---|
| izlenen ağaç (`.`) | `kristal[a-z_]*_model\.pt` | **108 / 33** |
| izlenen ağaç | `kristal[a-z_]*\.pt` (geniş) | **153 / 40** |
| `scratch/` | dar | **241 / 43** |
| izlenen ağaç `md`+`json` dahil | dar | **402 / 112** |

**111/35 hiçbirinin çıktısı değil.** Kapıyı **düzeltmedim**
([[tasarim-sayisi-betikle-hesaplanmali]]); kusuru rapora yazdım. Ders: ilan edilen
sayı **komutuyla** yazılmalı.

## 2. İKİNCİ KUSURUM — araç `scratch/`'ı sessizce atladı

`grep` burada **ugrep 7.8.4** ve **`.gitignore`'a uyuyor**. `scratch/` sembolik bağ
değil ama gitignore'lu ⇒ kökten özyinelemeli tarama onu **sessizce atlar**: `241`
atıf ilk sayımda görünmedi. [[gitignore-kapanis-kanitini-saklar]] tuzağının
**sayacın kendisinde** ikinci üreyişi. Sayı artık **kapsamıyla** yazılıyor.

## 3. Yapılan (yalnız `train.py` + belge)

| # | Yer | Ne |
|---|---|---|
| A | `train.py:149-158` | ölü varsayılan **kaldırıldı** (`model_save_path = None`); `# EMEKLI (T-0085):` etiketli **ayrı blok** (denetimde ayıklanabilsin) |
| B | `train.py:175-182` | **fail-closed zorunlu argüman kapısı** — yokluğunda `RuntimeError` |
| C | `USER_GUIDE.md` | tablo hücreleri güncellendi; T-0083 notu **silinmedi**, ardına **damgalı GÜNCELLEME** eklendi |
| D | rapor + bu not | — |

**Silinen satır: toplam 3** (train.py 1 ölü varsayılan + USER_GUIDE 2 bayat tablo
hücresi). Tarihsel not **silinmedi** — T-0084'ün banner disipliniyle aynı.

## 4. Ölçülen koşumlar (üçü de `data/anka_a1r_pretrain_val.bin`; eğitim BAŞLATILMADI)

| Koşum | `rc` | Sonuç |
|---|---|---|
| `--save-path` **yok** | **1** | `RuntimeError: … --save-path verilmedi …` · **sıfır yazım** (`data/*.pt` mtime korundu) |
| `--save-path $TMPDIR/…` (2 adım) | **0** | koşum tamam · atomik kayıt · artık `.tmp` yok · hedef `356M` `aafb33f7…` (sonra **silindi**) |
| `--save-path data/zzz_kapi_testi.pt` | **1** | **donmuş kapı** ateşledi; argüman kapısı **0 kez** ⇒ K3-pozitif + K4 tek koşumda |

Üçüncü koşum ikisini birden kanıtlar: yeni kapı **yalnız yoklukta** ateşler.

## 5. Diğer kapılar

- **K2:** küçük-harf `"kristal"` = **0**; harf-duyarsız = **4** — o 4 satır
  `KristalLM`/`KristalDataset`/`KristalEmbedding`, yani **korunan mimari sınıf adları**
  ([[mimari-korunur-kristallm]]). İki sayı çelişmiyor, **ölçütler farklı**.
  Emekli notu yolu **adıyla anmaz** ⇒ sayaç **şişmedi** ([[emekli-notu-sayaci-sisirir]]).
- **K4:** AST testi `9 passed, 3 deselected`; yapı: tanım 16 · çağrı **188** ·
  `torch.save` **306** ⇒ çağrı **save'den önce**.
- **K6:** `venv/bin/pytest` ⇒ **213 passed, 1 failed** — düşen tek test
  `test_gateway_http_server_endpoints`, nedeni `socketserver` `PermissionError`
  (**sandbox soket yasağı**); **sayı değişmedi**.
- **K7:** `git add -A` **kullanılmadı**.

## 6. Çürütme 2 — beyan edilen sapma

Kapı, veri yüklemesinden **sonra**; ama **her yazımdan ve eğitim döngüsünden ÖNCE**
(§4/1'de ölçüldü: durma anında yazım yok). Veri yüklemesi saniyeler, eğitim saatler.
Kapıyı **ölçümden sonra taşımak** ilan edilmiş kapsamı sessizce değiştirmek olurdu ⇒
taşınmadı, **AÇIK İŞ** olarak yazıldı (rapor §9/1).

## 7. Kapanış

- Kiralamalar: `train.py`, `USER_GUIDE.md`,
  `data/eval/anka_r8_train_varsayilan_2026-09-20.md`, `.agent-bus/notes/T-0085.md`
  ⇒ **boşaltıldı**.
- `changed_files` **4**, iki yönlü, `len(cf) == len(set(cf))`.
- Kapsam dışı **değişmedi**: `chat_prompt.py`, `test_model.py`, `src/**`,
  `scripts/**`, `CLAUDE.md`, `README.md`.
- Sınıfın kalanı **beyan edildi**, sessizce düzeltilmedi (§3 ayrımı raporda).
- `git add -A` **kullanılmadı**; commit/push **operatör yetkisinde**.
