# T-0049 Görev Kapanış Notu (F2-6: retrain_clean_models Kapısı + Kanonik Guard)

- **Görev ID:** `T-0049`
- **Tarih:** `2026-09-16`
- **Yürütücü:** `antigravity`
- **Durum:** `PASS`

---

## 1. Değiştirilen Dosyalar (`writes` & `changed_files`)
Beyan edilen 5 dosya birebir eşittir:
1. `scripts/retrain_clean_models.py`
2. `src/llm/frozen_guard.py`
3. `tests/test_compiler_entrypoints.py`
4. `data/eval/t0049_retrain_gate_2026-09-16.json`
5. `.agent-bus/notes/T-0049.md`

---

## 2. Yapılan Değişiklikler

### (1) Kanonik Yardımcı (`src/llm/frozen_guard.py`)
- `check_frozen_save_path(path: str, allow_frozen_write: bool = False, frozen_file: str = DEFAULT_FROZEN_FILE) -> None` fonksiyonu eklendi.
- Hata formatı: `f"Donmuş yola yazma engellendi: {path} (allow_frozen_write=False)"`.
- Fail-closed, saf fonksiyon ve tam tip ipuçları.

### (2) `scripts/retrain_clean_models.py`
- `allow_frozen_write = "--allow-frozen-write" in sys.argv` desteği eklendi.
- Her iki `shutil.copyfile` çağrısı öncesine (`:59` `data/kristal_model_pre_clean.pt` ve `:106` `data/kristal_model_sft.pt`) koruma guard'ı eklendi.
- 5 eğitim `run_cmd` çağrısının (Stage 1, Stage 2, Stage 3, Stage 4 DPO, Stage 5 Carpenter) hemen öncesine `check_frozen_save_path` koruması eklendi.
- Alt süreç komut listelerine koşullu olarak `--allow-frozen-write` argüman aktarımı sağlandı.

### (3) Testler (`tests/test_compiler_entrypoints.py`)
- `test_check_frozen_save_path_gate` kanonik guard'ı test edecek şekilde güncellendi.
- `test_ast_retrain_clean_models_has_frozen_guards` AST testi eklendi:
  - 2 copyfile çağrısı öncesi guard ve hedef doğrulaması,
  - 5 eğitim çağrısı öncesi guard ve sıralama denetimi,
  - 5 alt sürece koşullu bayrak aktarım denetimi.

---

## 3. Mutant Kanıtları (STATİK)
- **M1:** `scripts/retrain_clean_models.py` :59 copyfile guard'ı kaldırıldı -> AST testi `AssertionError: İlk copyfile öncesinde çağrı yok` vererek **KIRMIZI** oldu. Satır geri konulduğunda **YEŞİL** oldu. SHA-256 özdeşliği doğrulandı (`7da0833c081c4646c2a8d7d6c1dc6a4b2775b0685fcda0b72f5a035d9df4d5b6`).
- **M2:** Stage 1 eğitim guard'ı `run_cmd` sonrasına taşındı -> AST testi `AssertionError: run_cmd (satır 82) öncesinde check_frozen_save_path yok! Önceki: shutil.copyfile` vererek **KIRMIZI** oldu. Satır geri konulduğunda **YEŞİL** oldu. SHA-256 özdeşliği doğrulandı (`7da0833c081c4646c2a8d7d6c1dc6a4b2775b0685fcda0b72f5a035d9df4d5b6`).

---

## 4. Koşum Yok Kanıtı & Kanaryalar
- Hiçbir eğitim veya betik çalıştırılmadı.
- `data/` altında değişen dosya sayısı: 0 (yalnızca rapor eklendi).
- Kanaryalar korundu (`kristal_model.pt`, `kristal_model_pre_clean.pt`, `kristal_model_sft.pt`, `kristal_carpenter_model.pt`, `.bin` dosyaları, `vocab.json`, `tokenizer.py`).
- `data/*.pt`: 23, `data/*.bin`: 15, `scratch/f2_backup_2026-09-16/`: 6 dosya sağlam.
- Test paketi: **191 passed in 10.13s** (190 -> 191).
