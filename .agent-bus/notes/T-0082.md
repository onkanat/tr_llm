# T-0082 — Belge tazeliği onarımı (yürütüm notu)

**Tarih:** 20 Eyl 2026 · **Yürütücü:** claude (tek yürütücü) · **Kapsam:** belge

---

## 0. Ne istendi

Operatör (20 Eyl 2026): *"T-82'yi aç, SPEC.md:48'i düzelt ve diğer documanları."*

T-0080 `done` kapandı; kapanış raporu §5.1 ve `dokuman-borcu-2026-09-19.md` bu işi
T-0082'ye devretmişti.

---

## 1. Ölçüm (yazmadan ÖNCE)

Her iddia önce ölçüldü; hiçbiri varsayılmadı.

| Ne | Ölçülen | Kaynak |
|---|---|---|
| `.agent-bus/notes/` dosya / izlenen / izlenmeyen | **44 / 44 / 0** | `ls` + `git ls-files` |
| `6bca90b`'nin `notes/` altına eklediği dosya | **tam 17** | `git show --stat 6bca90b -- .agent-bus/notes/` |
| `SPEC.md` satır 3 damgası | `2026-09-19 (T-0081)` | `sed -n 3p` |
| `CLAUDE.md` donmuş liste ↔ `frozen.json` | **10/10 iki yönlü eşit** | betikle karşılaştırma |
| `venv/bin/pytest` | **213 passed, 1 failed** | yeniden koşuldu |
| `CLAUDE.md`'nin `train.py:66` / `:112-116` atıfları | **hâlâ birebir doğru** | `sed -n` ile satır okundu |
| `events.jsonl` | **1.191 satır**, tip sayısı **6** | `wc -l` + tip sayımı |

**Ölçülen tazelik hükmü:** yalnız `SPEC.md` bayattı. `CLAUDE.md`'nin üç iddiası da
( pytest sayısı, donmuş liste, `train.py` satır atıfları) **yeniden ölçüldü ve doğru
çıktı** ⇒ orada **değişiklik yapılmadı**, yalnız doğrulama kayda geçti.

---

## 2. Yapılan iki düzeltme (`SPEC.md`)

1. **D8 paragrafı.** "27'si izleniyor, 17'si izlenmiyor" ölçümü artık geçerli değil:
   `6bca90b` tam olarak o 17 dosyayı ekledi ⇒ dizin **44/44**.
   ⚠ Düzeltme **"0 izlenmiyor"** diye yazılmadı, çünkü **bu notu yazmak** dizinde
   yeniden 1 izlenmeyen dosya oluşturur. Kalıcı kural korundu: *durmak ≠ sürümlenmek*;
   durum **her turda yeniden ölçülür**.
2. **Satır 3 damgası** → `2026-09-20 (T-0082)`.

### 2.1 Aynı sınıftan bir madde daha düzeltildi

`SPEC.md` "Bilinen açık kusurlar" **#1** (`task_updated` olayı YOK) iki kez kaymıştı:
sayım `1.174`'tü, **1.191** ölçüldü. Daha önemlisi bir **ölçüm tuzağı** doğdu:
log artık `task_updated` dizgesini **1 satırda** taşıyor — ama o satır bir olay **tipi**
değil, **T-0081'in `result_reported` kaydının `evidence` metni**, yani kusuru *anlatan*
bir alıntı. `grep task_updated log/events.jsonl` yapan okuyucu **yanlışlıkla "kusur
kapandı"** okur. İddia artık **kaynak üzerinden** (`grep -c scripts/agent_bus_mcp.py`
= **0**) sınanacağı yazılı.

---

## 3. Kapsam dışında bulunan iki kusur (BEYAN — düzeltilmedi)

Ölçüm sırasında çıktılar; T-0082 kapsamına **girmez**, `writes[]`'te **yok** ⇒
dokunulmadı. Kayda geçiyor, T-0083'e devredildi.

1. **`USER_GUIDE.md` — 7 ölü referans**, `README.md` — 1 ölü referans.
   Hepsi `data/kristal_*.pt` yollarına **canlı komut** veriyor. Ölçüldü:
   bu checkpoint'ler **diskte de git'te de yok** (`ls data/*.pt` → yalnız
   `anka_a1.pt`, `anka_a1r.pt`). `CHANGELOG.md`'deki Kristal atıfları **düzeltilmeyecek**:
   changelog **tarihsel kayıttır**, o an olanı yazar.
2. **`train.py:149` — kod kusuru, belge değil.** `model_save_path = 'data/kristal_model.pt'`
   ⇒ `--save-path` verilmezse koşum, **silinmiş zincirin adıyla** diske yazar. Bu varsayılan
   `data/*.pt` (donmuş) desenine düştüğü için `check_frozen_save_path` **yüksek sesle
   durur** — sessiz değil; ama varsayılanın **kendisi** bayat. `USER_GUIDE.md:101`'in
   "*varsayılan `data/kristal_model.pt`*" satırı **kodu doğru anlatıyor** ⇒ kusur
   belgede değil, **kaynakta**.

---

## 4. Kendi kusurum (kayda geçer)

Donmuş liste karşılaştırmasında **ilk denemem YANLIŞ AYRISMA üretti**: regexim satırdaki
**ilk** parantezi yakaladı ve başlık metnini desen sandı ⇒ "doc→json eksik: 1". Çıpa
`**10 desen** (\`` olarak düzeltilince sonuç **10/10 iki yönlü eşit** çıktı.
⇒ *Ölçüm kabının kapsamı iddiadan dar* ailesi (**[[denetim-kapsami-iddiadan-dar]]**);
sayı doğruyken **araç** yanlıştı. Düzeltilmeden raporlanmadı.

---

## 5. Kapanış

- **Öngörü ÖLÇÜMLE doğrulandı (varsayım değil):** bu notu yazdıktan sonra
  `.agent-bus/notes/` → **45 dosya / 44 izlenen / 1 izlenmeyen**. Yani §2.1'de
  `SPEC.md`'ye "izlenmeyen 0" **yazmama** gerekçesi **kanıtlandı**; sabit bir "0"
  iddiası yazıldığı anda yanlış olurdu.
- Kiralamalar: `.agent-bus/SPEC.md`, `CLAUDE.md`, `.agent-bus/notes/T-0082.md`,
  `data/eval/anka_r6_belge_onarim_2026-09-20.md` — kapanışta **boşaltıldı**.
- `git add -A` **kullanılmadı**; commit/push **operatör yetkisinde**.
- `SPEC.md`: `5e9e08a0…` (274 satır) → **`afe111bd611f78e0cccb3090945aca43e0bdacc6c57e59a370cb99955a221c51`** (283 satır).
- `CLAUDE.md`: `d11e3e17…` → **değişmedi** (doğrulandı, düzeltme gerekmedi).
- Rapor: `data/eval/anka_r6_belge_onarim_2026-09-20.md`.
