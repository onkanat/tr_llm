# T-0077 — A1 ön-eğitim koşumu: yürütücü anlatı günlüğü

**Talimat (birebir):** *"periyodik checkpoint ekle, sonra A1'i başlat."* — ikinci yarı.
Birinci yarı T-0076 (kapandı, `train.py` sha `8b3959798b2a…`).

## 1. Yapılandırma — tercihle değil, T-0073 §2b'de İLAN EDİLEN karar kuralıyla

`b8×k256` (2.048 jeton/adım) · **48.828 adım** · `--save-every 500`.
Gerekçe: blok-256 kazananı b16×k128 tabanını **1,0861×** geçti ⇒ ilan edilen `>1,02` eşiği ateşledi.
Bildirilen süre **~10,58 saat** (kararlı 0,780 sn/adım).

**`--save-every 500` gerekçesi:** hedef **tek dosyadır** ve atomik yazılır ⇒ disk ayak izi
**birikmez** (tepe ~2×355 MiB). 500 adım × 0,78 sn ≈ **6,5 dk** en kötü kayıp. Boş disk 44 GB.

## 2. Girdi doğrulaması (ölçüldü)

| ne | değer |
|---|---|
| korpus | `data/anka_a1_pretrain.bin` · 200.000.000 bayt = **100.000.000 jeton** · sha256 `383a9c890b75ec96195122b9981d7a73371d3bcaa3b9f6e7c2a188510d917cb8` |
| sözlük | `data/rebuild/vocab_base_32852.json`, V=32.852 ⇒ **ln V = 10,3997** (canlılık ölçeği) |
| hedef | `data/anka_a1.pt` — koşumdan önce **yoktu** (sessiz üzerine yazma yok) |

**K8 TEMSİL (T-0072 kararının kapısı) gerçek dosyada teyit edildi:** `<ENT>`=0 · `<CAP>`=0 ·
`<PROPER_NOUN>`=%6,4021 ⇒ **SAĞLANDI**. Ek ölçüm: `<PAD>`=0 ve `<OUTPUT>`=0 ⇒ A1'de PAD kayıp
maskesi **etkisiz** (T-0053'ün 1,58/6,85 ölçek belirsizliği bu koşum için **yok**), `--pretrain`
zorunlu (SFT maskelemesi her pencereyi −100 yapar, MPS sessizce 0,0000 döndürür — T-0073).

## 3. Ön-kontrol fail-closed (iki dal da sınandı)

Bekçi: **sinyal okunamazsa DUR** (loadavg/vm_stat), **hedef doluysa DUR**. Yuk **değeri**
yüksekse durmaz — çünkü b8×k256 hücresi 0,780 sn/adım'ı **loadavg 4,24 altında** ölçtü;
ölçülmüş bir değeri reddetmek karar değil, sinyalsizlik reddedilir ([[on-kontrol-fail-closed-olmali]]).

Pozitif kontrol (`scratch/anka_a1_guard_probe.py`, üç dal): hedef-var → **rc=2, eğitim başlamadı**;
sinyal-yok (simüle `OSError`) → **rc=2, gerekçe log'da**; sinyaller okunur + hedef yok → **geçer**.
⇒ kapı **ayırt edici**, vakum değil ([[probe-pozitif-kontrol]]).

**Ölçüm kusuru (kayda geçti):** `temsil()` ilk sürümü sözlük dosyasını düz dict sanıp yedi işaretin
hepsini "sözlükte YOK" raporladı. Dosya `{"stoi": …, "next_id": …}` **sarmalıdır**; id'ler artık
kanonik `Vocabulary.load().stoi`'dan okunuyor. `<PN>` jetonu **yoktur** — person-name ekseni
`<PROPER_NOUN>`(=4) üzerinden ölçülür. (Belleğe yazıldı: [[sozluk-idisini-kanonik-kaynaktan-oku]].)

## 4. Başlatma ve canlı durum

`caffeinate -i -s` ile uykuya karşı korumalı, **sandbox DIŞINDA** (MPS sandbox'ta görünmez).
Log `scratch/anka_a1_kosu.log`; canlı bekçi `scratch/anka_a1_bekci.py` (Monitor).

| ölçüm | değer |
|---|---|
| cihaz | `Metal GPU (MPS) seçildi` ✓ |
| hedef modu | `ON-EGITIM (duz sonraki-jeton)` ✓ |
| periyodik duyuru | `her 500 adımda -> 'data/anka_a1.pt' ('.tmp' üzerinden atomik)` ✓ |
| **ilk kayıp** | **10,6032** (adım 1) — ln V = 10,3997; `0,0000` sessiz no-op imzası **yok** ⇒ G1 ✓ |
| kararlı adım (ilk 50) | 0,75–0,91 sn, **≈0,84 sn** ⇒ bekçi eşiği 2,34 sn'nin altında, **G6 ateşlemedi** |
| **periyodik kayıt CANLI** | adım 500 → `data/anka_a1.pt` **355 MiB** yazıldı, `.tmp` artığı **yok** ⇒ atomiklik **gerçek koşumda** doğrulandı (T-0076 yalnız sentetik mutantla sınamıştı) |
| kayıp gidişi | 10,6032 → 6,3011 (adım 10) → 5,2003 (adım 50) → 4,1968 (adım 510) ⇒ öğrenme var |
| adım süresi sürüklenmesi | adım 500 civarı **1,00–1,19 sn** (ilk 50'nin ~1,3 katı); eşiğin (2,34) altında ama **kayda geçiyor** — kapanışta medyan olarak raporlanır |

**Bildirilen süreye karşı canlı ölçüm:** ilk 50 adım 0,84 sn ⇒ ~11,4 saat (bildirilen 10,58).
Adım 500 civarında sürüklenme **1,06 sn** ⇒ **~14,4 saat**, sapma **+%36**. Kapanışta ham
medyanla raporlanacak; koşum sürdükçe yeniden ölçülecek (bildirilen sayı ölçümün kendisiydi,
tahmin değil — ama canlı ölçüm onu **aşıyor** ve bu yazılmalıdır).

## 4b. KESİNTİ KAYDI — 2026-09-18T16:20:55Z (koşum TAMAMLANMADI)

Koşum **harness tarafından durduruldu**: arka plan kabuğu (bln8lyq6u, `caffeinate -i -s
venv/bin/python -u scratch/anka_a1_kos.py`) sistem belleği kritik düzeye indiği için
**kill** edildi. Bu bir eğitim hatası **değildir** (çıktı dosyasında Traceback/RuntimeError
yok), ama koşum **bitmedi**: durdurulma anındaki son adım **770/48.828** = %1.58.

| sinyal | ölçüm | okuma |
|---|---|---|
| son log satırı | `Adım  770/48828 | Kayıp (Loss): 4.1808 | Adım Süresi: 1.23s | Toplam Süre: 762` | adım 770 |
| son periyodik kayıt | **adım 500** (68. satır) | 501–770 arası adım **kayıp** (~4.7 dk; ilan edilen en kötü kayıp ~6,5 dk — tasarım sınırı **aşılmadı**) |
| `data/anka_a1.pt` | **372.124.278 bayt**, mtime `16:15:35Z` | durum **kullanılabilir** (atomik yazım sayesinde) |
| `data/anka_a1.pt.tmp` | **yok** | kill yazımın ORTASINA denk gelmedi ⇒ atomiklik sarsılmadı |
| `scratch/anka_a1_kos_sonuc.json` | **yok** | başlatıcı ölçüm bloğuna **hiç ulaşmadı** ⇒ bu koşum için "sonuç dosyası" kanıtı YOKTUR |

**Yeniden başlatma kararı bende değil.** Harness açıkça "kendiliğinden yeniden başlatma"
dedi; kullanıcı onayı bekleniyor. Seçenekler: (a) 500 adımlık checkpoint'ten **devam**
(`--load-path data/anka_a1.pt`, aynı komut öneki), (b) A1'i **sıfırdan** yeniden başlat,
(c) kapsamı küçült. Üçü de T-0077'nin **kapsam/konfigürasyonunu değiştirir** ⇒ ölçüm öncesi
ilan edilen yapılandırmadan sapma olur ve **raporda ayrıca beyan edilmelidir**
([[kismi-sonuc-dosyasi-bitis-kanitlamaz]]).

## 4c. DEVAM DENEMELERI ve O6 KAPISI — 2026-09-18T17:08:44Z

**1. deneme (arka plan b943qa4go, 17:05:21Z): devam etti AMA yanlis ogrenme oraniyla.**
Log: `Mevcut model ağırlıkları … eğitim devam ettiriliyor (Resume)...` ✓ ve hemen ardindan
`(Öğrenme Oranı: 0.0002)` — ilan edilen **1e-3** yerine **2e-4**, yani **5x dusuk**.

**Kök neden (`train.py:135`, okundu):** `lr = 2e-4  # Lower learning rate when fine-tuning/resuming`.
Checkpoint varsa ve `--from-scratch` yoksa lr SESSIZCE dusuruluyor. O satir **ince ayar** icin
yazilmis; kesintiye ugramis bir **on-egitimi surdurmek** icin degil. O4/O5 kapilarim bunu
GORMEDI (resume dogruydu, kayip 4,2133 ile devam gibiydi) ⇒ ayni aile: [[sessiz-soyagaci-geri-dusmesi]] —
dallanan bir kod yolu **anlami** sessizce degistiriyor.

| islem | sonuc |
|---|---|
| kosum durduruldu | 1 adimda (log 1.503 bayt, dondu) |
| `data/anka_a1.pt` | sha256 `a48afe59856c51f2ffc0a79e667d55962a7d1628ff5c13df0db69c08aff040e5` — **DEGISMEDI** |
| `.tmp` artigi | yok |
| iptal bedeli | ~5,5 sn (ilk adim) + model yukleme |

**Duzeltme iki katmanli:** (1) KOMUT'a **acik `--lr 0.001`** — `train.py` `--lr`'yi resume
dalindan SONRA ayristirdigi icin ezer; (2) **O6 kapisi**: log'daki degeri ilan edilenle
karsilastirir, uyusmazsa **ilk checkpoint'ten once** oldurur.

**O6 pozitif kontrolu** (`scratch/anka_a1_devam_probe.py`, **DORT dal, 30 kontrol, hepsi GECTI**):
A → O4 atesler · C → O5 atesler · **D → O6 atesler (lr 0,0002)** · B → hepsi gecer. Kontrol
dosyasi iki olcum kusurunu da kayda gecirir: cocuklar aninda bitince "imza" kontrolu vakumdu;
uyku eklenince imza log'un **basligindaki komut satirinda** cikti (olcum yuzeyi kirlendi) ⇒
imza artik cocukta **calisma aninda** kuruluyor.

**Bellek tabani kusuru (kapi DEGIL artik):** ilk surumde `free+purgeable < 0,20 GB` bir
KAPIYDI ve ilk isi kosumu **reddetmek** oldu (0,099 < 0,20). Ama A1'in ilk kosumu ~0,04 GB
bos bellekle **770 adim kostu** ⇒ ilan edilen taban, CALISTIGI KANITLANMIS bir yapilandirmayi
reddediyor = **ayirt edici degil** ([[ilan-edilen-kural-ayirt-edici-olmali]]). Ayrica macOS
`Pages free`i yapisal olarak kucuk tutar. Karar: buyukluk **kayittir**, kapi degil; kayda
`kullanilabilir_gb` (free+purgeable+inactive+speculative) da eklendi.

**Sayfa boyutu kusuru (4x):** onceki betikte `sayfa.get("Page size of", 4096)` YANLIS ANAHTARDI
(vm_stat basligindaki deger ayristiricida "Mach Virtual Memory Statistics" anahtarina duser)
⇒ `anka_a1_kos_baslangic.json`'daki tum GB degerleri **4x kucuk** (bos_bellek_gb=0,01 gercekte
~0,04 GB). Yeni betik sayfa boyutunu **dogrular**, okuyamazsa fail-closed DURUR.

**2. deneme (arka plan bq3ono367):** `Resume` ✓ · `(Öğrenme Oranı: 0.001)` ✓ (O6 geçti)
· **ilk kayıp 4,3931** (adım 1, 1,7 sn) — O5 eşiği 8,0'in altında, yani **devam etti**
(sıfırdan başlasaydı 10,3997 civarı olurdu). Bekçi `scratch/anka_a1_bekci.py <log> devam`
modunda (canlılık ölçütü: ilk kayıp < 8,0 = devam etti).

## 4d. DEVAM KOSUMU TAKAS NEDENIYLE COKTU — 2026-09-18T17:14Z (olculdu, TAHMIN DEGIL)

Devam kosumu **teknik olarak ilerliyor ama pratikte durdu**. Ilk kosum 0,84–1,23 sn/adim
ile gidiyordu; devam kosumunda adim basina sure **tek yonlu bozuldu**:

| adim | adim suresi | toplam sure |
|---|---|---|
| 1 | 1,70 sn | 1,7 sn |
| 10 | 4,14 sn | 59,4 sn |
| 20 | 9,37 sn | 132,3 sn |
| **30** | **25,71 sn** | 229,1 sn |
| 40 | 4,57 sn | 334,4 sn |

**90 saniyelik pencerede YENI ADIM SATIRI YOK** (adim 30 civari) ⇒ ilerleme gercekten durdu.
Bekci iki kez atesledi: `YAVAS TEK ORNEK` (9,37 ve 25,71 sn) ve **`G6 BOZULMA` adim 40
kararli medyan 4,57 sn > 2,34**.

**Kok neden — TAKAS (swap). Makine 8,0 GB fiziksel ve bellek dolu:**

| olcum | deger |
|---|---|
| fiziksel RAM | **8,0 GB** (`sysctl hw.memsize`) |
| `Pages free` | 0,06–0,08 GB · sistem geneli boş **%12** |
| `Pages wired down` | **4,0–4,8 GB** (8 GB'in yarisi+) |
| kompresör | 1,2–2,2 GB (20 sn'de 1,10 → 2,10) |
| **Swapouts** | 20 sn'de +295.544 sayfa = **4.618 MiB ⇒ 230 MiB/s** |
| **Swapins** | 20 sn'de +277.491 sayfa = 4.336 MiB ⇒ 216 MiB/s |
| Compressions | **25,4 GB / 20 sn** · Decompressions 23,9 GB / 20 sn |
| 90 sn'de Swapouts | +1.207.924 sayfa = **18,4 GiB ⇒ ~210 MiB/s surdurulmus** |
| `train.py` (pid 67504) | RSS ~100 MiB · VSZ 467 GB · %CPU 7,5→70,3 · **STAT `U`** |

`STAT U` = kesintisiz bekleme, yani süreç **sayfalama bekliyor**; %70 CPU ile "calisiyor"
gorunmesi hesapladigi anlamina gelmiyor. RSS'in ~100 MiB olmasi MPS ayirmalarinin RSS'te
gorunmedigi anlamina da gelebilir — **hangisi oldugu OLCULMEDI, spekulasyon yazilmiyor**.

**Yetim surec DISLANDI (olculdu, `ps` ile, sandbox disinda):** tek `train.py` (pid 67504,
ppid 67500 = devam baslaticisi). Ilk kosumun sureci **olmus** — iki egitim ust uste binmiyor,
yani yavaslik "iki model ayni GPU'da" ile aciklanamaz.

**Komsu yuk (T-0052 imzasi):** `WindowServer` %24,4 · `WallpaperAerialsExtension` %15,6
(video duvar kagidi, 21 saattir) · `VTDecoderXPCService` (video cozme) · `Antigravity Helper
(Renderer)` %19,1 · `claude` %15,8. Bunlar GPU/bellek tuketir; kapatma yetkim **yok**.

**Butce asimi (beyan):** ilan edilen ~10,58 saat / 0,780 sn-adim. Medyan 4,57 ⇒ **~61 saat**;
en kotu ornek 25,71 ⇒ ~345 saat. Ilk kosumun kendisi de ayni baski altinda yavaslamisti
(0,84 → 1,06 → 1,23 sn) ve ardindan harness onu bellek nedeniyle oldurdu ⇒ **baski hic
temizlenmedi, devam kosumu kotu rejimde basladi.**

**Durum:** koşum **durdurulmadi** — ilerliyor ve `data/anka_a1.pt` (adim 500) bozulmadi.
`--save-every 500` sayaci BU kosumun kendi adimina gore isledigi icin bir sonraki periyodik
kayit devam-adimi 500'de (kuresel 1000) olacak; o ana kadar diskte kuresel-500 durumu kalir.
Karar kullanicida: durdur/yeniden baslat/birak. **Bu model kendi basina yeniden baslatmaz.**

## 4e. YALIN YENIDEN BASLATMA ve OLCULEN MEKANIZMA — 2026-09-18T17:18Z

**Kullanici karari (soru cevabi):** "Durdur + yalin baslat (Onerilen)" · makine sorusu:
"Kapatabilirim".

**Durdurma (olculdu):** `train.py` pid 67504 oldu; `ps` ile DOGRULANDI (harness daha once
kabugu oldurup torunu birakabilir — bu kez birakmadi). `data/anka_a1.pt` sha256
`a48afe59856c51f2ffc0a79e667d55962a7d1628ff5c13df0db69c08aff040e5` **degismedi**, `.tmp` yok.
Kosum diske hic yazmadi (ilk periyodik kayit devam-adimi 500'deydi; ~40'taydik).

### Mekanizma — TAHMIN DEGIL, OLCUM

Egitim oldugunde wired bellek **4,0-4,8 GB → 1,255 GB** dustu (egitim ~3,9 GB wired tutuyordu).
Nereye gittigi `vmmap` ile bulundu (pid 68287):

| bolge | olcum |
|---|---|
| `owned unmapped (graphics)` | **4,3 GB sanal · 2,0 GB yerlesik · 2,3 GB TAKASTA** |
| `TOTAL` (vmmap) | **2,4 GB yerlesik** |
| `ps` RSS | **75 MiB** |

⇒ **MPS/GPU ayirmalari `ps` RSS'te GORUNMEZ; ayak izi `vmmap`'te olculur.** Ayni surec icin
`ps` 75 MiB derken gercek yerlesik 2,4 GB. Bu, "RSS ~100 MiB, o halde bellek sorunu degil"
okumasini **curutur**. Takasin mekanizmasi: MPS arenasinin 2,3 GB'i takasta, her adim o
sayfalari geri cagiriyor.

**Oran parametresinin TABANI olculdu:** `torch.mps.recommended_max_memory() = 5,33 GB`.
`PYTORCH_MPS_HIGH_WATERMARK_RATIO=1.0` ⇒ tavan **5,33 GB** (varsayilan 1,7 ⇒ 9,07 GB).
Yani "1.0 = kucuk" sanmak yanlis; 8 GB'lik makinede 5,33 GB tavan fiilen sinirsizdir.
Daha asagisi (or. 0,25 ⇒ ~1,33 GB) arenayi tamamen RAM'e sigdirirdi — **denenmedi**, secenek
olarak kayitta.

### Yalin kosum: durum

`scratch/anka_a1_yalin.py` · log `scratch/anka_a1_yalin.log` · baslangic
`..._yalin_baslangic.json` · sonuc `..._yalin_sonuc.json` (arka plan gorevi `bbh9k8mia`).
Egitim yapilandirmasi **AYNI**; degisen yalniz ortam (MPS tavanlari). Bu bir **ortam
degisikligidir** ve raporda beyan edilir.

| olcum | deger |
|---|---|
| devam kaniti | `Resume` ✓ · `(Öğrenme Oranı: 0.001)` ✓ (O6 gecti) |
| ilk kayip | **4,2214** (adim 1) ⇒ O5 gecti (sifirdan baslasaydi ~10,4) |
| adim sureleri | 1→2,34 · 10→2,16 · 20→**6,23** · 30→**1,04** · 40→**1,08** sn (40 adim/86,7 sn) |
| **O7 karari** | adim 20'de medyan **2,34** ≤ 3,12 ⇒ **gecti** (ilk ornekler isinma/arena buyumesi) |
| on-kontrol bellek | free 0,06 GB · wired 1,255 GB · kompresor 2,261 GB |
| kosum sirasinda | wired **5,24 GB** ⇒ egitim +~3,9 GB |

**O7 POZITIF KONTROL** (`scratch/anka_a1_yalin_probe.py`, gercek `main()` yamali globallerle):
**14/14 GECTI** — A dali (4,57 sn) O7 ATESLEDI ve kosum tamamlanmadi; B dali (0,90 sn) gecti ve
tamamlandi; O4/O5/O6 iki dalda da dogru; checkpoint iki dalda da DEGISMEDI.
**Kontrolun ILK surumu VAKUMDU ve bunu kendisi ortaya cikardi:** cocuk 1,5 sn'de bitince bekci
dongusu 2 sn'lik ilk yoklamada `poll()` gorup cikti ⇒ HICBIR kapi degerlendirilmedi
(`lr_kapisi=resume_kapisi=canlilik_kapisi=None`). "Gecmedi" degil, **"olculmedi"**. Duzeltme:
cocuk yoklama penceresinden uzun yasar (4,5 sn). Ayni sinif: [[vakum-kanarya-satir-numarasi]].

**BEYAN EDILEN KISITLILIK:** O7 **BIR KEZ** degerlendirilir (kosul ilk saglandiginda). Adim
20'de gecip sonra bozulan bir kosumu **oldurmez**. Surekli gozetim canli bekctedir: `G6 BOZULMA`
esigi 2,34 (=3x beklenen), O7 tavanindan (3,12) **once** uyarir ⇒ bozulma yine de gorulur.

## 4f. TAVAN MERDİVENİ — "TAVANI DÜŞÜR" HİPOTEZİ ÖLÇÜMLE ÇÜRÜDÜ (2026-09-18)

Kullanıcı kararı "yalın başlat" (MPS tavanlarını düşür) idi. **Dört tavan denendi, dördü de
OOM verdi; hipotez ölçümle çürüdü.** Ham kanıt: `scratch/anka_a1_yalin{2,3,4}.log`.

| tavan (ratio) | tavan (GB) | sonuç | OOM mesajındaki sayılar |
|---|---|---|---|
| 0,5 | 2,67 | **OOM** | allocated 3,05 GiB · other 276,69 MiB |
| 0,8 | 4,27 | **OOM** | allocated 4,05 GiB · other 647,83 MiB · istek 256,66 MiB |
| 1,0 | 5,33 | **OOM** | allocated 5,05 GiB · other 647,83 MiB · istek 256 B |
| 1,1 | 5,87 | **OOM** | sürücü tepesi 5,695'e ÇIKTI, sonra OOM |
| 1,2 | 6,40 | **geçti** | 12 adım, sürücü 4,695'te sabit |
| 1,3 | 6,93 | **geçti** | 12 adım, sürücü 4,451'de sabit |

**Mekanizma (ölçüldü, `scratch/anka_a1_bellek_olcum.py` — train.py:171-201 döngüsünün aynı
işlem sırası, faz faz `torch.mps.current_allocated_memory()`):** ayırıcı önbelleği **tavana
kadar** büyütür; yeni blok isteğinde kontrol `allocated + other + istek` toplamına bakar.
`other allocations` (0,648 GB) ayırıcı bütçesinin **DIŞINDADIR** ⇒ fiilen kullanılabilir yer
`tavan − 0,648`'dir ve önbellek tavana dayandığı için istek her seferinde duvara çarpar.
**LOW watermark'ı 0,1'e indirmek HİÇBİR ŞEY değiştirmedi** (aynı sayılar, aynı adım) ⇒
önbellek talep üzerine **bırakılmıyor**.

**Canlı küme (önbellek HARİÇ, ölçüldü):** kurulum 0,699 · forward sonrası 2,86–3,27 ·
backward sonrası 2,24–2,62 · step sonrası 2,24–2,62 GB.
**Driver plateau 4,2–4,7 GB ve TAVANDAN BAĞIMSIZ** (1,2'de 4,695 · 1,3'te 4,451).
⇒ **Tavanı düşürmek ayak izini DÜŞÜRMÜYOR**, yalnızca ayırıcıyı OOM'a zorluyor.
Gerçek ayak izi ~4,5 GB'dir; "yalın" ayar bir **tasarruf değil**, ölçülmüş bir yanılgıydı.

**Ölçüm öncesi ilan edilen kural ihlali (kayda geçiyor):** 1,0 denemesi için "OOM olursa
sonraki adım 0,9'dur (tahmin değil, sıra)" yazmıştım; ölçüm 0,9'un (4,80 GB) gereken 4,96'nın
ALTINDA kaldığını gösterdi ⇒ **sıra atlandı, gerekçesi ölçümle yazıldı** (aynı OOM'u almak
ölçüm değil tekrar olurdu). Bu, [[ilan-edilen-kural-ayirt-edici-olmali]] ailesindendir.

**5. deneme (bu koşum):** `MPS_HIGH=1.3` (6,93 GB) / `MPS_LOW=0.5`. Tavan ölçülen plateau'nun
ÜSTÜNE konuldu; **tasarruf iddiası yok**, tek işi sınırsız (0,0) hâle karşı üst sınır koymak.
Gerçek bekçi **O7**'dir: arena RAM'e sığmazsa adım süresi bozulur ve O7 süreci öldürür.
Dosyalar: `scratch/anka_a1_yalin5.log` · `..._yalin5_baslangic.json` · `..._yalin5_sonuc.json`.
(0,5 · 0,8 · 1,0 denemelerinin dosyaları **OOM kanıtı** olarak üzerine yazılmadan duruyor.)

**Yan bulgu (T-0073 ailesine eklenir):** `ank…_bellek_olcum.py` ilk sürümünde forward **sonrası**
örnekleme yapıldığı için OOM, ölçüm betiğinin İÇİNDE de yeniden üretildi — yani hata
yapılandırmaya değil **adımın kendisine** aittir; betik train.py'nin döngüsünü birebir
kopyaladığı için ayırıcı durumu da birebir aynı yerde patlıyor.

## 4g. 5. DENEME CANLI — BEYAN EDİLEN HIZA DÖNÜLDÜ (2026-09-18T17:5xZ)

`caffeinate -i -s`, sandbox DIŞINDA. Bekçi `scratch/anka_a1_bekci.py <log> devam` (Monitor).

| ölçüm | değer |
|---|---|
| devam kanıtı | `Resume` ✓ · `(Öğrenme Oranı: 0.001)` ✓ (O4/O6 geçti) |
| ilk kayıp | **4,3107** (adım 1, 1,64 sn) ⇒ O5 geçti (sıfırdan olsaydı ~10,4) |
| **adım süresi** | adım 40→0,73 · 50→0,76 · 60→0,79 sn ⇒ **beyan edilen 0,780 sn/adım ile uyumlu** |
| O7 | medyan 3,12'nin çok altında ⇒ ateşlemedi |
| G6 | 2,34'ün altında ⇒ ateşlemedi |

**Karşılaştırma (anlamlı olan budur):** ilk koşum 0,84 sn/adım · takas çöken devam koşumu
4,57 sn (medyan) · **bu koşum 0,73–0,79 sn/adım**. Yani makinenin yükü (kullanıcı video duvar
kâğıdını kapattı; `WallpaperAerialsExtension`/`VTDecoderXPCService` artık yok) düştükten SONRA
hız beyan edilen değere **döndü**. Bu, 4d'de ölçülen çöküşün nedeninin eğitim değil **komşu
yük** olduğunu bağımsız olarak doğrular ([[mps-yuku-adim-suresini-bozar]]).

Beklenen bitiş: 48.328 × ~0,78 sn ≈ **10,5 saat**. Kapanış yine üç sinyalle yapılacak.

**30 dk sonraki canlı durum (adım 1760/48.328 · %3,6):**

| ölçüm | değer |
|---|---|
| periyodik kayıtlar | adım **500 · 1000 · 1500** yazıldı (atomik) ⇒ ilerleme diske bağlandı |
| `data/anka_a1.pt` | 355 MiB · `.tmp` artığı yok |
| adım süresi gidişi | 0,73–0,79 (adım 60) → 0,93–0,97 (adım ~1500) → **1,21–1,38** (son 3 örnek) |
| son-10 medyanı | ~1,0 sn ⇒ O7 eşiği 3,12'nin **altında**, ateşlemedi |

**Takas ölçümü (adım ~3540, 20 sn penceresi — ÇÖKÜŞ REJİMİ DEĞİL):**

| ölçüm | bu koşum | çöken koşum (4d) |
|---|---|---|
| Swapouts | **11,8 MiB/s** | 210 MiB/s (sürdürülmüş) |
| Swapins | 5,2 MiB/s | 216 MiB/s |
| wired | 5,87 GB | 4,0–4,8 GB (dalgalı) |
| free | 0,06 GB | 0,06 GB |

⇒ **Takas 18 kat düşük** ve adım süresi kararlı (0,94–1,39 sn). `free`in 0,06 GB olması
YANILTICI: arena artık **RAM'de yerleşik** (wired 5,87 GB), sayfalanmıyor. Yani çöküşün nedeni
"boş bellek azlığı" değil, **arenanın RAM'de tutulamamasıydı**; komşu yük kalkınca tutulabiliyor.
Not: `ps` anlık `STAT` alanı bu koşumda `UN` okundu — **anlık durumdan süreç hükmü çıkarılmaz**
([[anlik-olcumden-surec-durumu-cikarma]]); yargı adım süresinden verilir.
Risk kaydı: harness ilk koşumu bellek baskısı nedeniyle öldürmüştü; periyodik kayıt (~9 dk)
kaybı sınırlıyor.

**ÖLÇÜM ALETİ KUSURU (kendi hatam, kayda geçiyor):** süreç kontrolünü
`ps -Ao pid,command | grep "train.py --pretrain"` ile yaptım ve **"SÜREÇ YOK"** okudum — koşum
sağlıklıyken. Neden: `ps -o command` sütunu **kırpıyor** (genişlik sınırı), tam komut satırı
çıktıya girmiyor ⇒ grep tutmuyor. Doğrusu `-ww` (kırpma yok) veya kimliği **pid** üzerinden
doğrulamak. Aynı sınıf: [[anlik-olcumden-surec-durumu-cikarma]] — aletin ürettiği boş sonuç,
ölçülen şeyin yokluğu DEĞİLDİR. Bu vakada hükmü log'un mtime'ı (12 sn önce) düzeltti.

**Adım 5260 (%10,9) · canlı:** kayıp 3,6738 · adım süresi 0,89–1,32 sn · 10 periyodik kayıt
(son 22:07'de adım 5500) · süreç pid 69283 (`UN`, %7,5 CPU) · log 12 sn önce yazıldı.

### TEK ÖRNEK SIVRİ UCU — adım 5750, 12,08 sn (2026-09-18T22:4xZ)

Bekçi `YAVAS TEK ORNEK` verdi (>7,80 = beklenenin 10 katı). **Bu bir eşik uyarısıdır, öldürme
DEĞİL** — öldüren kapı **G6 medyanıdır** (son 5 örnek > 2,34) ve **O7 medyanıdır** (son 10 > 3,12).

| ölçüm | değer |
|---|---|
| sıvri uç | adım 5750: **12,08 sn** — sonraki adımlar 1,21 · 1,27 · 1,27 sn |
| son-12 adım | 0,92–1,38 sn, tek sıvri uç dışında |
| **son-10 medyan** | **~1,2 sn** ⇒ G6 (2,34) ve O7 (3,12) ateşlemedi |
| takas | swapouts 34,3 MiB/s · swapins 23,9 MiB/s (çöküş rejimi 210/216) |
| wired · free · kompresör | 6,06 GB · 0,06 GB · 0,79 GB |
| komşu yük (o an) | `mediaanalysisd` %43 · `intelligencetasksd` %37 · `parsecd` %44 · `Terminal` %74 |

**Çöküş rejiminden ayırt eden ölçüt SÜREKLİLİKTİR, sivri uç değil:** 4d'deki çöküşte adım
süresi **tek yönlü** bozuldu (1,70 → 4,14 → 9,37 → 25,71) ve 90 s'de **hiç yeni adım** gelmedi.
Burada sıvri uçtan sonra hız **geri döndü** (1,21–1,27). Tek örnekle hüküm verilmez
([[iki-sayi-celisiyor-sanma-once-kume]] ailesi: önce KÜMEYİ doğrula).
Takas önceki ölçüme göre 3 kat arttı (11,8 → 34,3 MiB/s) ama çöküşün %16'sı; izleniyor.

### G6 İKİ KEZ ATEŞLEDİ — AMA GEÇİCİ BANTMIŞ (2026-09-18T22:2x–22:3xZ, ÖLÇÜLDÜ)

Bekçi adım 5890 ve 5930'da `G6 BOZULMA` verdi (son-5 medyan 2,55 > 2,34). **Bu kez hüküm
vermeden önce İKİ pencere ölçtüm** ve iki pencere ZIT okundu:

| ölçüm | pencere 1 | pencere 2 (30 sn sonra) |
|---|---|---|
| Swapouts | **203,7 MiB/s** | **15,1 MiB/s** |
| Swapins | 189,0 MiB/s | 14,1 MiB/s |
| Compressions | 20,43 GB/20sn | — |
| kompresör | 0,74 GB | 0,66 GB |

Pencere 1 **çöküş rejiminin ta kendisiydi** (4d: 210/216 MiB/s · 25,4 GB/20sn). Pencere 2'de
takas **13 kat düştü** ve adım süreleri başlangıç rejimine döndü: son-10
`0,97 · 2,73 · 4,07 · 1,07 · 0,99 · 1,02 · 1,00 · 0,87 · 0,82 · 0,86` ⇒ **medyan 0,99 sn**.

**O anki komşu yük (ölçüldü):** `textunderstandingd` %42,5 · `spotlightknowledged` %22,2 ·
`mediaanalysisd` %16,3 · `claude` %25,2 · `Antigravity language_server` %15,6 · `git` %10.
Bunlar macOS arka plan servisleri + iki ajan oturumu. **Sıvri uçların kaynağı bu servisler
olabilir ama ÖLÇMEDİM — atıf yazılmıyor.**

**Ders (bu gece iki kez işe yaradı):** tek pencere ölçümü bu makinede **kapı ateşletmeye
yetiyor ama hüküm vermeye yetmiyor**. 4d'de çöküş *sürekliydi* (90 s'de sıfır adım); burada
geçici. Ayırt edici ölçüt **iki ardışık pencerenin aynı yönü göstermesi**dir.

**Ölçüm hatam (kayda geçiyor):** "kalan adım = 48328 − 601" yazdım. 601, **log satırı**
sayısıdır; log her **10 adımda** bir satır basar ⇒ gerçek adım **6010**. Doğrusu: kalan 42.318
adım ⇒ son-10 medyanla **11,7 saat** · koşum ortalamasıyla (1,090 sn/adım) **12,8 saat**.
Tahmini bitiş **19 Eyl ~10:30–11:30**. Aynı sınıf: [[iki-sayi-celisiyor-sanma-once-kume]]
(önce KÜMEYİ, yani neyi saydığını doğrula).

**Adım 6010 (%12,4) · kararlı:** ilk 200 satırın medyanı 0,97 sn — yani hız **başlangıç
rejiminde**. `[ckpt] adım 6000` yazıldı (11. periyodik kayıt). O7 ateşlemedi.

**Sürüklenme kayda geçiyor (ilk koşumun imzası):** ilk koşumda da 0,84 → 1,06 → 1,23 olmuştu.
Şu anki hızda (1,3 sn/adım) koşum ~**17,5 saat** sürer — beyan edilen 10,58 saatin 1,65 katı,
ama O7'nin 4 kat sınırının **altında**. O7 medyanı son 10 örneğe baktığı için bu sürüklenmeyi
**öldürmez**; 3,12'yi aşarsa öldürür. Rapor bu farkı ham medyanla verecek.

## 5. Kapanış (AÇIK — koşum devam ediyor)

Devam koşumu (`bq3ono367`, 48.328 adım, adım-500 checkpoint'inden) **sürüyor**; bu yüzden
`scratch/anka_a1_devam_sonuc.json` henüz **yazılmadı** ve kapanış **yapılamaz**. T-0077
`claimed` kalıyor; `data/` ve `scratch/` kiraları **bırakılmadı**. Koşum gerçekten bittiğinde
kapanış üç sinyalle ölçülecek: mesaj + kiralar boş + `changed_files` **iki yönlü**
([[kismi-sonuc-dosyasi-bitis-kanitlamaz]]).

**İlan edilen yapılandırmadan sapmalar (raporda ayrıca beyan edilmeli):** kesinti nedeniyle
koşum **iki parçalıdır** (0–500 · 500–48.828) ve AdamW momentleri kaydedilmediği için ikinci
parça **ağırlıktan ısınan** bir yeniden başlangıçtır (bit-bit sürekli bir koşumla kıyaslanamaz).

---

## 6. Gece nöbeti · bekçi aracı düzeltmesi (18 Eyl 22:4x–23:xx)

**ARACIN KUSURU, ÖLÇÜLEN ŞEYİN DEĞİL.** Gece boyu bekçiyi her 30 dakikada yeniden kurmam
gerekiyor (Monitor penceresi 30 dk). Bekçi log'u **baştan** okuyordu ⇒ **her kurulumda aynı dört
tarihsel olay** yeniden basılıyordu: `ILK ADIM: adim=1`, `YAVAS TEK ORNEK: adim=5750`,
`G6 BOZULMA: adim=5890` ve `adim=5930`. Koşum o anda ~9.040'ta olduğu için bu satırlar **bayat**;
ancak gece 03:00'te bayat bir `G6 BOZULMA`yı canlı bozulmayla karıştırmak gerçek bir risktir.

**Düzeltme:** `scratch/anka_a1_bekci.py` üçüncü argüman olarak **başlangıç bayt ofseti** alır
(`BASLANGIC`, varsayılan 0). Çağıran, log'un o anki boyutunu verir. Ofset > 0 iken canlılık
satırı **basılmaz** (o adım koşumun ilk adımı değil, devamıdır) ama `ilk_bildirildi=True`
yapılır ki **SESSIZLIK bekçisi ofsetli kurulumda da çalışsın** — aksi hâlde düzeltme sessizlik
tespitini kör ederdi.

**İki yönlü sınama (ikisi de geçti — yalnız "artık basmıyor" demek kör etmeyi de kapsar):**

| Dal | Kurulum | Beklenen | Ölçülen |
|---|---|---|---|
| negatif | kopya log, ofset **yok** | dört eski olay basılmalı | dördü de basıldı (kusur gerçekti) |
| pozitif | kopya log, ofset = boyut, + sonradan eklenen 12 sn'lik adım satırı | yalnız YENİ olay | `BEKCI KURULDU: ofset=82.618` + `YAVAS TEK ORNEK: adim=9040 sn=12.0` — eski olaylar **basılmadı** |

`py_compile` geçti ([[scratch-dosyasi-test-yuzeyinde]] kuralı). Bekçi çıktısındaki
`nice(5) failed: operation not permitted` satırı **sandbox** gürültüsüdür, betikten gelmiyor.

**Canlı durum (20:13 UTC / 23:13 yerel):** adım **9030/48.328 (%18,7)** · son-10 medyan
**0,80 sn** (min 0,77 / maks 0,89) · 17 periyodik kayıt · canlılık 45 sn'de **+512 bayt** ·
sonuç dosyası yok · O7 ve G6 ateşlemedi. Kalan 39.298 adım × ~0,84 sn ≈ **9,2 saat ⇒ tahmini
bitiş ~08:25 yerel**; sabah raporu (08:12) koşumun son demlerinde, sonuç raporu (11:47) bitişten
sonra ateşlenecek. **`ps` bu oturumda sandbox tarafından engellendi** (`operation not permitted`)
⇒ canlılık **log büyümesiyle** ölçüldü ([[anlik-olcumden-surec-durumu-cikarma]]).

**TAHMİN HATASI VE DÜZELTMESİ (kayıt için).** 23:44 UTC'de "kalan ~35.008 adım" diyerek bitişi
~12:00–12:30 yerel'e koydum. Doğrusu `kalan = 48.328 − 23.320 = **25.008**` ⇒ 10.000 adımlık
(≈2,8 saat) bir kayma. **Formülü yazmadan oran kurmak yine yanılttı**
([[sure-hesabinda-boyut-kontrolu]] aynı ailede ikinci kez). Düzeltilmiş hesap (00:15 UTC):
son 2 sa 38 dk'da 14.900 → 25.150 = 10.250 adım / 9.380 sn = **0,915 sn/adım**; kalan
**23.178** adım × 0,915 ≈ **5,9 saat ⇒ bitiş ~06:05 UTC / 09:05 yerel**. Bu, 08:12 raporunun
koşumu sürerken (~%85), 11:47 raporunun bitişten **sonra** ateşleneceği anlamına gelir; 13:12'ye
kurulan `e6808b6e` artık **yedek**tir, birincil değil.

**Adım süresi kayması (ölçüldü, geçici):** 5 dk'lık kovalar 23:1x'te `0,844 → 1,084`'e çıktı
(%28), sonra `0,912`'ye ve son-10 medyan **0,82**'ye geri indi ⇒ monoton bozulma değil, geçici
tepe. Aynı 3 dakikada `Pages free` 0,062 → 0,577 GB, `wired` 6,33 → 5,42 GB oynadı ⇒ **tek
`vm_stat` örneğinden bellek hükmü çıkarılamaz** ([[anlik-olcumden-surec-durumu-cikarma]] onuncu
vakası). Yerleşik bant ~0,95–1,0 sn/adım; O7 (3,12) ve G6 (2,34) eşiklerinin çok altında.

---

## 7. KAPANIŞ — koşum TAMAMLANDI (19 Eyl 06:16:33Z)

`EĞİTİM BAŞARIYLA TAMAMLANDI` **görüldü** (bekçi bu satırı görünce çıktı) ve
`scratch/anka_a1_yalin5_sonuc.json` **yazıldı** ⇒ kapanışın iki maddesi sağlandı.

**Ham sayılar (kaynaklardan yeniden hesaplandı, elle yazılmadı):**
`rc=0` · `duvar_sn=45.380,9` (12,61 saat) · 4.833 adım örneği · **medyan 0,86 sn/adım**
(oran **1,103×**, ilan 0,780) · en yavaş örnek **12,08 sn** (adım 5750) · **97** `[ckpt]`
(ilk 96'sı 500'ün tam katı, 97. kapanış kaydı 48.328) · kayıp 4,3107 → 3,2456 ·
`tmp_artigi=False` · `hedef_degisti=True` · hata izi **0** · `0,0000` **yok**.
Artefakt: `data/anka_a1.pt` 372.124.278 bayt ·
sha256 `f32d492d9361c668bc86e4e4b0f99f5c473af552486837452475f8b0aa3eb5b4` ·
101 anahtar · gömme **(32.852, 768)** = kanonik V ile birebir · NaN/Inf **yok**.

**Kapılar:** G2 ✓ · G3 ✓ (mtime örneklemesi 2 nokta) · **G4 birebir** (48.828 adım ·
99.999.744 jeton) · G5 ✓ · G6 ✓ (4 örnek, ham `0,92·0,74·0,73·0,76`, std medyan 0,75 — ölçüt
zayıf, asıl bekçi O7) · **G1 ve G7 ölçütleri koşumun tasarımıyla ÇELİŞİYOR** ⇒
[[ilan-edilen-kural-ayirt-edici-olmali]] gereği "GEÇTİ" yazılmadı, beyan edildi:
sıfırdan koşum için yazılmış olan G1 bandı (ln V=10,3997) ve G7'nin "hedef varsa başlama" maddesi,
**devralma (Resume)** kararıyla geçersizdir — devralma sessiz değildi (log'da `Resume`, 97
atomik kayıt).

**Beyan edilen sapmalar:** (1) koşum **çok parçalı** — 0–500 devralındı, bu parça 48.328 ekledi;
OOM ile ölen 0,5/0,8/1,0/1,1 tavan denemeleri **kayıt yapamadı** ve checkpoint sha256'sı her
iptalden sonra `a48afe…` olarak değişmediği doğrulandı; (2) **ağırlıktan ısınan** yeniden
başlatma (AdamW momentleri kaydedilmiyor); (3) **ortam değişikliği**
(`PYTORCH_MPS_HIGH_WATERMARK_RATIO=1.3` / `LOW=0.5`).

**Ölçüm araçlarımın iki kusuru:** (a) G6 medyanı iki farklı konvansiyonla 0,74/0,76 çıktı —
standart medyan 0,75; launcher'ın O7'si **üst-orta** konvansiyonu kullanıyor ve bu yazılı
değildi (etkisi sıfır, ~1,3 ≪ 3,12, ama beyan edildi); (b) rapor betiği JSON'u yazdıktan
**sonra** çöktü ⇒ "başarılı ama `rc≠0`" — düzeltilip `rc=0` doğrulandı.

**Rapor:** `data/eval/t0077_a1_pretrain_2026-09-19.md` + `.json` (JSON betikle üretildi:
`scratch/t0077_kapanis_olcum.py`).

**Açık kalan iş (bu görevin dışı):** K8/TEMSİL bedeli — özel ad üretememe (%7,94) A1 sonrası
ölçülmedi; A1 sonrası kalitatif çıkarım denetimi yapılmadı.
