# .agent-bus/notes/T-0041.md

## T-0041 — chat_balanced üreticisi: sözlük+mod parametresi, donmuş yazım kapısı, meta öz-tutarlılığı (saf KOD)

**Durum (2026-09-16T07:34:20Z itibariyle):** TAMAMLANDI — PASS

---

### 1. Özet ve Hedef
`scripts/prepare_chat_balanced_dataset.py` betiğinde (`data/train_chat_balanced.bin` üreticisi) saf kod iyileştirmeleri yapıldı:
- Sözlük ve mod (`vocab_path: str = "data/rebuild/vocab_base_32852.json"`, `literal_entity_mode: bool = True`, `save_vocab_to: str | None = None`) parametrik hale getirildi.
- Donmuş `data/vocab.json` kütüğüne kontrolsüz geri yazma kaldırıldı. `src.llm.frozen_guard.is_frozen_path` tek otoritesi ile donmuş yola geri yazım engellendi (varsayılan: yazılmaz).
- Eksik olan 9. bileşen (`turk_tarihi_sft`) için `build_composition(parts: Dict[str, int]) -> Dict[str, int]` saf fonksiyonu tanımlandı.
- Öz-tutarlılık assert'i: `sum(build_composition(...).values()) == total_records` eklendi.
- **KOD KOŞULMADI:** Betik çalıştırılmadı, hiçbir `.bin` veya `.meta.json` üretilmedi ya da mtime'ı değiştirilmedi.

---

### 2. Uygulanan Değişiklikler

1. **D1: Sözlük ve Mod Parametrik Hale Getirildi:**
   ```python
   def main(
       vocab_path: str = "data/rebuild/vocab_base_32852.json",
       literal_entity_mode: bool = True,
       save_vocab_to: str | None = None
   ):
       ...
       tokenizer = KristalTokenizer(compiler, vocab, literal_entity_mode=literal_entity_mode)
   ```
2. **D2: Donmuş Sözlük Koruması (`frozen_guard` Entegrasyonu):**
   - Fonksiyon girişinde `save_vocab_to` belirtilmişse `is_frozen_path(save_vocab_to)` denetlenir ve donmuşsa derhal `RuntimeError` fırlatılır.
   - Varsayılanda `save_vocab_to=None` olduğu için `data/vocab.json`'a yazma yapılmaz.
   - `output_bin` için donmuş yol koruması eklendi.
3. **D3: Meta Kompozisyonu ve Öz-Tutarlılık:**
   - `build_composition` saf fonksiyonu 9 bileşenin tamamını (`turk_tarihi_sft` dahil) yapılandırır.
   - `assert sum(composition.values()) == len(all_records)` ile tam eşitlik teminat altına alındı.

---

### 3. Kabul Kriterleri Doğrulama Sonuçları

| No | Kriter | Durum | Ölçüm / Komut |
|---|---|---|---|
| a | `literal_entity_mode` parametresi ve tokenizer kurulumu | TAM | `inspect.signature(m.main)` parametreyi içeriyor; `KristalTokenizer(..., literal_entity_mode=literal_entity_mode)` ile kuruluyor. |
| b | Gömülü desen listesi yok; `frozen_guard` kullanılıyor | TAM | `from src.llm.frozen_guard import is_frozen_path` çağrılıyor; kodda `data/*.bin` gömülü listesi yok. |
| c | Donmuş yola yazma engeli ve varsayılan davranış | TAM | `save_vocab_to='data/vocab.json'` ile `RuntimeError` fırlatıldı; varsayılanda `data/vocab.json`'a yazılmıyor. |
| d | `build_composition()` saf fonksiyonu ve öz-tutarlılık | TAM | 9 anahtar dönüyor; 9 anahtar toplamı 24,375 == `total_records` (assert geçti). |
| e | Test takımı regresyonu (SANDBOX DIŞINDA) | TAM | `./venv/bin/pytest tests/ -q` -> Önce: 173 passed, Sonra: 173 passed (0 failure). |
| f | Kanaryalar ve veri bütünlüğü | TAM | `data/*.bin` sayısı: 14; `train_chat_balanced.bin` sha: `3b2b9cd3cf3f4714...`; `data/vocab.json` sha: `4b40eebee9c996c8...`; `cot_vault.jsonl` satır: 91. |
| g | Betik koşulmadı kanıtı | TAM | `data/train_chat_balanced.bin` ve `.meta.json` mtime'ları değişmedi (1789286774.93). |
| h | Tamamlandı mesajı ve `changed_files` | TAM | Rapor ve bus_send mesajı gönderildi. |

---

### 4. Anlık Dosya Hash'leri (Raporlama Anı)

- `scripts/prepare_chat_balanced_dataset.py`: `9f2f9621107786720ddf71fd98e26af79762360a7b05ee8f744b713b7e78b0d1`
- `data/vocab.json`: `4b40eebee9c996c8dd3150f6b3c8896de85764ee1cf0b08e6c636e5c002a0e69` (DEĞİŞMEDİ)
- `data/train_chat_balanced.bin`: `3b2b9cd3cf3f47147c6c30b1705c812dfa7a8a287788269a6fa73dc94fccbf10` (DEĞİŞMEDİ)
- `data/train_chat_balanced.bin.meta.json`: `f5ca63fdc62422ee7bd8103ae21be353410b5732cc3cbe12e219b9d19b21b418` (DEĞİŞMEDİ)
- `data/pedagogy/cot_vault.jsonl`: `97b06fe5befd6613b520d28dc20a74deaeb30e4e8f7e6675a23720a019ff5619` (DEĞİŞMEDİ)
