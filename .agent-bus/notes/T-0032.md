# T-0032 Yürütüm Notu: RAG Pipeline'ı Devreye Alma

**Tarih:** 2026-09-15
**Yürütücü:** antigravity
**Durum (15:23:00Z itibariyle):** Tamamlandı — Tüm kanonik birimler çıkarıldı, epistemic_agent bağlandı, 6 zorunlu test ve mutant probe geçti, 141/141 pytest testi yeşil.

---

## 1. Problem Tespiti ve Gerekçe

`src/rag/rag_pipeline.py` daha önce derlenmekte fakat hiçbir yerde kullanılmamaktaydı:
1. **Modüler Değil:** Tüm mantık `main()` içinde monolitik olarak bulunmaktaydı, dışarıdan import edilemiyordu.
2. **Kapsamsız:** Test kapsamı sıfırdı (`tests/test_phase2.py:86::test_rag_pipeline_integration` gerçekte `rag_pipeline`'ı import etmiyor, yalnızca `VectorMemory` ve tokenizer'ı sınırlı test ediyordu).
3. **Eşik Kapısı Eksikliği:** `src/rag/epistemic_agent.py:301` koşullamayı `match_score >= 0.40` kapısına bağlarken, `src/rag/rag_pipeline.py:120` hiçbir kapı olmadan en iyi dökümana koşulluyordu (halüsinasyona açık).

Kullanıcı kararı (15 Eyl 2026) doğrultusunda dosya silinmeyip, README mimarisine ve "durust cekinme (abstain)" hedefine uygun biçimde tek kanonik arama-koşullama-üretim dikişi olarak devreye alındı.

---

## 2. Yapılan İcraat ve Kanonik Birimler

### a) `src/rag/rag_pipeline.py` Refaktörü
Aşağıdaki saf ve import edilebilir birimler çıkarıldı:
* `RAG_MATCH_THRESHOLD: float = 0.40`: Çıplak sihirli sayı yerine adlandırılmış tek eşik sabiti.
* `build_query_vectors(query, tokenizer)` -> `(query_token_ids, query_tags, dense_vec, sparse_vec)`
* `retrieve_context(memory, dense, sparse, query_tags, top_k=1)` -> `Optional[Dict[str, Any]]`
* `is_context_usable(match_score, threshold=RAG_MATCH_THRESHOLD) -> bool` -> `float(match_score) >= float(threshold)`
* `build_rag_prompt_tokens(tokenizer, vocab, query, doc_text=None, instruction=...) -> List[int]`: Kanonik zarf ve `<OUTPUT>` dilimleme.
* `greedy_decode(model, token_ids, eos_id, device, max_new_tokens=60, rep_window=12, rep_penalty=1.5, max_len=128) -> List[int]`: Deterministik argmax decoding, repetition penalty (1.5) window (12) korundu.
* `class RagPipeline`: `answer(query) -> Dict[str, Any]` (`output_tags`, `retrieved_doc`, `match_score`, `conditioned`, `generated_tokens`, `prompt_tokens`).
* `build_from_disk(...)`: `KristalLM` lazy import kalıbıyla (`from scripts.train_step_demo import KristalLM`) model ve bellek yükleme.
* `main()`: `USER_GUIDE.md:280` ekran çıktısı ve CLI sözleşmesini eksiksiz koruyan ince kabuk.

### b) `src/rag/__init__.py` Dışa Aktarımı
`RagPipeline`, `build_query_vectors`, `retrieve_context`, `is_context_usable`, `build_rag_prompt_tokens`, `greedy_decode`, `RAG_MATCH_THRESHOLD`, `build_from_disk` modül düzeyinde dışa aktarıldı ve `__all__` listesine eklendi.
* Lazy Import Kanıtı: `./venv/bin/python -c "import sys, src.rag; print('scripts.train_step_demo' in sys.modules)"` -> `False`.

### c) `src/rag/epistemic_agent.py` Entegrasyonu ve Bit-Özdeşlik
Adım 4 içindeki çıplak 0.40 eşik kontrolü `is_context_usable(match_score, RAG_MATCH_THRESHOLD)` ve `build_rag_prompt_tokens` çağrılarına bağlandı.
* Bit-Özdeşlik Ölçümü: Eşik üstü (gerçek metin), eşik altı ve sınır (0.40) vakalarında üretilen prompt token dizileri eski kod ile yeni kod arasında birebir bit-özdeştir (ölçüm kanıtı: `data/eval/t0032_rag_pipeline_service_2026-09-15.json` `bit_identity_verification` tablosu). Boş veya beyaz boşluklu belge metni senaryolarında (`doc_text=""` veya `"   "`) ölçülen bilinçli bir sapma mevcuttur: eski kod 11 tokenlik boş `<BELGE> </BELGE>` zarfı üretirken, yeni kod modeli gürültüden korumak için 9 tokenlik yalın sorgu üretmektedir; bu sapma mimari iyileştirme olarak kabul edilmiştir.

### d) `tests/test_rag_pipeline.py` (Yeni Test Paketi)
Qdrant sunucusu, MPS ve checkpoint gerektirmeyen saf mock nesneler ile 6 zorunlu test yazıldı:
1. `test_rag_pipeline_gate_low_similarity_skips_conditioning`: `match_score=0.39` -> `conditioned is False`, prompt'ta `<BELGE>` geçmez.
2. `test_rag_pipeline_gate_high_similarity_enables_conditioning`: `match_score=0.41` -> `conditioned is True`, prompt `build_rag_input` ile birebir.
3. `test_rag_pipeline_gate_boundary_threshold_40`: Sınır `0.40` -> `conditioned is True` (`>= 0.40` ajandan miras).
4. `test_rag_pipeline_greedy_decode_determinism`: İki bağımsız çağrıda birebir aynı token dizisi.
5. `test_rag_pipeline_greedy_decode_stops_at_eos_and_respects_max_len`: `eos_id`'de durma ve `max_len=128` sınırına riayet.
6. `test_rag_pipeline_negative_retrieve_none_fallback`: `retrieve_context` None döndüğünde çökme olmaz, `conditioned is False` döner.

* Mutant Probe Kanıtı: `RAG_MATCH_THRESHOLD = 0.0` yapıldığında test (i) `assert False is True` ile kırmızı (FAILED); `0.40`'a geri alındığında yeşil (PASSED).

### e) İsimlendirme Uyumu (`tests/test_phase2.py`)
Yanıltıcı `test_rag_pipeline_integration` metodunun adı `test_vector_memory_and_tokenizer_integration` olarak güncellendi. `grep -rn "def test_rag_pipeline_integration" tests/` 0 sonuç verdi.

### f) Wiki Güncellemesi (`wiki/entities/rag_pipeline.md` ve `wiki/log.md`)
Bayat prompt tarifi (`belge: ... sorgu: ...`) kanonik `<BELGE> {doc_text} </BELGE> {query}` sözleşmesine güncellendi; kaynak SHA-256 (`af796b5fec837d098e6d9ed0b25f33293f9058dbd55206fc9bc4a7ec968b3b6f`) ve 2026-09-15 tarihi işlendi.

---

## 3. Somut Artefakt Atıfları (Yol + SHA-256)

> [!NOTE]
> **Kural:** Artefakt hash atıfları RAPOR ANINDA tazelenir; tazelenemiyorsa not hash tablosu taşımaz, rapora atıf verir.
> **Ölçüm Damgası:** 2026-09-15 16:15:00Z itibariyle `shasum -a 256` komutuyla diskten ölçülmüştür.

* `src/rag/rag_pipeline.py` (sha256: `af796b5fec837d098e6d9ed0b25f33293f9058dbd55206fc9bc4a7ec968b3b6f`)
* `src/rag/epistemic_agent.py` (sha256: `9d1cc5295c7ef82ef9442843ee1f588a1d1156d1341421a82522c8077e5d0f76`)
* `src/rag/__init__.py` (sha256: `7d1568a8524ae9401f3d2967a39ae693c600a0ccca04c7ec91b1f09196ad75a8`)
* `tests/test_rag_pipeline.py` (sha256: `df4784ff949501760de53c087066d1039781586a6250dbd62b32c26aa21bdc2f`)
* `tests/test_phase2.py` (sha256: `989549c02024aa8d804cd8f5f530a656c144924d74e82346f8621f1076301b7e`)
* `wiki/entities/rag_pipeline.md` (sha256: `63571b84e476c8a6a1d00c8dcc9dde479cefeca73ac453d91d9c547dd8d6a1ac`)
* `data/pedagogy/cot_vault.jsonl` (sha256: `97b06fe5befd6613b520d28dc20a74deaeb30e4e8f7e6675a23720a019ff5619`) — Değişmez (91 satır).
* `data/future_train_vector.jsonl` (sha256: `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`) — Değişmez (0 satır).

---

## 4. Raporlanan Bulgular ve Kapsam Dışı Analizler

1. **Bulgu 1 (Eşik Kalibrasyonu):** `0.40` eşik değeri ampirik bir kalibrasyon eğrisine dayanmayan çıplak bir sabittir. Gelecek görevlerde RAG retriever doğruluk-kesinlik eğrisi (Precision-Recall tradeoff) ölçülerek kalibre edilmelidir.
2. **Bulgu 2 (KristalLM Konumu Uygunsuzluğu):** README.md `KristalLM` sınıfını `src/llm/` altında anmasına rağmen, sınıf `scripts/train_step_demo.py:219` içinde tanımlıdır. 40+ dosya bu betiği import etmektedir. Bu borç ileride `src/llm/model.py` gibi kanonik bir konuma taşınarak çözülmelidir.
3. **Akış Kopyalarının Durumu:**
   - `src/rag/epistemic_agent.py`: Koşullama ve prompt inşası kanonik dikişe (`is_context_usable`, `build_rag_prompt_tokens`) bağlandı; üretim döngüsü (`generate_tokens` / entropy_post / logits_processor) epistemik telemetriye sıkı bağlı olduğu için ajan içinde korundu.
   - `verify_rag.py`: Kök dizinde eski test betiği; T-0032 şartnamesi gereği kapsam dışı bırakıldı.
   - `scripts/test_rag_interactive.py`: README'de belgeli bağımsız etkileşimli test aracı; kapsam dışı bırakıldı.
