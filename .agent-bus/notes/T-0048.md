# T-0048 Görev Kapanış Notu (F2-5: Donmuş Yazım Kapısı — Kalan Delikler ve Test Ekseni)

- **Görev ID:** `T-0048`
- **Tarih:** `2026-09-16`
- **Yürütücü:** `antigravity`
- **Durum:** `PASS`

---

## 1. Değiştirilen Dosyalar (`writes` & `changed_files`)
Beyan edilen 6 dosya birebir eşittir:
1. `scripts/run_goal_pipeline.py`
2. `train.py`
3. `train_dpo.py`
4. `tests/test_compiler_entrypoints.py`
5. `data/eval/t0048_frozen_write_holes_2026-09-16.json`
6. `.agent-bus/notes/T-0048.md`

---

## 2. Kapatılan Bulgular ve Değişiklik Detayları

### (A) V1 Kapanışı — `shutil.copyfile` Öncesi Koruma
- **Dosya:** `scripts/run_goal_pipeline.py`
- **Satır 294:** `shutil.copyfile("data/kristal_model.pt", "data/kristal_model_sft.pt")` çağrısından hemen önce:
  ```python
  check_frozen_save_path("data/kristal_model_sft.pt", allow_frozen_write=allow_frozen_write)
  ```
  çağrısı eklendi. Böylece operatör açık onayı olmadan pipeline çalıştırıldığında ara checkpoint dosyası dahi kopyalanamaz/ezilemez.

### (B) V3 Kapanışı — `train.py` Erken Koruma ve CLI Desteği
- **Dosya:** `train.py`
- `check_frozen_save_path` fonksiyonu eklendi.
- `allow_frozen_write = "--allow-frozen-write" in sys.argv` desteği eklendi.
- Argüman ayrıştırma bloğunun hemen ardından (model kurulmadan, ağırlıklar belleğe alınmadan veya diskten okunmadan):
  ```python
  check_frozen_save_path(model_save_path, allow_frozen_write=allow_frozen_write)
  ```
  erken denetimi yerleştirildi. Doğrudan `train.py` komutu çalıştırıldığında donmuş model hedeflenmişse sıfır hesaplama ile anında engellenir.

### (C) V2 Kapanışı — Mesaj Şablonu Hizalaması
- `scripts/run_goal_pipeline.py:50`, `train_dpo.py:35` ve `train.py:18` hata mesajları:
  ```
  f"Donmuş yola yazma engellendi: {save_path} (allow_frozen_write=False)"
  ```
  kalıbına birebir eşitlendi.
- `tests/test_compiler_entrypoints.py:233, 237` bu kalıbı denetleyecek şekilde güncellendi.
- Üç üreticiye dokunulmadı.

---

## 3. Test Ekseni ve Bağımsız Mutant Kanıtları (STATİK)

### Genişletilmiş Testler (`tests/test_compiler_entrypoints.py`)
1. `test_check_frozen_save_path_gate`: `run_goal_pipeline`, `train_dpo` ve `train` fonksiyonlarının üç durumlu (donmuş+False -> hata, donmuş+True -> serbest, donmamış+False -> serbest) davranışını doğrular (KOŞUM).
2. `test_ast_run_goal_pipeline_has_frozen_guards`: 5 adet `run_cmd` eğitim çağrısının ve 1 adet `shutil.copyfile` çağrısının hemen öncesinde sıralı `check_frozen_save_path` varlığını doğrular (STATİK).
3. `test_ast_train_dpo_has_frozen_guards`: `torch.save` öncesinde guard varlığını doğrular (STATİK).
4. `test_ast_train_py_has_frozen_guards`: Argüman ayrıştırmasından hemen sonra erken guard olduğunu ve `torch.save`'den önce yer aldığını doğrular (STATİK).

### Mutant Kanıtları
- **Mutant 1 (copyfile guard kaldırıldı):** AST testi `AssertionError: Beklenen 6 check_frozen_save_path çağrısı (5 run_cmd + 1 copyfile), bulunan: 5` ile **KIRMIZI** oldu. Satır geri konulduğunda **YEŞİL** oldu. Dosya SHA-256 özdeşliği doğrulandı (`2a88708edc0b9a8d156d070371fb43ba9e4f2b26014a72ae2d979f4c49c4d874`).
- **Mutant 2 (train.py guard'ı torch.save'den sonraya taşındı):** AST testi `AssertionError: Erken guard satırı (197) >= torch.save satırı (196)` ile **KIRMIZI** oldu. Dosya geri konulduğunda **YEŞİL** oldu. Dosya SHA-256 özdeşliği doğrulandı (`d08b195d5fd31f288e421ed9f339e950478516bcb0d14fa4ed047d09723c7c93`).

---

## 4. Koşum Yok Kanıtı & Kanaryalar
- Hiçbir eğitim veya pipeline dosyası çalıştırılmadı.
- `data/` altında değişen dosya sayısı: 0 (yalnızca değerlendirme raporu eklendi).
- Kanaryalar korundu:
  - `data/kristal_model.pt`: `02e15b8198f8f3334609eb52691d699e80f88972245377c1a79772cb42ae1679`
  - `data/kristal_model_sft.pt`: `b6764c7062ce627a0ba9437cf052a28d0ac4e2e4cf3b3df3e3110d05a1ba2957`
  - `data/kristal_carpenter_model.pt`: `5f0b8c71727d15e338e4301634b0d05652c3230cebc532b1124aff3761e93b4d`
  - `data/train_balanced_sft_v2.bin`: `53b8406eee7e98d8339cd047a18a281b7a355ca5ba1891137701890dc322402c`
  - `data/train_balanced_sft.bin`: `55863e36b62414c6ada30956b30dc64a5254fe0717582384d43abd5d9cf46a24`
  - `data/train_chat_balanced.bin`: `443f93d352b07defca91b2a620f2e4154c3ed0b14f7c695535c959209e7608fa`
  - `data/train_carpenter_specialization.bin`: `13dd81bf700690b78ca15246a2fc1582ee6f8fea587c6d4e78f8ab1e1017b109`
  - `data/vocab.json`: `4b40eebee9c996c8dd3150f6b3c8896de85764ee1cf0b08e6c636e5c002a0e69`
  - `src/llm/tokenizer.py`: `560a3bfbf32c394a23559c9d3645cfbc3b554223a032156924749dcfa896a7af`
  - `data/*.pt`: 23, `data/*.bin`: 15, `scratch/f2_backup_2026-09-16/`: 6 dosya.
- Tüm test paketi: **190 passed in 10.58s** (189 -> 190).
