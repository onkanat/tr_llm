# Kristal-Vektörel Mimarisi - Migration & Handover State

**To the Gemini CLI Agent on the new Windows/CUDA environment:**
When you load this workspace, read this document to instantly synchronize with the "Kristal-Vektörel Başmühendisi" persona and resume the architectural roadmap exactly where the macOS session left off.

## 1. Architectural State (What has been built)
- **Ontology (Lexicon):** `roots.tsv` has been purified to ~50,000 *pure lemmas*. Derived forms (-mak, -lık, -leş) were removed. It supports homonyms and phonetic attributes (VOICING, VOWEL_DROP).
- **Epistemology (Morphotactics):** `src/compiler/morphotactics.py` features a universal State Machine covering Verb Voices, Gerunds, Modals (Potential/Impotential), Equative Case, and Copula Aorist.
- **Physics (Phonology):** `src/compiler/phonology.py` natively handles Progressive Vowel Harmony and consonant mutations (hardening, voicing) without pre-calculated strings. Tested with 100% success on a golden dataset (`tests/test_phonology_golden.py`).
- **Vector Rover (RAG):** The system uses **Hybrid Search** (Rota C). `VectorMemory` stores both **Dense Vectors** (generated via deterministic cryptographic hash with Positional Encoding and Morpheme Weighting - TF-IDF) and **Sparse Vectors** (BM25 frequencies). 5,000 examples were perfectly processed and stored.
- **Pedagogy (Infancy Dataset):** `scripts/generate_infancy_dataset.py` crawled the vector space and autonomously generated a contrastive RLHF/SFT dataset containing 2,427 high-quality Positive/Negative semantic boundary pairs (Noun+Verb, Adj+Noun, Adv+Verb). Saved in `data/pedagogy/infancy_dataset.jsonl`.

## 2. The Immediate Next Step (Mission Objective)
The project has migrated to a Windows environment specifically to leverage **Nvidia GPU/CUDA** for the training phase.

**Your First Task upon resuming:**
1. Guide the user to set up the Windows Python environment (`venv`) and install CUDA-enabled PyTorch.
2. Build the **NanoGPT Skeleton** (`src/llm/model.py`) using `vocab_size = 21000`.
3. Create the **Dataloader** (`src/llm/prepare.py`) to convert `data/pedagogy/infancy_dataset.jsonl` into PyTorch tensors.
4. Begin building the SFT (Supervised Fine-Tuning) pipeline.

## 3. Restored Private Project Memory
*(The following was stored in the macOS private `memory.md` and MUST be obeyed. Update your local memory if necessary)*

> **Lexicon (roots.tsv) Genişletme Felsefesi**
> Projeye devasa, denetimsiz istatistiksel veri setleri (corpus dumps) dahil edilmemelidir. Bunun yerine "Sıfır OOV" hedefine yönelik olarak kök sözlüğü (`roots.tsv`); manuel olarak denetlenmiş, sadece saf kökleri barındıran ve Türkçe'nin matematiksel/fonetik katı kurallarını (örn. `VOICING`, `VOWEL_DROP`) işletmeye yönelik mikro ölçekte büyütülen deterministik bir sistemdir. Sözlükteki her kelimenin `pos` (Sözcük Türü) ve `attributes` (Nitelikleri) kesin olmalıdır. 

---
**END OF STATE**
